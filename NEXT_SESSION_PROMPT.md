# Next Session Prompt — ACTIVE: #67 World Map navigation (all-continent) — Galbadia catalog empty. `main` (pushed) = v0.18.3.51; LOCAL = v0.18.3.52 (Balamb-nav CI guard, BAT-confirmed, READY TO PUSH). Next concrete step: a `WM_CALIB_DIAG` position-trace to disambiguate the Galbadia bug. #61 queued after #67

## Greeting & ritual

Start every response with `## Claude Says`. Read `DEVNOTES.md` and THIS file before any work; `DEVNOTES_HISTORY.md` only when tracing a past decision. Then `github:list_issues` for the live backlog and confirm HEAD with `github:list_commits`.

## Where we are at session open

**CURRENT STATE — Switch line of work fully shipped; #67 (World Map navigation) is active and its first step is built.** `main` (pushed) = **v0.18.3.51**; **LOCAL (built, not pushed) = v0.18.3.52**. `github:` tools work directly — use them for issues/commits/comments (curl raw.githubusercontent.com still fine for raw file reads).

**#67 step 1 DONE (LOCAL v0.18.3.52) — Balamb-nav regression guard.** Before touching the world-map mapping for the all-continent work, the working Balamb on-foot navigation is now locked behind a real-code CI guard (mirrors `chase-harness`). Pure extraction (NO runtime change): the coord/segment/BFS math (WorldXToSegCol/Row, SegmentCenterToWorld, TorusBearing, GetVehicleType, GetBfsRuleClass, IsCanonicalLocomotion, IsSegmentTraversable, CalculateWrappedDistance, ComputeReachability) moved verbatim out of `world_map_segments.inl`/`world_map_catalog.inl` into new `src/world_map_geometry.inl` (no Win32/SEH, included right after state.inl). New files: `src/world_map_geometry.inl`, `tests/world_map_terrain_grid.txt` (committed wmx.obj terrain snapshot), `tests/gen_world_map_fixture.py`, `tests/world_map_harness.cpp`, and a `world-map-harness` job in `.github/workflows/safety-checks.yml`. The harness compiles the REAL geometry and asserts Balamb on-foot reaches Garden/Town/Fire Cavern, excludes Dollet (Galbadian continent), exactly 3 catalog locations. Container-verified PASS + negative-control FAIL. **BAT-CONFIRMED v0.18.3.52** (Garden→Balamb Town auto-drive worked — the extraction is behavior-preserving). **READY TO PUSH** (.52 carries the guard); first action next session is to confirm it landed via `github:list_commits` and that the new `world-map-harness` CI job went green. When the #67 fix lands, add a 2nd assertion flipping Galbadia from empty → Deling/Timber/Galbadia Garden present.

**#65/#66 Switch screens — DONE / PUSHED v0.18.3.51 / #66 CLOSED** (#65 was already closed). The main-menu Switch (menu mode 6) and the forced party-select screen (game mode 10) are the SAME UI, driven by one memory-first engine `PollSwitchScreen(state, off)` in `menu_tts_switch.inl` — wrappers `PollForcedPartySelect` (off=0) and `PollSwitchSubmenu`/`ResetSwitchSubmenu` (off=0x78, gated `s_prevCursor==6 && +0x1E8==10`); working blocks identical, shifted +0x78 in the main menu. Shipped over .49 (unify) + .50 (entry-delay fix — entry gate now accepts "Select party members" as the screen-ready signal) + .51 (release cleanup — `FORCED_PSEL_DIAG`→0, `SWITCH_DISCOVERY_DIAG` removed). All diagnostics off; nothing Switch-related outstanding. (Optional future, NOT a bug: reserve names prefer the GCW token with +0x1ED fallback — works as-is; memory-only would be its own BAT.)

## Next pick: #67 — World Map navigation, all continents

**Found in play-testing:** World Map navigation works on the starting continent but **not on Galbadia** — the catalog comes up empty there. **Root cause already diagnosed from the v0.18.3.51 `ff8_world.log` (no build needed):** on Galbadia the live position (e.g. -29270,-24056) maps via WorldXToSegCol/Row onto a wmx grid cell classed OCEAN, as do all neighbors, so on-foot BFS reaches 1/768 and BuildDistanceCatalog filters to 0 reachable. Every Galbadia destination (Deling seg(20,8), Galbadia Garden seg(20,11), Timber seg(23,13)) sits on a dead cell. The terrain grid itself loaded fine (185 land/10 forest/573 ocean). The bug correlates with **sign of X**: positive-X Balamb maps onto land (works); negative-X western locations map onto ocean (broken). Catalog coords and live position share the SAME engine frame (`FF8_EN.exe+1C3EE80`), so it is NOT a catalog-vs-live mismatch.

**Two candidate root causes — do NOT guess between them (v0.14.73):** (a) the coord-mapping offset is skewed for the western hemisphere, or (b) the terrain classifier mislabels Galbadia land as ocean. **NEXT CONCRETE STEP: build a blind-accessible `WM_CALIB_DIAG`** — as Aaron walks Galbadia on foot, log live pos → seg(col,row) → terrain class + the wmsetus region byte for that cell. That disambiguates (a) vs (b) and measures the correction. (Goal/full scope in issue #67: all-continent nav + auto-drive, per-conveyance reachability foot/chocobo/car/Garden/Ragnarok, proper location catalog.) Aaron has a Galbadia world-map save ready for diag builds. Then fix, and extend the `world-map-harness` with the Galbadia assertion. `github:get_issue` #67 for the full scope; consult `Plan & Research Documents/World Map Location Coordinates Research Findings.md` before interpreting map data.

**Queued after #67 — #61:** `FF8TextDecode::Decode` renders the page/line-break control code as a spoken "L" game-wide (root site identified; v0.18.3.28 was a narrow speak-layer workaround only, so the real fix belongs in the decoder).

**Open backlog (not picked):** #50-#53 (Rinoa/Angelo gauges + name-variable dialog bugs), #37 (source-file-size refactor queue), Irvine Shot limit page (on hold until he joins the party). #45 blocked (no GF teaches a non-command ability yet).

---

**(Historical detail below — the incremental #65/#66 path that led here, superseded by the unify above.)**

**#65 Switch submenu (main-menu) — SHIPPED & CLOSED** at v0.18.3.37 (`b342a5df`, push verified). **ACTIVE CHAPTER — #66: forced party-select / junction-switch story screens** (follow-up). Research doc: Switch Member is "only available on world map or at forced party-select story points" → the forced screens likely reuse the same party-select UI (`+0x1E8==10`, focus `+0x22E`==2 member list, same `<Name>LVHP` layout) but WITHOUT the main-menu context (`PollSwitchSubmenu` gates on `s_prevCursor==6`). **Hypothesis:** broaden the gate to `+0x1E8==10` alone → the forced screen may reuse the whole `.37` announce path. **UNKNOWNS (discovery BAT at a story-forced select):** is `pMenuStateA` populated, is `+0x1E8==10`, does the layout match. GCW clue: exiting Switch at Yaulny Canyon showed help "Select party members".

**CONFIRMED (v0.18.3.34 `[SwitchDiag]` BAT, 2026-06-12):** Switch subsystem **`+0x1E8 == 10`**. Focus **`+0x22E`**: **12 = two-option action bar**, **2 = member-select list**. Action option **`+0x25E`**: **0 = Switch Member** (help "Please make a party of 3"), **1 = Junction Exchange** (help "Exchanges all that is junctioned"). The member list renders the 3 active members + the highlighted candidate as `<Name>LVHP` tokens (4th token = cursor target). A swap rewrites a working buffer (`+0x262`/`+0x265` hold char-ids, e.g. `1↔3` = Zell↔Quistis) and the on-screen trio; the roster `+0x1DB` reorders on EXIT (active = first 3). **STILL OPEN:** the source/destination cursor bytes (the `+0x24C`/`+0x24E`/`+0x262` cluster, tangled in the one busy capture) and the savemap active-party commit (`+0xAF1`/`+0x0B04` never fired — commits as the screen tears down, after the diag un-gates).

**v1 build** = `menu_tts_switch.inl` (`PollSwitchSubmenu`, included from menu_tts.cpp, gated `+0x1E8==10`): action-bar option from `+0x25E` + help; member list announces the active trio on entry, the candidate on cursor move, and the new party after a swap. Reads names by scanning the GCW for known `CHAR_NAMES` (no dependence on the unmapped cursor bytes). **All BAT-confirmed working** through `.37`: `.35` = v1 (Switch Member + Junction Exchange both, incl. Exchange list parse); `.36` = polish (post-swap interrupt fix; exact labels "Switch Member"/"Junction Exchange"; `SWITCH_DISCOVERY_DIAG`=0); **`.37` = level + HP** — announces "Name, active/reserve, Level N, HP X of Y." via `SwitchCharLevelHP` (POD+SEH; EXP→level flat 1000; HP from computed-stats `0x1CFF000` for live-formation members else menu HP array `+0x71E` for benched; char-id from the name scan).

**#66 LOCAL = v0.18.3.48 — forced party-select; BAT PENDING.** Switch Member screen in **game mode 10**; `PollForcedPartySelect` (`menu_tts_switch.inl`, top of `Update`, self-gates `gameMode==10`). **BAT-confirmed through .45:** the live working-party read (filled active slots + reserve names correct), the empty-slot announces ("Empty Party Slot" / "Empty Reserve Slot"), and the action-bar option read. Live party at `pMenuStateA+0x1EA` (3 active slot char-ids, 0xFF=empty; reserves from `+0x1ED`; `+0x1EC`=slot 3 swap-confirmed) via `ForcedPselActiveSlots`/`ForcedPselIsActive`. Empty reserve slots: reserve id read from `+0x1ED` indexed by the reserve cursor `+0x1E9` (the 2×4 portrait grid, indices 0–7, maps 1:1 onto the array; 0xFF=empty) via `ForcedPselReserveId`, with a frozen-GCW fallback that names a present member from `CHAR_NAMES[rid]`. Option read from the authoritative byte `pMenuStateA+0x1E6` (0=Switch Member, 1=Junction Exchange).

**.46 → .47 (action-bar toggle + auto-announce on landing):** `.46` fixed the toggle re-reading the character on every flip — the toggle now speaks the OPTION NAME ONLY ("Switch Member." / "Junction Exchange."); toggling changes only `+0x1E6`, the cursor doesn't move. `.47` adds the feature Aaron wanted: when focus drops from the action bar back onto a character, announce that character + level/HP, even if it's the same slot (the on-screen cursor was hidden on the bar, then reappears on landing). `.47` is CONFIRMED working by ear. `.48` adds the mirror (Aaron's request): moving focus UP onto the bar announces the current option name ("Switch Member" / "Junction Exchange") via `movedToBar` (onBar && prev focus==2), so arriving on the bar is no longer silent until you flip.

**FOCUS byte FOUND (v0.18.3.46 BAT, clean isolated up/down motion): `pMenuStateA+0x1B6`** — `2` = character grid focused (hand on a character), `0x0C` = action bar focused (hand on the tabs, no character cursor drawn), `0x0B` = transient while moving up. It sat just below the old `+0x1C0` diagnostic window — that's why the .44/.45 traces missed it; widening to `+0x100..+0x500` surfaced it. In the capture `+0x1B6` was the ONLY byte toggling `02↔0C` at the press cadence and stayed `02` through all character navigation — unambiguous. `+0x22E` (the main-menu Switch focus byte) stays constant in mode 10, so it is NOT the forced-screen focus byte. New `ForcedPselFocus()` reads `+0x1B6`; `PollForcedPartySelect` tracks `s_fpsPrevFocus` and fires on both transitions: bar→character (`focus==2 && prev∈{0x0B,0x0C}`) announces the focused character, and character→bar (`onBar && prev==2`) announces the current option.

**BAT (v0.18.3.48):** build → load the pre-battle save → win → from a character go Up onto the action bar (confirm it announces the current option), flip Switch ↔ Junction (option name only), then come Down onto a character (the character announces on landing). Send the `[ForcedPSel]` lines if anything's off. `FORCED_PSEL_DIAG`=1 stays on this BAT.

**THEN (push prep):** map the reserve index→char-id fully off `+0x1ED` so reserve names never depend on the GCW, set `FORCED_PSEL_DIAG`=0, and push (this likely also covers other story-forced selects — same mode-10 screen).

---

**MOST RECENT — v0.18.3.33 PUSHED (`c9a4c777`) and #64 CLOSED. Battle diagnostic cleanup arc COMPLETE.** #64 was the third and last leftover battle diagnostic capture: the Scan-UI screenshot (`HookedScanGetText` / sub_B687C0 hook in `scan_tts.cpp`) that wrote a timestamped `Logs/screenshots/scan_*.bmp/.png` per Scan cast, now gated behind `SCAN_DIAG_SCREENSHOTS` (scan_tts.h, default 0). The `OnScanPopupSpawn` announce and all `s_scanCache` reads stay outside the gate. BAT-confirmed 2026-06-12 (Initialized v0.18.3.33; Scan cast with GF-HOOK `scan=`0→16; `Logs/screenshots/` stayed empty), verified on `main`. All three capture families now gated, each behind its own default-0 flag: `BATTLE_DIAG_SCREENSHOTS` (#62), `BATTLE_DIAG_LOGGING` (#63), `SCAN_DIAG_SCREENSHOTS` (#64). **ONE local-only doc fix pending** (not pushed): the v0.18.3.33 CHANGELOG insert accidentally dropped the `## v0.18.3.32` heading (so the .33 commit message absorbed the .32 body) — heading restored locally; cosmetic, will ride the next version bump's push. **FIRST ACTIONS NEXT SESSION:** pick from the open backlog — **#61** (the field text decoder renders page/line-break control codes as a spoken "L" game-wide; root `FF8TextDecode::Decode`; self-contained, confirmable via ff8_dialog.log) is the cleanest standalone pick. Also open: #50/#51/#52/#53 (Rinoa/naming/item-target bugs), Irvine Shot limit page (on hold).

**TIMBER TRAIN HIJACK CHAPTER (#60) — COMPLETE, PUSHED, ALL ISSUES CLOSED.** Shipped through v0.18.3.30 in commit `bb3ba05` (parent `3515729`=v0.18.3.21; the whole .22–.30 batch squashed into that one push). Fully playable blind via three modes chosen at an in-engine ASK on the train roof: **Manual** (default — guards move + code announce + per-guard proximity cues), **Freeze** (guards held, just enter codes), **Skip** (bypass to the Forest Owls' base, deliberate −1 SeeD-rank cost). Code announce, "/" key-layout readout, Rinoa's "L L L L" instruction fix, guard cues, and the 5-minute timer (T=announce, Shift+T=freeze, auto-cues 1:00/0:30, leftover-active fix) are all in and BAT-confirmed. **#56/#57/#58/#59/#60 all CLOSED** with version-paired comments. Code lives in: `train_mode_ask_overlay.*`, `field_dialog_lifecycle.inl` (TrainCodeAnnounce/TrainCodeKeyHelp/GuardFreezePin), `field_nav_observe.inl` (GuardManualCue), `field_dialog_helpers.inl` (ApplyTrainCodeKeyFix), `countdown_timer.cpp`. Mode enum `{TGM_MANUAL=0,TGM_FREEZE=1,TGM_SKIP=2}` (field_dialog.h); INI `train_guard_mode`. Optional, deliberately NOT built (file a fresh issue if wanted): full-automation "Auto" mode; the cosmetic mode-ASK box width clip (TTS unaffected).

**Bug filed this session — #61:** the field text decoder renders the page-break / line-break control code (and the directional-button sprites) as a literal "L", so a stray "L" gets spoken mid-dialogue game-wide. Root-cause site `FF8TextDecode::Decode`; the v0.18.3.28 train fix was only a narrow speak-layer workaround. Self-contained, confirmable via ff8_dialog.log — a good standalone pick-up.

**NEXT — candidates (Aaron picks):** **#61** page-break "L" decoder fix; **#50** Rinoa Angelo gauges (deferred follow-up to #49); **#51/#52/#53** Rinoa naming / item-target bugs; **#37** source-file refactor queue. **ON HOLD until Irvine joins the party:** his Shot limit page (flip `ST_LIMIT_DIAG`→true in `menu_tts_status.inl`, reuse the #49 limit-page infra), then circle back to the status screens.

---

**↓↓↓ ARCHIVED Timber discovery scaffolding (chapter is DONE — every ">>> … pending BAT" block below is BAT-confirmed + pushed in `bb3ba05`; safe to prune next session). ↓↓↓**

**ACTIVE CHAPTER: Timber train hijack minigame accessibility (#60 umbrella).** Three-mode design (Auto/Manual/Original) mirroring the X-ATM092 chase, injected at the Watts "Are you ready, Squall?" ASK. Sub-issues filed: **#56** announce Rinoa's uncoupling code numbers (FIRST target), **#57** code-entry key layout, **#58** guard awareness, **#59** timer freeze+readout. Sequencing: code-entry mechanic FIRST (mappable on the briefing-room PRACTICE panel — no timer/guards, repeatable; and required before the live train fields are reachable), then guard/timer suppression for the live fields, then the mode ASK + Auto automation.

**IMMEDIATE NEXT ACTION — runtime read of the 4 code vars (#56).** LOCAL chapter; GitHub HEAD still `faa31be`/v0.18.2.50. The v0.18.3.2 `[SCRIPT-DUMP]` JSM dump (BAT 2026-06-09) **located the code**. Practice field `tiagit5` (15 entities, all Other); apparatus = **Angoyarukun (ent12)** orchestrator, **Keykantoku (ent13)** translate+draw-dispatch, **Keyjokantoku (ent14)** sprite draw + random-gen (opcode 0x21) + validation. **The 4 displayed digits are field-varblock vars 1026, 1027, 1028, 1029, holding values 1-4** (Keykantoku `method[4]`/LBL89 buckets a raw source vs 64/128/192 -> digit 1-4 via `POPM_L`; Angoyarukun copies each -> scratch var 1030 -> `REQEW` Keykantoku to draw). Varblock base **0x1CFE9B8** (FFNx `field_vars_stack`; already used by the mapjump resolver), read `*(int16_t*)(base+addr)`. CAVEAT: the digits use `POPM_L` (LONG) at consecutive indices 1026-1029 -- raw byte-offset would overlap, so the long-bank addressing is unconfirmed; pin it empirically. Full record: the #56 comment.

**ADDRESS CONFIRMED (v0.18.3.3 `[CODEVAR]` BAT 2026-06-09):** the 4 digits are **bytes at `0x1CFE9B8 + 1026/1027/1028/1029`, values 1-4, left-to-right** -- `byte@idx` was the only candidate reading four 1-4 values cycling on the ~5s refresh (word/long/idx*2/idx*4 all read garbage or zero). Stable quads logged this run: `[4 4 3 3] [4 1 2 2] [4 2 4 2] [1 4 3 3] [2 1 3 4] [1 4 2 4] [3 1 2 3]`.

**v0.18.3.4 BAT'd 2026-06-09 -- WORKS on the practice panel.** `TrainCodeAnnounce()` (field_dialog_lifecycle.inl, from PollWindows, gated `tiagit*`): reads bytes 0x1CFE9B8+1026..1029; when a new code settles (held 3 polls) speaks four words via `ScreenReader::Speak` **assertive/interrupt** ("Code. four, three, three, one."), once per settled code. Aaron heard the codes, input them (keys per #57: 1=Right,2=Down,3=Left,4=Up -> W/A/D/X), and completed the practice. **Digit ORDER confirmed correct** (left-to-right) by successful entry. All 3 discovery diags gated off.

**v0.18.3.5 BAT'd -- real code field = `tilink1` (id 902).** Field-load trail: tiagit1/3/4 -> tiyane1/2/3 -> titrain1 -> **tilink1**. `tilink1`'s SYM has the apparatus (`Angoyarukun`/`Keykantoku`/`Keyjokantoku`). The widened gate announced there, BUT 1026-1029 read `[1 1 1 3]`/`[1 1 1 4]` -- three pinned 1s, only the last digit moving = NOT the live code (the practice never produced 3 identical leading digits). So on `tilink1` the digits sit at DIFFERENT var indices.

**TIMBER CODE CHAPTER DONE -- #56 CLOSED + #57 resolved.** v0.18.3.7 BAT'd: the real-train code announce works -- `tilink1` vars **1029-1032** (practice `tiagit*` 1026-1029, shifted +3); Aaron entered the spoken codes and uncoupled the car (`[TRAINCODE-SAY]` 2221/4334 matched `[TRAINWIN]`). `TrainCodeAnnounce` (field_dialog_lifecycle.inl) selects the base per field. #57 key map = 1=Right(D), 2=Down(X), 3=Left(A), 4=Up(W), Q=quit. **v0.18.3.8 SHIPPED** (HEAD `1b32bf3`, pushed 2026-06-10): cleanup -- turned off `TRAIN_FIELD_SCAN_DIAG` (`[TRAINWIN]`/`[TRAINSCAN]`) + `GUARD_RECON_DIAG` (`[GUARDPOS]`); both retained behind their `#define` for one-line re-enable.

**#58 GUARDS -- ACTIVE, v0.18.3.14 BAT-CONFIRMED (Manual WORKS). The guards DO catch Squall** if they get too close during code entry (Aaron confirmed from playing -- a real mechanic, NOT decorative; do not re-conclude otherwise). **The failure = a field ASK dialog** (`[AASK]` in `ff8_dialog.log`): "Rinoa: What happened!? Squall!!!" + two choices ("it's nothing" / "I failed. It's all over"); NOT a mapjump -- the field just freezes (guards stop, ASK opens). The same fail-ASK is shared by guard-catch AND timer-expiry. **v0.18.3.11 get-caught run (analyzed):** Squall stationary at the panel, guard ent5 (=GalHei, model 5) closed to dist **70-78** (`[GUARDPOS]`), fail-ASK fired ~4s later. Leading read = a GUARD catch at threshold ~70-80u with a ~4s scripted reaction delay (only ~34s since the "minutes left" line argues against timer-expiry) -- NOT yet confirmed; a clean run still wanted. **Static dump cleared entities 0-17** (party/NPC sprites; Downyaru/Upyaru = on-screen arrow SPRITES, not controllers; TrainSindou=shake; hatchcont/view=down-up+camera, var 1044; Noriuturiline1=code-entry line, var 1043 + savemap 724) -- none holds a guard-proximity check or the fail-ASK, so the trigger is in **entities 18-30** (ten `blind*` invisibles + `hatch`/`point`/`light`), which sat in an unreadable mid-log zone. v0.18.3.10's by-name dump (worked great -- lands at top of a clean log) showed the catch is NOT in GalHei1/GalHei2 (decorative `Background` sprites, BGDRAW 0x099 only), TrainSindou (train-rumble controller, *sindou*=vibration -- random/anim loop, vars 1041/1042), or point (tiny scenery orchestrator REQ-ing hatch/light). So the detection lives in another entity (prime suspects: `blind1`-`blind10` invisibles, Downyarukun/Upyarukun train-movement entities) or is engine-level proximity. **v0.18.3.12 BAT DONE (2026-06-10) -- entities 18-25 mapped.** **The patrol guards are blind2 (ent19, SETMODEL 5) + blind3 (ent20, SETMODEL 6)** -- invisible Other entities that walk the corridor (spawn (-1315,-505)/(-1312,93)); these ARE the recon's moving model-5/6 "ent5/ent6", NOT GalHei (which are static Background sprites). Their method[1] is a rail MOVE loop (opcodes 0x3C/0x3D/0x3E/0x65/0x67) gated on **var 1040**; they do NOT read player position. **blind4 (ent21) = minigame master:** method[1]=init (zeroes entered-code accumulators 1024-1027, code-entry var 1043, patrol var 1040; REQEWs the helper entities); **method[4]=FAIL/RESTART -> MAPJUMP3 to field 902 (tilink1 itself = reload), spawn picked by savemap 724**; **method[5]=SUCCESS -> MAPJUMP3 to field 925**. **blind5/6/7 (22/23/24) = per-digit validators** (gated var 1042, read accumulators 1024-1026). **blind8 (25) = code DISPLAY** (reads the live code digits 1029-1032 into scratch 1033, draws via REQEW param=12). **Guard master switch = var 1040** (set by blind4, read by blind2/3/8). **Round-active flag = var 1042.** NO opcode 0x4A (ASK) anywhere in entities 0-25, so the fail dialog "Rinoa: What happened!? Squall!!!" lives in **blind9/blind10 (26/27, still UNREAD -- head=1680 exceeded the inline read limit)** or is REQ'd as part of the method[4] reload. **NEXT: (a) narrow the dump to entities 26-30** (blind9/blind10/hatch/point/light) to find the 0x4A + any player-distance check; **(b) a runtime diag to confirm which value of var 1040 FREEZES the guards** (the suppression lever). **Three modes (refined w/ Aaron 2026-06-10):** **Auto/Skip** = skip the minigame entirely -- fire the game's own success path (`blind4` method[5] -> MAPJUMP3 to field 925) and replicate the success savemap flags so the story + SeeD state stay correct. **Manual** = player enters the code; guards FROZEN (hold var 1040 at its idle value). **Original** = player enters the code; audible indicator when guards approach/recede (off the recon distance to the model-5/6 movers blind2/blind3). **v0.18.3.13 BAT DONE (2026-06-10) -- guard switch + catch CONFIRMED.** `[GUARDVAR]` on a real run: during code entry **var 1040=1** (guards patrol) and **1042=0**; at the CATCH **1040 flips 1->0, 1042 flips 0->1, 1038->3, 1028->100, entered[1024-27] reset to 0000** and the field FREEZES (no mapjump; fail-ASK opens -- the log keeps emitting [GUARDVAR] in the frozen state). The catch fired as the moving guard entity closed to ~talk-radius: `[GUARDPOS]` showed the mover (this run the model-3/4 movers -- model labels differ from last session's 5/6; the recon's nearest MOVING entity is what matters) reach distToPlayer ~139 (talk radius 128) -> round ended. Static ent2 sat at dist 68 the whole run and NEVER caught -> proximity alone doesn't catch; only the MOVING guard entering its talk radius does. **=> MANUAL SOLVED: force var 1040=0 to freeze the guards** (the code validators gate on var 1042, not 1040, so code entry still works with guards frozen). **ORIGINAL SOLVED: announce the moving guard's distToPlayer, warn as it crosses ~150->128.** code[1029-32] = the live target (matches [TRAINCODE-SAY]); entered[1024-27] = digits entered so far. NO opcode 0x4A in entities 0-27 (blind9 = code-input/generator that writes 1029-32 from the dial; blind10 = display) -> the fail dialog is NOT a tilink1 script; it's on the post-catch destination or REQ'd via blind4 method[4]. **SKIP still needs the WIN flag pattern** (this run captured the CATCH, not a clean uncouple) -- either capture a win-only run, or design Skip by driving blind4 method[5] (MAPJUMP3 -> field 925) + replicating its savemap writes (re-read the method[5] dump from the v0.18.3.12 log). **v0.18.3.14 BAT-CONFIRMED (2026-06-10) -- MANUAL mode WORKS.** Aaron entered BOTH code sets and completed the whole train minigame with zero guard catches and no need to avoid them. `GuardManualFreeze()` (field_dialog_lifecycle.inl, from PollWindows) pins var 1040=0 on `tilink*` each poll; INI key `train_guard_mode` [Accessibility] (0=Original,1=Manual,2=Skip), **default Manual**. The gate `_strnicmp(fn,"tilink",6)` matches BOTH stages -- **tilink1 (id 902, first code car) AND tilink2 (id 903, second code car)** -- so the freeze covered both code sets; [GUARDPOS] on tilink2 showed the guards fully frozen (static Y) through the post-uncouple cutscene. Note: `[GUARDVAR]`/`[GUARDFREEZE]` only log while PollWindows runs (field mode), so cutscene/transition stretches show only [GUARDPOS]. **Win destination found:** the post-uncouple MAPJUMP3 fires on `tilink2` -> **destField=892 (Timber - Forest Owls' Base 1)** (NOT 925 as the static dump suggested -- revisit for Skip). JSM dump now off (`GUARD_JSM_DUMP_DIAG` 0); [GUARDVAR]/[GUARDPOS] retained. **v0.18.3.20 BAT-CONFIRMED (2026-06-10) -- ORIGINAL mode, Y-AXIS discriminator. WORKS.** Aaron (repeated request) + the F11 screenshots (`Logs/screenshots/f11_193625_766.png` roof view, `f11_193634_044.png` code-entry view) settled it: the train runs LEFT-RIGHT; when Squall drops to the code panel he's at the BOTTOM of the screen (lower coupling) while the party stay UP ON THE ROOF. So a party member can stand horizontally right above Squall -- Euclidean-NEAR -- yet be FAR from him on the depth/Y axis. Every distance-based attempt (v.15-.19) cannot separate those, which is why the party kept leaking. **v0.18.3.20 judges proximity on the Y axis ALONE: |entity.Y - player.Y|.** The guards patrol the interior corridor so their Y sweeps straight through Squall's (down to ~90, the corridor width = catch); the roof party sit at |dY| >= ~1360 and stay there. Thresholds on |dY|: close <=480, approaching <=960, clear >=1152 (hysteresis 960..1152); anything past 1152 (the whole roof party) is ignored and never labelled. Motion gate kept as a secondary guard vs a static same-lane prop; per-guard lowest-free labels released on recede. (Field axes are ROTATED -- this Y axis reads as HORIZONTAL on screen, hence the earlier "vertical" mislabel.) **tilink1 coords (v0.18.3.18 BAT):** player Y=1509; guards ent5/ent6 sweep Y -505..1662 (cross 1509, |dY|->~0); party ent1 Y=-415 (|dY| 1924), ent2 Y=145 (|dY| 1364), static. **RESULT (BAT-CONFIRMED):** Aaron heard EXACTLY two guards per car (both tilink1 and tilink2), no party false alarms, and the "approaching" lead time was "just right." Original mode DONE; with Manual (v0.18.3.14) that closes the two guard-awareness pillars of #58. **DIAGNOSTICS CLEANUP DONE (v0.18.3.21):** turned OFF `GUARD_RECON_DIAG`=0 (`[GUARDPOS]`, field_nav_observe.inl) and `GUARD_VAR_DIAG`=0 (`[GUARDVAR]` + the `[GUARDFREEZE]` Manual confirmation, field_dialog_lifecycle.inl); both retained behind their flags for one-line re-enable. The Manual var-pin and the `[GUARDCUE]` feature cue (level-change only, low volume) are unchanged -- NO behavior change, log is now clean during tilink play. **v0.18.3.21 PUSHED 2026-06-11 (HEAD `3515729`; parent 1b32bf3=v0.18.3.8 -- .9-.21 squashed into this push). #58 CLOSED (guard awareness shipped: cue + suppression).**

**ACTIVE NOW = the in-engine mode ASK (#60), REVISED 3-mode scheme (Aaron 2026-06-11):** drop the fully-vanilla option (useless to a blind player -- without the code announce they're stuck). Three options, displayed in THIS ORDER, #1 DEFAULT:
  1. **Manual** -- guards move; code announce + per-guard proximity cues (= behavior currently coded as mode 0 / old 'Original' = GuardOriginalCue).
  2. **Freeze** -- guards held; player just enters codes (= behavior currently coded as mode 1 / old 'Manual' = GuardManualFreeze).
  3. **Skip** -- bypass the train scene entirely (mode 2, NOT yet built).
**Numeric INI mapping UNCHANGED** (0=guards+cue, 1=frozen, 2=skip); only the user-facing LABELS + the DEFAULT change (default now 0, was 1). **RELABEL DONE in v0.18.3.22:** enum is now `{ TGM_MANUAL=0, TGM_FREEZE=1, TGM_SKIP=2 }` (field_dialog.h); `GuardOriginalCue`->`GuardManualCue` gating `TGM_MANUAL` (field_nav_observe.inl); `GuardManualFreeze`->`GuardFreezePin` gating `TGM_FREEZE`, plus default/bounds/`[TRAINMODE]` log strings (field_dialog_lifecycle.inl). Behavior-preserving per INI value; default flipped 1->0. NOT yet built/pushed (v0.18.3.22 is local).
**ASK BUILT v0.18.3.23 (pending BAT).** New module `train_mode_ask_overlay.h/.cpp` (mirrors chase_ask_overlay). **Trigger PINNED from the capture run (ff8_dialog.log + ff8_field.log, 2026-06-11):** Watts' 'Are you ready, sir!?' is an AASK in **tiagit1 (field 892, Owls' base room)**; answering 'Yeah' -> MAPJUMP3 -> **tiyane1 (field 930, 'Timber - Train 3')**, where AMESW 'Rinoa "Squall, over here!"' plays. So trigger = strstr `Squall, over here!` gated to field `tiyane1`, once per mission, re-armed on return to tiagit1. OnDialogText forwarded from the show_dialog hook (mode==1) next to ChaseAskOverlay's; Initialize/Update/Shutdown wired in dinput8.cpp; `train_mode_ask_overlay.cpp` added to deploy.bat source list; forward-decl in field_dialog.cpp. Choices Manual/Freeze/Skip (default Manual, slot 2, announceCommit=false) -> `FieldDialog::SetTrainGuardMode()` (Manual->0, Freeze->1). **Skip's bypass NOT built yet -> ANSWER_SKIP currently falls back to Freeze with an honest spoken note** (change that case when the real bypass lands). The overlay closes itself if the player leaves tiyane1 without answering (default mode stands). **BAT next:** load before the Watts briefing, say Yeah, confirm the ASK opens on the train roof, navigate arrows + X, pick each option; check `[TrainModeAskOverlay]` lines in ff8_field.log + `[TRAINMODE] set to` for the mode write. If the tiyane1 cutscene advances before the player answers and cuts the ASK off, move the trigger earlier or add handling. If it works, push (already v0.18.3.23). >>> UPDATE 2026-06-11: **BAT-CONFIRMED + visually verified.** Heard the ASK, navigated all three, selected Freeze, completed the train with guards frozen. F11 screenshot (Logs/screenshots/f11_222427_753.png) shows the ASK rendering as a native dialog on the train roof with the hand cursor on Manual and Rinoa's line beneath. Minor cosmetic only: prompt + option labels clip at the right window edge (box too narrow); TTS reads full labels so blind play is unaffected -- fix later by shortening labels or widening the DialogInject window if a clean visual is wanted.

**SKIP -- discovery capture shipped v0.18.3.24 (pending one WIN capture run).** Plan = true bypass: on Skip, warp to where a successful uncouple returns the player + replicate the persistent story state (don't guess flags -- v0.14.73 rule). Added a logging-only diag `TRAIN_SKIP_CAPTURE_DIAG` (ON) in train_mode_ask_overlay.cpp: reuses the overlay's field-change detection to snapshot the savemap (base 0x1CFDC5C, 4 KB) at tiyane1 (mission start) and again at the first non-roof field after the tilink code cars (= the success return field), then logs the byte diff to ff8_field.log as `[SKIP-CAP]`. **CAPTURE RUN:** Aaron plays the train ONCE start->finish normally (Freeze is fine), winning (no catch). Then read ff8_field.log `[SKIP-CAP]` lines: note the return field name + the changed savemap offsets. Ignore gametime + step-counter churn (steps near 0xD64); the story/progression flag is a small targeted change (byte 0->1 or +1, or a set bit). **THEN implement Skip:** ANSWER_SKIP -> write the captured flag(s) to savemap + MAPJUMP to the return field, and drop the interim Freeze fallback in CommitChoice. If the diff has no obvious story flag inside 4 KB, widen SKIP_SNAP_LEN. Mind the no-catch SeeD-rank-up bonus (a win may grant it; a raw skip won't).

>>> CAPTURE DONE (v0.18.3.24 BAT, run ~23:1x). **WIN PATH CONFIRMED:** tilink2 (field 903) MAPJUMP3 at IP=1048 (0x418), inline_param 0x3E -> destField=892 (Timber - Forest Owls' Base 1) = tiagit1 (VM stack [8]=892=destField, transition_type=1). The save FILES were compressed (94% byte-diff, useless) so we used the in-memory [SKIP-CAP] capture instead. **[SKIP-CAP] AFTER captured at tiagit1; 29 changed savemap bytes (base 0x01CFDC5C), tiyane1 baseline -> tiagit1 return:** 0x000:67>6A | 0xCCC:6A>1E 0xCCD:9F>A2 0xCD0:00>9D 0xCD1:00>01 | 0xD60:88>50 0xD61:11>17 | 0xD64:96>5E 0xD65:2C>32 | 0xDAC:6E>D0 0xDAD:05>01 | 0xDB0:7E>87 | 0xE10:5E>26 0xE11:01>07 | 0xE1F:01>FF 0xE20:02>FF | 0xE23:18>23 | 0xE25:01>00 | 0xE29:14>DC | 0xE2B:00>01 | 0xE31:18>23 | 0xE34:02>00 | 0xE36:FF>00 | 0xE38:10>3C | 0xE40:00>FF 0xE42:00>FF 0xE44:00>FF | 0xE50:D1>A0 0xE51:01>00. **NOISE (ignore):** step/clock counters all jumped by exactly +1480 (0x5C8): 0xD60 (u16 4488->5968), 0xD64 (11414->12894 = step count), 0xE10 (350->1830). **LAYOUT:** the field var bank starts at savemap+0xD5C (varblock abs 0x1CFE9B8 = base+0xD5C); the train's TEMP vars are 1024+ (0x400+ = savemap 0x115C+, OUTSIDE the 0x1000 capture), so every 0xD5C+ change captured here is a GLOBAL/persistent var (id 0-1023), NOT a train temp. So changed globals = savemap 0xDAC,0xDB0,0xE1F-0xE51 -> var ids 0x50,0x54,0xC3,0xC4,0xC7,0xC9,0xCD,0xCF,0xD5,0xD8,0xDA,0xDC,0xE4,0xE6,0xE8,0xF4,0xF5. **No single tidy flag** -- the win writes MANY persistent globals (var 0xCF: 0->1 is the most flag-like; var 0xC3-C4 -> 0xFFFF; 0xCD0-0xCD1 fixed-savemap 0->413). **SKIP PLAN:** warp to field 892 + replay the persistent delta (set all non-step/time changes to their AFTER values). Needs a NEW programmatic-warp capability -- call the engine field-transition the mod already hooks (FieldNavigation MAPJUMP-HOOK; transition_type=1, destField=892). TEST forward using save24 as the backup; if the post-mission story (President-dummy reveal / debrief) misbehaves, dump blind3(ent18) m5 success-script POPM_W writes on tilink2 for the exact minimal flag set rather than the whole delta.

>>> SKIP BUILT v0.18.3.25 (pending BAT). `ExecuteSkipBypass()` in train_mode_ask_overlay.cpp. (1) Writes the 23-byte persistent delta to savemap base 0x01CFDC5C (the 6 step/clock bytes 0xD60/0xD61/0xD64/0xD65/0xE10/0xE11 omitted): 0x000=0x6A; 0xCCC=0x1E 0xCCD=0xA2 0xCD0=0x9D 0xCD1=0x01; 0xDAC=0xD0 0xDAD=0x01 0xDB0=0x87; 0xE1F=0xFF 0xE20=0xFF 0xE23=0x23 0xE25=0x00 0xE29=0xDC 0xE2B=0x01; 0xE31=0x23 0xE34=0x00 0xE36=0x00 0xE38=0x3C; 0xE40=0xFF 0xE42=0xFF 0xE44=0xFF 0xE50=0xA0 0xE51=0x00. (2) Writes the engine transition-request block (layout from field_nav_mapjump_diag.inl): 0x01CE4762=892(dest) 0x01CE4764=66(X) 0x01CE4766=64760(Y) 0x01CE4768=0(Z) 0x01CE476C=62(inline) 0x01CE476E=128(top), then 0x01CE4760=1(type) LAST to arm it -- field loop polls it and loads field 892. Deferred ~400ms post-ASK-commit via s_skipPending (Update()) so it doesn't race the dialog teardown. ANSWER_SKIP now sets TGM_SKIP + arms bypass (Freeze fallback removed). TRAIN_SKIP_CAPTURE_DIAG flipped to 0. **BAT:** pick Skip at tiyane1; expect to land in tiagit1 (the base) and the post-mission story to play (President-dummy reveal / debrief). RISK: the entry coords (66,64760,0) are inferred from the real MAPJUMP3 VM stack -- if the warp fires but spawns off-mesh/wrong spot, those need fixing (extend LogMapjumpResult in field_nav_mapjump_diag.inl to dump the full block X/Y/Z on a real win). If the warp doesn't fire at all, the field loop may need a trigger flag beyond transition_type -- check [SKIP] log line fired + whether field changed. Reload save24 if anything's off.

>>> SKIP BAT-CONFIRMED v0.18.3.25/.26. Picked Skip at tiyane1 -> landed in tiagit1 (the base), post-mission dummy-President dialogue played and the conversation started normally. Warp + delta + entry coords (66,64760,0) all correct -- no refinement needed. BONUS: the capture run had several failed attempts, so the replayed persistent delta carries a net -1 SeeD rank; Aaron likes this as an intentional, deterministic 'cost of skipping' and we kept it. v0.18.3.26 = Skip ASK label updated to 'Skip: jump to the end of the train mission, drops your SeeD rank by one' (label text only, no behavior change). **#60 mode-ASK + Skip = DONE/BAT-confirmed.** All of .22/.23/.24/.25/.26 are LOCAL (built, not pushed) -- Aaron's next push carries them. Possible follow-ups (optional, not started): #60 'Auto' (mod drives + enters codes), the ASK box cosmetic width clip, and #59 timer freeze+readout.

>>> CODE-KEY HELP v0.18.3.27 (pending BAT). New `TrainCodeKeyHelp()` in field_dialog_lifecycle.inl (called from PollWindows beside TrainCodeAnnounce). On any code-apparatus field (tiagit* practice OR tilink* live), pressing "/" speaks "Code entry keys. 1 is D, 2 is X, 3 is A, 4 is W." -- number-first to match how the code is read aloud. Field-gated + edge-detected on the 0x8000 down bit; no SEH (no memory reads). NO conflict with the menu "/" help (menu_tts.cpp gates it to game mode 6; code entry is field mode). #57 full map for reference: 1=Right/D, 2=Down/X, 3=Left/A, 4=Up/W, Q=quit (arrows + Q intentionally omitted from the announce for brevity -- can add if Aaron wants). BAT: at the briefing-room practice panel and on the moving train, press "/" -> should hear the key layout; watch [TRAINCODE-KEYS] in ff8_field.log.

>>> CODE-INSTRUCTION FIX v0.18.3.28 (pending BAT). Rinoa's uncoupling briefing line (tiagit5, [AMESW] win[0]) reads "...Each code is made up of numbers 1-4...For instance, if I relay the code 3124, you'll  push L L L L, in  that order." -- the four key sprites decode to a meaningless "L L L L". New `ApplyTrainCodeKeyFix(std::string&)` in field_dialog_helpers.inl rewrites "L L L L" -> "A, D, X, W" (3=A,1=D,2=X,4=W for the example 3124) and appends "You can press the slash key anytime to hear the key layout." Wired into ScanAndSpeakAllWindows (field_dialog_scan.inl) right before the Speak: dedup keys (lastSpokenText/lastRawText/MarkPendingAsSpoken) keep the ORIGINAL `decoded` so it never re-speaks; only the spoken/repeat copy (toSpeak + s_lastDialogSpoken) is rewritten. Match is the unique 4-L run; the single page-break 'L' in other lines (e.g. "onboard.L") is untouched. Confirmed source string from ff8_dialog.log @15:01:59 + Aaron's 3 screenshots (f11_150220/150845/151211, all the "2341" box with the W4/A3/D1/X2 corner diagram). NOTE: the live-train code-entry diagram overlay (W4 A3 D1 X2) is NOT read as dialogue (not a text window), so only this briefing line needed fixing. BAT: replay the tiagit5 briefing (or the live tilink instruction if it repeats) -> the "push..." line should now say "A, D, X, W" + the slash reminder; watch the [AMESW] Speaking line in ff8_dialog.log.
>>> TIMER FIX v0.18.3.29 (#59, pending BAT). Train 5-min mission timer now wired through the existing CountdownTimer module (countdown_timer.cpp; same engine global 0x01CFE92C in SECONDS as Dollet/Fire Cavern). ROOT CAUSE: 300s fell below the old SECONDS classify floor (500-3000) -> UNKNOWN -> never activated; the static leftover 79 (held post-mission) hit EnterActive every tick -> UNKNOWN-log flood (the 5.62MB ff8_mod.log). FIX (5 edits, all committed): (a) ClassifyUnits widened to 1-14999=SECONDS, dropped the speculative MINUTES 5-60 branch (would have mis-scaled the final minute 60x; units latch once at EnterActive anyway); kept FRAMES 15000-60000 fallback. (b) decrement-gated activation: file statics s_actPrevRaw(-1 sentinel)/s_actDecrements + ACT_DECREMENTS_NEEDED=2; the INACTIVE branch in Update() now seeds on first sighting, counts clean small step-downs (delta 1..3), activates only after 2 confirmed decrements, resyncs on jump-up/discontinuity, resets on raw==0 -> a static value never activates and never spams. (c) shortened the UNKNOWN log. (d) Initialize() resets the new statics + version tag -> v0.18.3.29. No deploy.bat change (file already in build). BAT on the LIVE TRAIN: when the 5-min timer starts you should hear "Timer detected. ~5 minutes remaining", auto-cues at 1:00 and 0:30, T=announce, Shift+T=freeze; watch ff8_mod.log for [CountdownTimer] ENTER ACTIVE + clean "live raw=" decrement lines (NOT the old "value N unclassifiable" spam). FALLBACK if it does NOT activate (0x01CFE92C isn't the train timer): re-enable the scanner (COUNTDOWN_SCAN_ENABLED=1 in countdown_scan.inl) to find the address, as was done for Dollet. ALSO worth a quick Dollet regression check (Aaron has a Dollet save) since activation now needs ~2s of decrement first (functionally identical after). >>> UPDATE: v0.18.3.29 announce BAT-CONFIRMED (timer announces on the live train). v0.18.3.30 (#59, pending BAT) fixes the leftover glitch Aaron then hit -- the timer stayed "active" after the mission ended because the engine global freezes at the leftover ~79 instead of zeroing, and the module only deactivated on raw==0. ACTIVE state now also EnterInactive()s after STALL_TIMEOUT_MS=8000 with no value change (s_lastDecrementTickMs tracks last activity) -- mirror of the decrement-gated activation; FROZEN untouched. Same root cause as the old Dollet/Fire Cavern leftover-active glitch, so this fixes all three. BAT: finish (or abort) the train mission -> within ~8s the phantom timer should clear (T -> "No timer active"); watch ff8_mod.log for [CountdownTimer] ENTER INACTIVE reason="timer stalled -- sequence ended".

**THEN Skip** (multi-stage tilink1->tilink2->892; capture a [GUARDVAR] win for the exact success flags). Remaining Timber: **#59** timer freeze+readout, **#60** mode ASK + Auto/Skip.

Timer (#59) = shared FF8 countdown engine (Dollet/Fire Cavern; #33 + `Plan & Research Documents/Dollet timer countdown deep research results.md`). Aaron has a save just before the briefing. **STATUS: fix built v0.18.3.30, pending BAT (see TIMER FIX block above; .29 announce already BAT-confirmed, .30 = leftover-active fix).**

All chase reusables to mirror: `chase_detector` (field-set + debounced `pCurrentFieldName` + INI `Mode` enum), `chase_ask_overlay` (true in-engine ASK proxy from a captured `ff8_win_obj` template), `dialog_inject`, mode-gated behavior modules.

**#54 Status detail pages (Pages 1/2/3) DONE — all keys BAT-confirmed through v0.18.2.50; PUSH LANDED; ISSUE CLOSED.** Shipped as commit `faa31be` (v0.18.2.50, tagged), verified via the git transcript (`92297c8..faa31be -> main`) + local ref. **#54 was closed/commented against v0.18.2.50 at the open of the 2026-06-09 session** (the closing comment had been deferred because the GitHub API tools weren't loaded the turn it shipped). HEAD confirmed `faa31be` via `github:list_commits`. **ON HOLD: Irvine's Shot limit page** (Irvine isn't in the party yet — map it when he joins) — when ready, flip `ST_LIMIT_DIAG`→true in `menu_tts_status.inl` to map it (reuses the #49 limit-page infrastructure: page `+0x257==3`, `[STBAND]`/`[STLIMIT]` dumps), then **circle back to the status screens after Irvine** per Aaron. All four status discovery diagnostics (`ST_LIMIT_DIAG`/`ST_DETAIL_DIAG`/`ST_PAGE2_DIAG`/`ST_MAGSCAN`) are retained but gated OFF. Recently shipped:

- **v0.18.2.x menu chapter (closed #48)** — all main-menu character-select TTS: Junction reserve announce, "Rearrange party order" party-nav, Magic/Status char-select, active/reserve grouping cues. Detail in `CHANGELOG.md` v0.18.2.0–.23 + `DEVNOTES_HISTORY.md`.
- **v0.18.2.24 — community PR #26 (@djoleninja):** review/rewrite of all 21 disc-0 FMV audio descriptions, taken wholesale (merge conflict resolved), rebuilt + BAT-confirmed, pushed. PR merged and the thank-you comment to djoleninja is **posted** — that loop is closed, nothing outstanding.

## THIS SESSION — Status detail pages 1-3 (#54)

The Status DETAIL view has 4 pages cycled with L1/R1; page 4 (Limit Break) shipped in #49. **#54 = the remaining three:** (1) Stats & equipment, (2) Elemental & status resistances, (3) GF compatibility — identical in structure across characters, so one implementation covers everyone.

**Build on the existing `src/menu_tts_status.inl`** (dispatch `+0x1E8==5` / focus `+0x22E==3`, GCW snapshot/decode, SEH raw reads, `/`-key help re-read). Add page-1/2/3 handling alongside the limit-page path, each gated on page index **`+0x257`** (3 = limit confirmed; 1-3 likely 0/1/2 — **verify which value is which FIRST**).

**Discovery (no sighted step):** flip `ST_LIMIT_DIAG`→`true` in `menu_tts_status.inl` for `[STBAND]`/`[STLIMIT]` dumps; SUBMON harness (`menu_tts_diagnostics.inl`) for page/cursor bytes; read `ff8_menu.log`.

**DESIGN FINALIZED with Aaron (full spec in #54 comments).** Model: each page, on entry, speaks its name + enumerates its number-key shortcuts; keys speak labeled values (#44). **Page 1 (Character Statistics):** 0 overview (name/level/HP) · 1 experience · 2-7 Str/Vit/Mag/Spr/Spd/Luck · 8 Evade+Hit · 9 equipment (condensed enumeration on entry). **Page 2 (Elemental & Status):** 1 Elemental Attack · 2 Elemental Resistances · 3 Status Attack · 4 Status Resistances. Page 2 has 4 panels: elem-atk/st-atk = single icon+% (only render when junctioned), elem-def 8-grid, status-def ~13-grid. **Page 3 (GF Compatibility):** 0 = list all GFs + compat; diamond = junctioned to this char.

**v0.18.2.35 `[STDETAIL]` BAT DONE — results below.** **PAGE MAP CONFIRMED:** `+0x257` 0=Page1 Stats / 1=Page2 Elem+Status resist / 2=Page3 GF compat / 3=Page4 Limit. **NO per-page cursor:** band +0x25F..+0x264 stayed all-zero on all three pages (confirms Aaron — static info displays; per-page reads will be mod-side number-key hotkeys, like G/T/L/R). **GCW carries LABELS + NAMES ONLY, not numbers (H2 confirmed):** P1 GCW = `SquallLVHP/AttackMagicGFItemRevolver` (field labels + equipped weapon `Revolver` + junctioned command names; NO level/HP/stat numbers); P2 GCW = only that same left summary panel (zero element/status/% content); P3 GCW = obtained GF names `QuezacotlShivaIfritSirenDiablos` (+ a stray `A` glyph between Siren/Diablos — possibly the junction marker). The numeric values are drawn by a routine that bypasses `get_character_width`. **v0.18.2.36 `[STCALC]` BAT DONE = GO.** While a Status detail page is open (viewing Squall: HP 883/1044, L20), `char_comp_stats_1CFF000` slot 0 = curHP 883 / maxHP 1044 / mult 20 — exact match. So `0x1CFF000` is live in-menu and **slot 0 holds the VIEWED character's computed values** (the screen recomputes the viewed char into slot 0; slot 1 mirrors it, slot 2 unused). `+0x1B8` (FFNx `stat_multiplier`) = 20 = level, so it doubles as a level field. **The savemap base struct is NOT a shortcut:** it stores curHP (+0x00) + EXP (+0x04), but maxHP (+0x02) and base stats (+0x0A-0x0F) read ZERO — FF8 derives base stats from the level curve and computes maxHP at runtime, so `0x1CFF000` slot 0 is the only practical stat source. **Page-1 stat offsets RESOLVED** (disasm of `compute_char_stats_sub_495960`, the function that fills `0x1CFF000`; asm uploaded this session). Read `0x1CFF000 + slot*0x1D0 + offset`: curHP word +0x172, maxHP word +0x174, level byte +0x1B8, STR +0x1BB, VIT +0x1BC, MAG +0x1BD, SPR +0x1BE, SPD +0x1BF, LUCK +0x1C0, EVA +0x1C1, HIT +0x1C2 (stat bytes 0-255, clamped; HP word). Corroborated two ways: the junction-bonus factor array at +0x1C7..+0x1CF reads in canonical order [HP,STR,VIT,MAG,SPR,SPD,EVA,HIT,LUCK] with each stat store paired to its factor, and EVA is computed by passing the SPD value. (+0x1C4/+0x1C5 = extra derived bytes from arg-less fns — likely Page-2 elem/status attack, map later. The +0x1C7..+0x1CF accumulators also mark which stats are junctioned.) **v0.18.2.37 IMPLEMENTED Page 1 + Page 3 (LOCAL, awaiting BAT).** In `menu_tts_status.inl`: `PollStatusDetailPages()` announces page name + viewed char + key hint on entry to page 0 (stats) / page 2 (GF compat); `StatusDetailHotkeys()` reads number keys gated to those pages; both wired into the `sub==5` block in `menu_tts.cpp` (else-branch also calls `ResetStatusDetailPages()`). **Page 1 keys** (read `0x1CFF000` slot via HP-match, fallback slot 0): 0 overview (name / level / HP cur of max), 1 experience (savemap char+0x04), 2-7 STR/VIT/MAG/SPR/SPD/LUCK, 8 Evade+Hit; key 9 equipment deferred. **Page 3 key 0**: enumerate obtained GFs (`GF rec[+0x11]`) via `DecodeGFName` + compat `(6000-char[+0x70+gf*2])/5` + junctioned marker (`char[+0x58]` bit gf). Viewed char = roster `ms[0x1DB+ms[0x1E9]]` (primary), with an HP-match cross-check logged. Names use defaults (renamed Squall/Rinoa fall back to default for now; v2 reads the savemap name). **v0.18.2.38 BAT-CONFIRMED (Squall + Zell).** Page 1 stats correct both chars; Page 3 GF compat correct both chars. Viewed-char resolution verified: `[STPAGE] page=2 view=1(roster) hpMatch=1 name=Zell` — roster and HP cross-check agree; the screen computes the viewed char into `comp[0]` regardless of party slot (viewing Zell: comp[0]=Zell HP866, comp[1]=Squall HP883), and the HP-match picker resolves it. Compat is a 0-1000 scale; Zell's Shiva 0 / Ifrit 1000 is genuine per-char affinity, not a misread. `ST_DETAIL_DIAG` flipped OFF (discovery done); `[STPAGE]*` production logs remain. **v0.18.2.39** added the F11 screenshot index (`[F11-SHOT]` inline marker routed to the active domain log + a re-appended `[F11-INDEX]` summary so tail reads surface captures; final consolidated index to the mod log at shutdown). **v0.18.2.40** made Page-1 key 1 speak EXP-to-next ("Experience N. M to next level."; FF8 chars use a flat 1000 EXP/level so to-next = level*1000 - exp; verified against Squall's screenshot = 310). Both BAT-CONFIRMED (v0.18.2.42). **NEXT for #54:** (1) Page-1 key 9 = equipment DONE (v0.18.2.41 — weapon parsed from the GCW page-1 panel, NOT a weapon-ID table: isolate one panel between two menu-bar repeats, skip command tokens after the innate "Attack", take the trailing run; speaks "Weapon, Revolver" / "Weapon, Metal Knuckle"; ST_CMD_TOKENS is the complete verified set of FF8 command abilities (18 + innate Attack, both hyphen/space variants for multi-word names; Mug/Med Data/Junk Shop removed as non-commands) as of v0.18.2.42; **BAT-CONFIRMED** — `[STPAGE1] key=9: Weapon, Metal Knuckle` parsed cleanly from panel `ZellLVHP/AttackMetal Knuckle`). (2) **Page 2 (Elemental & Status)** is the remaining #54 work. **Inputs CONFIRMED** (FFNx `ff8/save_data.h` `savemap_ff8_character` + research doc): char struct internal offsets (NOT savemap-base, so no 0x14 correction) Elem-Atk `+0x65`, ST-Atk `+0x66`, Elem-Def `+0x67..0x6A` (4 magic IDs), ST-Def `+0x6B..0x6E` (4 magic IDs); all magic IDs, 0=none. Element order Fire/Ice/Thunder/Earth/Poison/Wind/Water/Holy; status-def order Death/Poison/Petrify/Darkness/Silence/Berserk/Zombie/Sleep/Slow/Stop/Curse/Confuse/Drain. Displayed %s bypass GCW and FFNx leaves the elem/status arrays unnamed inside `ff8_char_computed_stats.unk1[370]` (struct 0x000..0x171; stats live later: curHP +0x172, level +0x1B8, STR..HIT +0x1BB..0x1C2). **v0.18.2.43 = `[ST2DIAG]` Page-2 discovery diagnostic (LOCAL, ST_PAGE2_DIAG=true):** gated to Page 2 (focus +0x22E==3, page +0x257==1), logs the 10 junction input IDs + a hex dump of the viewed char's 0x1CFF000 computed slot (non-zero 16-byte rows across the full 0x1D0). **v0.18.2.43 `[ST2DIAG]` BAT 1 (Squall) DONE — results** (all offsets into the 0x1CFF000 slot, slot*0x1D0+off; inputs atk_ele=7/atk_st=46/def_ele=[0,0,0,0]/def_st=[27,0,0,0]; screen: elem-def all 0%, elem-atk Thunder 43%, st-atk 66%, st-def mostly 4%): **SOLVED Status Defense** = 13 bytes at **+0x1A4**, **% = byte−100** (neutral 100; Squall [0,4,4,4,4,4,0,4,4,4,4,4,0] matched). **SOLVED Elemental Attack** = element **bitmask +0x1C4** (Fire01/Ice02/Thunder04/Earth08/Poison10/Wind20/Water40/Holy80; Squall 0x04=Thunder) + **% +0x1C5** (0x2B=43 matched). **LOCATED scale-TBD Elemental Defense** = 8 LE words at **+0x194**, neutral 800 (Squall all-0% → all 800), contiguous right before the status-def array; need a char with NON-ZERO elem resist to derive the word→% scale + confirm element order. **Status Attack %** NOT cached in the slot (66 absent) → read from kernel Magic Data per-spell st-atk value (also map magic id→status/element name for panel identity). **BATs 2-5 + IMPLEMENTED (v0.18.2.44).** Elem-Atk re-confirmed ×3 (Thunder 0x04/43, Water 0x40/100, Ice 0x02/80). Status-Def confirmed ×3 (0/4/66%; +0x1A4 byte−100). **ELEM-DEF SCALE CONFIRMED:** Blizzara→Elem-Def gave Ice word +0x196=824, screen 24% → **elem % = word − 800** (flat 1:1, neutral 800; the earlier ÷8 guess was WRONG — the empirical BAT caught it, per the v0.14.73 rule). Element order Fire/Ice/Thunder/Earth/Poison/Wind/Water/Holy confirmed (panel column-major). **Page 2 keys 1/2/4 IMPLEMENTED & wired** in `menu_tts_status.inl` (StSpeakElemAtk +0x1C4/5 · StSpeakElemDef 8 words +0x194 word−800 resist/weak · StSpeakStatusDef 13 bytes +0x1A4 byte−100; `StatusDetailHotkeys` page==1 keys 1-4; `PollStatusDetailPages` page==1 entry announces "Elemental and Status" + hints). No `menu_tts.cpp` change (it already calls those). **Key 3 Status-Atk = INTERIM** (reads char+0x66 ST-Atk magic id, reports presence only + logs magicId; status name+% are NOT cached in the computed buffer → needs a magic→(status,value) table — THE one remaining #54 refinement). `ST_PAGE2_DIAG=false` (discovery done; `[ST2DIAG]` kept behind the flag for the key-3 table work). **v0.18.2.45 BAT-CONFIRMED (Squall):** key 2 now speaks resist/immune/absorb — Blizzaga Elem-Def-J (Ice value 120) → "Ice absorbs 20 percent" ✓; keys 1/4 unchanged ✓. **KEY 3 DONE (v0.18.2.50):** Status-Atk % = the stock count of the junctioned ST-Atk spell. ST-Atk-J inflicts status at 1% per spell stocked, max 100 (confirmed Final Fantasy Wiki / Neoseeker / GameFAQs) — so the "66" found earlier in the magic-inventory list was NEVER a coincidence; Squall had 66 Berserk stocked and 66 stock = 66%. Key 3 reads the ST-Atk magic id (char+0x66 = FFNx `j_atk_mtl`), names the spell from `ST_MAGIC_NAMES` (full 0-56 table, validated: id7=Thunder, id46=Berserk both match our data), and reads the % as that spell's count from the per-character magic inventory (FFNx `savemap_ff8_character.magics[32]` at char+0x10; each u16 = id low byte, qty high byte; slot-ordered so scan all 32). Speaks e.g. "Status attack, Berserk, 66 percent"; `[STPAGE2] key=3` logs magicId+stock. Names the junctioned SPELL (not the inflicted status) to stay exact + self-verifiable. **The kernel-scanner plan described just below was ABANDONED** (3 variants .46-.48 all failed — the magic table isn't a flat id-indexed 0x3C array, and this value was never in a kernel table; it's simply the stock count). `ST_PAGE2_DIAG`+`ST_MAGSCAN` flipped OFF (retained gated). — Superseded original note follows: **NEXT: finish key 3 Status Attack (status name + %).** It still speaks the interim stub "Status attack junctioned" (BAT: magicId 46 = Berserk, 66% — cross-referenced via the ST-Def index-5 reading). The status name + % are NOT in the computed buffer and FFNx's named magic loaders are the battle EFFECT data (\Data\Magic\ / dav_aoy), not the stat table — so Aaron chose the runtime kernel read. **v0.18.2.46 adds `PollStatusMagScan` (`ST_MAGSCAN=true`, gated Page 2, in `menu_tts_status.inl`, wired into the sub==5 chain after PollStatusPage2Diag):** a ONE-SHOT VirtualQuery walk of committed/readable memory for a 0x3C-stride array whose per-entry magic-id field counts up (id-sequence sig at candidate offsets +0x04/+0x00/+0x02 u16 and +0x04/+0x06 u8, or a name-offset-monotone fallback @+0x00), CONFIRMED by the content anchor that entry 46 contains 0x42 (Berserk's status-attack 66); also checks entry 7 for 0x2B (Thunder elem-atk-J 43). On a hit it logs base + sig + dumps entries 0/1/7/27/46 in full hex so the real field offsets (status-attack byte, status bitmask) are READ, not assumed. SEH-guarded, log-only, no speech/writes. **NEXT BAT (v0.18.2.46): Status → Page 2 with the Berserk spell still on ST-Atk-J (as last BAT), then read `[STMAGSCAN] base=… sig=… magic7has0x2B=…` + the five entry dumps from `ff8_menu.log`.** If found: implement key 3 to read status name (from the bitmask) + % (status-attack byte) from kernel entry `base + id*0x3C`, then flip `ST_MAGSCAN` false. If NOT FOUND: widen the scan range or fall back to a static disasm trace of the menu ST-Atk-J render. Once 1/2/4 BAT-confirmed, build the Status-Atk magic→status table for key 3, then the whole status submenu (Pages 1/2/3) is ready to push as ONE package (HEAD still v0.18.2.34; all #54 work unpushed — confirm with `github:list_commits`). **#55 CLOSED (not a bug):** "SirenA" is a save-file artifact — the name was entered manually with a trailing "A" before the Siren naming-bypass was fixed, so the savemap genuinely stores "SirenA" and `DecodeGFName` reads it faithfully. Do NOT strip trailing glyphs (would corrupt legitimately renamed GFs). If an unexpected trailing letter appears on a name again, check for a user-entered/legacy save name before suspecting the decoder. **Page 3 is implementable NOW without more discovery:** GF names from the GCW or savemap GF records, compat from savemap `char+0x70` `(6000-raw)/5`; only the junctioned-to-this-char marker still needs sourcing. Element/status identities on page 2 are icon SPRITES → fixed-order tables (elements Fire/Ice/Thunder/Earth/Poison/Wind/Water/Holy; status-def ~13: Death/Poison/Petrify/Darkness/Silence/Berserk/Zombie/Sleep/Slow/Stop/Curse/Confuse/Drain — confirm order via kernel Section 31). Attack-panel element/status named from junctioned magic (char `+0x65` Elem-Atk / `+0x66` ST-Atk). Char record live base ~`SAVEMAP+0x48C` stride `0x98`; gf_compat char `+0x70` display `(6000-raw)/5` (verify vs Squall: Quez 504/Shiva 1000/Ifrit 289/Siren 528/Diablos 648). **#50** (Angelo gauges) deferred.

*(#49 original chapter plan — completed, kept for reference:)*

**Goal:** wire up the per-character **Status screen** options related to **Limit Breaks** for the characters that have them — Aaron specifically named **Squall, Zell, and Rinoa**. Per Aaron, **Zell's and Squall's limit breaks are effectively inaccessible to a blind player without these options being readable/adjustable**, so this is the priority of the chapter, not cosmetic.

**Build on what's done, don't re-derive:** the Status screen's *character-select* is already handled — subsystem `pMenuStateA+0x1E8 == 5`, char-select cursor `+0x1E9` → roster `+0x1DB`, focus `+0x22E` in {0,8} (v0.18.2.20–.23, shared `AnnounceJuncCharSelect`). The NEW surface is what appears **after** you confirm a character on the Status screen: that character's Status **detail** view, and within it the Limit Break option(s) that can be read and changed.

**Step 1 is discovery + existing-knowledge-first (do BEFORE coding):**
1. **Check `Plan & Research Documents/` and the disassembly FIRST** (per the standing rule — v0.14.73 shipped wrong data by skipping a research doc). Look for anything on Status-screen layout, limit-break flags/settings, and the per-character limit data. Use `project_knowledge_search` for the disassembly and filesystem tools for the research docs.
2. **Pin down the exact mechanic with Aaron before building** — confirm precisely what "options that can be adjusted" means per character (e.g. what Squall's vs Zell's vs Rinoa's option is, what values it toggles between, and whether it lives on the Status detail screen or an adjacent panel). Aaron's domain knowledge is ground truth here; ask him to describe one concrete example (which character, what the option is, what changing it does) and verify it empirically. Do NOT assume the FF8 mechanic — confirm it.
3. **Find the offsets with the SUBMON auto-monitor harness** (`menu_tts_diagnostics.inl`) — the same memory-diff path that found the Item/Junction/GF/Magic-Status offsets. It diffs memory as the cursor moves, so there is **no "press a key when X is on screen" step**: Aaron navigates the Status detail / limit option, the monitor logs which bytes near `pMenuStateA` change. Read the SUBMON output from `ff8_menu.log`. Unknowns to find: the Status-detail phase byte (analog of Junction `+0x22E`), the option/sub-cursor offset, and the value field for each adjustable limit option.

**Suggested implementation (mirror the GF/Ability chapters, which worked):**
- New `menu_tts_status.inl` (or extend the existing Status handling) — textual `#include` from `menu_tts.cpp`, statics-first `*_state` block, no header guards/namespace inside the `.inl`, keep under the 60 KB soft cap.
- Dispatch seam gated on `+0x1E8 == 5`, suppressed while other submenus are active; reset state on entry/exit.
- Announce the option name + its current value on cursor move, and announce the new value when it's changed. Never speak a raw number a blind player can't otherwise verify (the #44 rule) — give it meaning.
- File ONE GitHub issue (label menu-tts / accessibility) for the chapter at the start, close it against the shipping version at wrap-up. (Offer this to Aaron; he hadn't filed it yet as of last session.)

## Backlog — secondary, pick with Aaron if Status work stalls (live list: `github:list_issues`)

- **#30** menu_tts.cpp T-handler missing `!shift` gate (trivial). **#36** F9 auto-drive gateway `driveSkipTrigIdx` range guard. **#43** calibrate remaining GF per-level EXP costs (web research doable). **#33** Dollet timer doc.
- **#5** announce disabled/greyed menu items — related to Status if any limit option is conditionally unavailable.
- **#2** (high) speech-rate default offset; **#18/#19** battle Magic scroll + page/slot; **#20/#22** Draw "???" / wrong character; **#21** location names as numbers; #3, #6, #7, #9, #15, #25.
- Field/world verify items (#31, #34, #35, #27) need in-game traversal (flip `LINEDIAG_ENABLED`=1). Blocked/hold: **#45** (junction non-command abilities — no GF teaches one yet); #28/#29 (world-map planner); #37/#38/#39.

## FMV audio descriptions — carry-forward (from the PR #26 work)

- VTTs live in `Audio Descriptions/disc*_*h.vtt`, embedded as **RCDATA at build time** (`resources.rc`); parser is `src/fmv_audio_desc.cpp`. **Any VTT edit needs a rebuild to ship.** Parser skips `NOTE` blocks and tolerates minor timestamp typos (lenient `sscanf`); flag duplicate cue start-times (the later cue interrupts/drops the earlier — chain the start to the prior cue's end).
- Disc-2+ have paired `_ad.vtt`; only disc-0/1 + the intro / opening-credits VTTs were in PR #26's scope. @djoleninja may submit more passes — **when reviewing a future PR, verify merged file content with `github:get_file_contents`, not just "merge succeeded"** (the v0.18.2.24 stale-PR conflict-marker incident).

## Reusable reference — DO NOT re-derive

**Menu char-select offsets (v0.18.2, vs `pMenuStateA`):** `+0x1E8` subsystem (`0xFF` bare / `17` Junction / `14` Ability / `3` Magic / `5` Status); `+0x22E` focus (`0`/`8` = char-select); `+0x1E9` char-select cursor → roster `+0x1DB` (all members incl. reserves, `0xFF`-terminated); `+0x71E` per-character menu HP array (stride 0x20, cur +0 / max +2, covers benched); battle formation `savemap+0xAF0` = active-party test. SUBMON harness in `menu_tts_diagnostics.inl` (no sighted step).

**GF memory map (v0.18.0):** GF savemap record base `SAVEMAP_BASE + 0x4C`, stride `0x44`, 16 records canonical order: name `+0x00`, Current EXP u32 `+0x0C`, obtained `+0x11`, HP u16 `+0x12`, complete_abilities[16] `+0x14`, APs[24] `+0x24`, kills `+0x3C`, learning ability id `+0x40`. GF-list cursor `pMenuStateA+0x253`; gate `+0x1E8==4`. AP for an ability = `APs[slot-of-id]` (slot from `gf_ability_slots[gf]`, key off id not display order); cost `ability_ap_cost[id]`; helpers `GFAbilityApCost`/`GFReadAbilityAP` in `menu_tts_gf.inl`. GF level = flat per-level cost (`level = exp/cost + 1`); `GF_EXP_PER_LEVEL[16]` has only CONFIRMED costs (Quez/Shiva/Ifrit/Diablos 500, Siren 400; rest 0 = EXP only) — **#43** fills the rest.

**Character savemap (carry-forward, verify against research docs):** per-CHARACTER record holds stats + `gf_compat[16]` at char `+0x70` (u16 by GF id, display = `(6000 - raw)/5`). **Status/limit fields are NOT yet mapped — that's this chapter's discovery; check `Plan & Research Documents/` first.** Savemap header = 76 bytes (0x4C), community offsets run +0x14 too high — subtract 0x14. Base `0x1CFDC5C`.

**Status limit page (#49, SHIPPED v0.18.2.33 — `src/menu_tts_status.inl`):** page index `pMenuStateA+0x257==3`; detail focus `+0x22E==3`. **Toggles** (keyed off GCW help, announce name+on/off, re-announce on flip): Gunblade Auto `savemap+0xD1C & 0x01` (1=ON), Zell Duel-Auto `+0xD1C & 0x02` (set=ON), Renzokuken Indicator `+0xD1D & 0x80` (0=ON, INVERTED) — announces "disabled" on focus when Gunblade Auto on (greyed but help still drawn). `+0xD1C` shared auto bitfield (Irvine likely bit 0x04). **Per-char cursor byte (hardcoded):** Squall `+0x25F` (step1, leadingToggles4: cur 0-3 = 2 toggles x ON/OFF columns, 4+ = finishers), Zell `+0x260`, Quistis `+0x262` (auto-detect), Rinoa `+0x263`. **Read-only lists** via GCW longest-match parse; baked move descriptions on the toggle pages (Squall/Zell) because GCW help lags the cursor there, with a learned-row→full-table index map. **`ST_LIMIT_DIAG` flag (off)** in the `.inl` re-enables `[STBAND]`/`[STLIMIT]` to map Irvine's Shot page. **#50** = Angelo gauges (data ~`savemap+0xB16`, GF-style known/learning/points).

**SeeD savemap reference:** `.ff8` saves are FF7/FF8 LZSS-compressed (4-byte LE size header; N=4096 F=18 THRESHOLD=2, init-pos 0xFEE). Live offset X == decompressed-file offset `0x184 + X`. SeeD points `+0x0D6C` (u16, rank = points/100); salary count `+0x0CDE`; steps-since-pay `+0x0D64`; gameplay Gil `+0x0B08` (u32).

## Push-flow & build reminders

- **Aaron pushes via `Utilities/push_to_github.ps1` (double-click the `.vbs`); Claude NEVER pushes.** Utility refuses unless `CHANGELOG.md`'s top `## vX.Y.Z` heading matches `FF8OPC_VERSION`. **After a push, verify with `github:list_commits` AND a touched file's content — don't trust "I pushed" alone.** The v0.18.2.24 push didn't fire on the first attempt; `Logs/push_diagnostic.log` shows each `.ps1` run and `Logs/git_latest.log` shows the git result — read those to triage a non-landing push.
- **Version bump = one place:** `FF8OPC_VERSION` in `src/ff8_accessibility.h`, paired with a new top `CHANGELOG.md` entry. Deploy = `deploy.vbs` → `src/deploy.ps1` → `src/deploy.bat` (update `deploy.bat` only when adding/removing source files).
- **"BAT"** = read `Logs/build_latest.log` tail for version + success, then the relevant domain log (here `ff8_menu.log`). Read the full relevant log, not just the tail.
- **`filesystem:` tools for all Windows project files.** `edit_file` multi-edit is all-or-nothing (one stale `oldText` silently rejects the batch — read exact current text first; `dryRun:true` tests an anchor). A literal `$` in `newText` corrupts the file — use `write_file` for full rewrites with `$`. OneDrive can throw a transient EPERM on `edit_file` rename — retry once.
- **F12 = per-session diagnostic only** (one at a time; remove old before new). F-key handlers gate on `!(GetAsyncKeyState(VK_MENU) & 0x8000)`. **NEVER re-enable the SET3 hook (0x1E)** — hangs the infirmary scene (CI guard). Source size 60 KB soft / 80 KB hard (CI + push Step 7c); split via `.inl`.
- **DEVNOTES.md hard 10 KB cap** — long narratives go to `DEVNOTES_HISTORY.md`. Update DEVNOTES + this file at every version bump and after every BAT.

## Closed / verified — do NOT re-open

- **PR #26 / v0.18.2.24** — disc-0 FMV audio descriptions (djoleninja), merge conflict resolved, pushed + verified clean; thank-you comment posted.
- **v0.18.2.x menu chapter** — #10, #42, #46, #47, #48 closed; shipped through v0.18.2.23 (`34cbefc`).
- **GF submenus** — #41 + #44 closed, v0.18.0.15 (`9102689`); #40 dup of #41. **Ability "Use GF ability" refine flow** — #42 closed, v0.18.1.13 (`9cba532`).
- **Track A F9 auto-drive** — v0.17.9.17 (`808d4802`); **exit-destination interpreter** — v0.17.9.6 (`502516c3`) + v0.17.9.11 (`3478683`); **Chapter 5 SeeD rank/salary** — v0.17.9.1 (`5c3af6a5`). Diagnostics gated off.
- **deploy.bat "Version:" extraction** — verified correct (`/B` anchor); don't touch.

## #54 closing comment (ready to post)

Close **#54** against **v0.18.2.50** (commit `faa31be`) with a comment along these lines:

> Status detail pages 1–3 complete as of v0.18.2.50 (commit faa31be).
>
> - Page 1 (Character Statistics): keys 0–9 — overview, EXP + to-next, STR/VIT/MAG/SPR/SPD/LUCK, Evade+Hit, weapon.
> - Page 2 (Elemental & Status): key 1 Elemental Attack, key 2 Elemental Resistances, key 3 Status Attack, key 4 Status Resistances.
> - Page 3 (GF Compatibility): key 0 lists every GF with its compatibility value + a junctioned-to-this-character marker.
>
> Key 3 (Status Attack) was the final piece. ST-Atk-J inflicts status at 1% per spell stocked (max 100), so the displayed percent is simply the stock count of the junctioned spell. Key 3 reads the junctioned magic id at char+0x66, names the spell, and reads the count from the per-character magic inventory at char+0x10. Speaks e.g. "Status attack, Berserk, 66 percent."
>
> All pages BAT-confirmed. Status discovery diagnostics retained but gated off (ST_LIMIT_DIAG kept for the upcoming Irvine Shot-limit mapping). Closing as shipped.
