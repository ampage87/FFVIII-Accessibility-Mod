**Purpose & context**

Aaron is sole developer of the FF8 Accessibility Mod — a `dinput8.dll` injection for FF8 Steam 2013 (App ID 39150, FF8_EN.exe + FFNx v1.23.x) making the game playable for blind players via SAPI TTS, navigation assistance and entity catalogs. Aaron is blind, uses NVDA, and is the primary tester.

**Root:** `C:/Users/ampag/OneDrive/Documents/FFVIII-Accessibility-Mod/FF8_OriginalPC_mod/` · **GitHub:** `ampage87/FFVIII-Accessibility-Mod`. Aaron pushes via `Utilities/push_to_github.ps1`; **Claude never pushes**; diagnostic builds stay LOCAL.

History: `CHANGELOG.md` (authoritative) + `DEVNOTES_HISTORY.md`. This file = live state + rules; under 10 KB.

---

## Where we are at session open

**★ LATEST (v0.22.5 — #81: THE LATCH WAS DISARMED BY THE VERY FRAMES IT HAD TO SURVIVE).** **(1) THE ALL TRANSFER WAS STILL SILENT.** v0.22.4's detection-by-effect was right; its CLEARING RULE was not — it disarmed on any poll that was not state 97, and the chain is **99 -> 104 -> 105 -> 96 -> 97** where 104 and 96 both map to MP_NONE. A poll landing on either **disarmed the latch one or two frames BEFORE the transfer it existed to detect.** **And the v0.22.4 test PASSED, because it stepped straight from 99 to 97 and never visited the transient states.** That is the SECOND time in two builds that a green test covered a path the game cannot take (v0.22.1 tested state 105 directly, which the poll can never observe). The latch now survives until the player is demonstrably out of the flow — action row, closing, or a different giver — and menu_sim walks the REAL chain including 104/105/96, asserting the latch is still armed at 104. **(2) THE STEP PROMPT REPEATED WITH EVERY CHARACTER.** v0.22.3 made a character change force the header, which on the All steps prefixed "Select member to receive magic" to every single left/right. **The prompt belongs to the STEP, the name to the LINE:** new `MagicLineNamesCharacter()` suppresses the header on the two All steps and the Exchange partner picker, and deliberately NOT on the spell list or action row, whose lines name nobody and where v0.22.3's fix is still exactly right. Gated both ways. **METHOD RULE, and it is the one to remember from this whole arc: both recent failures had the same shape — a test that CONSTRUCTS a state the game only reaches through a SEQUENCE, without walking the sequence. The tests were not wrong about the logic; they were wrong about the world. Where a transition matters, replay the transition rather than jumping to its endpoints.** **Verified: menu_sim OK (0 bad); menu_magic_compile OK (0 bad); entryaim/trigwalk/trigseg/pathdecimate/vehsig OK; catalog_story and garden_aboard 0 failures; lint_seh OK (89 files); all four harnesses compile.** **BAT: complete one All transfer (expect "All magic moved from X to Y") and cycle a few characters on each step to confirm the prompt is spoken once rather than before every name.**

**Prior (v0.22.4 — #81: THE ALL TRANSFER RAN SILENTLY).** **v0.22.3's character-change fix is CONFIRMED WORKING** — the log shows three consecutive `"Use"` lines at char=2/0/5, which only appear BECAUSE the character changed (the string-equality check suppresses an unchanged line), plus the spell list re-announcing across char=5 -> char=2. **AND THE BAT FINALLY COMPLETED AN ALL TRANSFER, WHICH EXPOSED A REAL DEFECT: THE MOD SAID NOTHING.** Receiver Zell(1), giver Irvine(2); afterwards **Zell's Exchange list reads "Life 75 / Scan 88 / Cure 92" — Irvine's exact loadout from earlier in the SAME log** — and there are ZERO phase=14 announcements. **Cause: state 105 performs the transfer and lasts ONE FRAME.** The chain is 99 -> confirm -> 104 -> 105 -> 96 -> 97 and everything but 99 and 97 is transient, so a polling reader cannot observe it. **v0.22.1 implemented the announcement on state 105 and unit-tested it, and the test PASSED — because a test can hand itself state 105 whenever it likes. The game will not.** **Fixed by watching the EFFECT, not the state:** latch the giver's TOTAL HELD while the giver step is up and announce when it reaches zero; a cancel leaves the total untouched, so the two outcomes separate cleanly without ever seeing the state that did the work. Three details the log itself dictated: the latch **re-snapshots every tick** (the log shows the giver moving Squall -> Irvine, so a once-only latch would have named the wrong character); it requires the same giver on both sides of the comparison; and a giver holding nothing cannot produce a false positive. State **107** also joins 106 as the pre-flight-warning phase (106 opens the box, 107 polls it) for the same one-frame reason. **METHOD RULE EARNED HERE, and it generalises well beyond this screen: a state that only the game can enter TRANSIENTLY cannot be verified by a test that constructs that state directly. Where the effect is observable in the savemap, watch the effect.** **And this is the value of writing down "never observed live" rather than assuming tested code works** — v0.22.3's notes named this exact gap, and one BAT later it contained a bug, a SILENT one that a tester cannot report because nothing happens. **Verified: menu_sim OK (0 bad) with a new block replaying the log (completed transfer, cancel, giver changed mid-step, empty giver); menu_magic_compile OK (0 bad); entryaim/trigwalk/trigseg/pathdecimate/vehsig OK; catalog_story and garden_aboard 0 failures; lint_seh OK (89 files); all four harnesses compile.** **BAT: complete one All transfer and confirm "All magic moved from X to Y"; then cancel out of another and confirm silence. That is the last untested path in the Magic submenu.**

**Prior (v0.22.3 — #81: THE ONE THING THE LOG FOUND THAT THE PLAYER DID NOT).** Aaron BAT'd v0.22.2 clean (*"That worked great! I think it is good to go"*) and asked for a full log review before calling Magic production-ready. **The log is excellent: 1,448 lines, ZERO errors.** The help bar reads the game's own text including the per-spell DESCRIPTIONS it shows on the list ("Revive from KO", "Restore HP", "Scan enemy's HP and weakness", "Fire magic damage/one enemy"); castability is right LIVE (Scan 88 and Thunder 90 say "cannot be cast here", Life 75 and Cure 92 do not — the seven-spell mmagic.bin set behaving exactly as extracted); the Exchange split ran `Fire, move 0, keep 100` -> `move 50, keep 50` and the savemap agrees afterwards (both Zell and Rinoa read Fire 50); the slot formula held through a page change with a STALE cursor (`page=1 cur=3 -> slot 7`), the case the game's own draw path handles identically; and `Magic module at 0x01D76CB8 (pool slot 2)` — the walk CONFIRMS the slot-2 inference rather than depending on it. **THE ONE DEFECT: L1/R1 CHANGES THE CHARACTER WITHOUT LEAVING THE PHASE, so nothing was said about it.** The log has the spell list going `char=2` -> `char=5` announced only as "Cure, quantity 82, slot 1 of 32" — **you would believe you were still looking at Irvine's magic when you were looking at Selphie's.** On a screen whose entire purpose is "whose spells are these and how many" that is the worst thing it could get wrong, **and it is invisible in play because both lines are individually correct** — which is exactly why the log review was worth doing. The action row had the same shape: `char=1` -> `char=5` audible only as the position qualifier quietly disappearing when the enable mask happened to change. A character change is now an ARRIVAL, so the header fires; **and the list header NAMES THE CHARACTER** ("Selphie's magic") instead of the generic "Magic list" that said nothing precisely when it mattered. Which character an announcement is ABOUT differs by phase — the partner panel is about the partner, everything else about the screen's own character — so swapping the partner re-announces in the partner panel and changing it on the action row stays quiet. **A latent bug fell out of writing the test:** the header used the cached `charName` rather than deriving from `charId`, and those can desync because a swap updates charId first — the fixture produced "Squall" where the character was Selphie. It now derives from charId and cannot desync. **Cosmetic, deliberately not fixed:** the help bar renders the game's interpuncts as spaces ("Rearrange in order of Restore Attack Indirect"). That is the game's own text, it reads fine aloud, and special-casing the character inside the SHARED decoder would risk every other help string for no gain. **NOT EXERCISED IN ANY BAT YET: an All transfer has never been COMPLETED in a log** — the v0.22.2 run reached the receiver step and backed out, and the "works as expected" confirmation came from play rather than a log line. States 105 (transfer) and 106 (pre-flight warning) are implemented and unit-tested but never observed live. Worth one run when convenient; not a blocker. **Verified: menu_sim OK (0 bad) with a new block replaying the log's own transitions (spell-list swap, action-row swap, partner swap, and the case that must NOT fire); menu_magic_compile OK (0 bad); entryaim/trigwalk/trigseg/pathdecimate/vehsig OK; catalog_story and garden_aboard 0 failures; lint_seh OK (89 files); all four harnesses compile.** **BAT: short one — in the spell list press L1/R1 to change character and confirm it now says whose magic you are looking at; same on the action row. Then Magic is done.**

**Prior (v0.22.2 — #81: THE GARBLED HELP BAR, AND ONE NAME TOO MANY).** BAT of v0.22.1: **"All now works as expected"** — which settles the direction correction (step 1 receiver, step 2 giver) that contradicted Aaron's original description. Two defects left. **(1) HELP TEXT WAS GARBLED THROUGHOUT — "Use magic" was read aloud as "AaI'UEIOE".** `FF8TextDecode::DecodeMenuText` indexes its 224-entry glyph table with a **GLYPH INDEX**, because its existing caller hands it the GCW buffer, which the RENDERER has already converted. mngrp.bin is one stage earlier and holds **TEXT-STREAM bytes**, and a text byte is `glyph + 0x20` — so every character came out 32 slots too high. Proof from the real file (group 8 string 1 = `59 71 63 20 6B 5F 65 67 61`): indexed raw -> `AaI'UEIOE`; minus 0x20 -> `Use magic`. **The v0.22.1 comment that the bar and the scrape "were always reading the same encoding, just at different points in the pipeline" was HALF RIGHT AND THEREFORE WRONG — same TABLE, different OFFSET, and the offset was the whole bug.** Method note, third time this pattern has cost a build: **a confident sentence asserting two things are equivalent is where these keep coming from.** New `MagicTextToGlyphs()` in the model does the shift and drops control codes plus the parameter byte of a 0x0A..0x0E variable (the game's own strings contain `Can't carry {0A}5`); **DecodeMenuText is LEFT ALONE — its other callers pass glyph indices and are correct.** **(2) EXCHANGE REPEATED THE PARTNER'S NAME ON EVERY SLOT.** The owner prefix now lives in the PHASE HEADER ("Zell's magic"), spoken once on arrival in a panel, never on the per-slot line — the panel cannot change while you move within it, so the name is news exactly once. v0.22.1 put it on every line for a real reason (two lists on one screen, "Cure, quantity 47" ambiguous between them); the header solves that without the repetition. **NEW GATE, AND WHAT IT CAN ACTUALLY PROVE: menu_sim now decodes THREE byte sequences lifted VERBATIM out of mngrp.bin** ("Use magic" and both All step prompts) **and asserts the UN-SHIFTED reading is WRONG** — a fixture that decodes correctly either way proves nothing. menu_magic_compile's stub decoder is now the exact inverse of the shift, and its header states plainly that it checks CONTROL FLOW (pointer read, sentinels, fault path, declining off-screen) and NOT encoding: **a stub cannot falsify its own convention; only the real bytes can.** **Verified: menu_sim OK (0 bad); menu_magic_compile OK (0 bad); entryaim/trigwalk/trigseg/pathdecimate/vehsig OK; catalog_story and garden_aboard 0 failures; lint_seh OK (89 files); all four harnesses compile.** **BAT: press "/" on the action row, the spell list, the sort popup and both Exchange panels — it should now be the game's own words. Then walk Exchange and check the partner is named once per panel rather than once per slot.**

**Prior (v0.22.1 — #81: THE FOUR MAGIC BUGS).** v0.22.0 BAT-confirmed Use and Rearrange clean; these are the other four Aaron reported. **(1) "Magic list" was spoken on EVERY page turn** — paging runs 13 -> 14/16 -> 13 and the poll recorded the TRANSIENT phase, so the return looked like a fresh arrival. It now keeps the last SPOKEN phase; header once, on arrival. **(2) "/" DID NOTHING on the Magic screen** — AnnounceHelpText scrapes the RENDERED text from the GCW buffer hunting a dash separator or a known prefix, and the Magic bar has neither, so the key was silently a no-op. It never needed the scrape: **the module caches a pointer to the exact string the bar draws** (module +0x24; draw 0x004F6996 pushes callback 0x004F70C0 with ctx &module[+0x24]), and the getter 0x004BD630 does NO copying — it returns a pointer straight into the loaded mngrp.bin. Raw NUL-terminated FF8 bytes in long-lived data written with one aligned dword store, so cross-thread reads are safe. NULL / 0x01D7714C (getter fallback) / 0x01CFF84C (empty-string constant) = no text. **Stated not hidden: the game does not refresh +0x24 in target select (20) or All step 2 (99), so those read slightly STALE text — never wrong text.** **(3) EXCHANGE WAS ENTIRELY SILENT** — states 26 (your list) / 28 (choose partner) / 44 (their list) / 52,55 (Give All, Take All, Split) / 63 (quantity split). Both columns share the ONE per-character cursor array at +0x38; column B pages on **+0x46**, so partner slot = `(cursor[B] & 3) + pageB*4`. Split counters **+0x58 / +0x59**, sum conserved, 0..100, spell id +0x5A. **Each panel now names WHOSE list it is** — two lists on one screen and "Cure, quantity 47" spoken identically for both was the exact ambiguity that made it unusable; the split reads BOTH counters because the two numbers move in opposite directions. **(4) ALL WAS CONFUSING — AND THE DIRECTION IS THE REVERSE OF AARON'S DESCRIPTION.** He reported give-then-receive; **state 97 picks the RECEIVER and state 99 the GIVER.** Proof: state 105 calls `0x004F5FA0(arg1=+0x62, arg2=+0x64)` where 0x4C2C70 ADDS arg1's spells to arg2 and 0x4C2D50 REMOVES them from arg1 — so the step-2 character loses and step-1 gains; and step 2's mask is `available & ~(1 << +0x64)`, which only makes sense if step 1 already fixed a role. **The game's own help bar agrees**: group 8 entry 12 on step 1 = "Select member to receive magic", entry 13 on step 2 = "Select member to transfer magic" — so the mod speaks the game's wording VERBATIM rather than an interpretation. Announcing "choose who gives" on the receiver step would have been an accessibility bug dressed as a feature. No Yes/No confirm in this flow: the second confirm runs a pre-flight and either commits (105) or opens a warning box (106); both announced. **ROOT CAUSE OF (3) AND (4): v0.22.0 routed on `screenMode == 3`, and BOTH flows use mode 3 AND mode 4 — so Exchange and All were literally indistinguishable and neither could be narrated. Routing is now on the STATE number, which for every screen here is reached from nowhere else in the machine.** **lint_seh CAUGHT A C2712 BEFORE MSVC COULD** — the first draft of the help reader mixed `__try` with a `std::string`, which MSVC rejects (no object unwinding in an SEH frame). The raw copy is now its own frame. That gate paid for itself. **Verified: menu_sim OK (0 bad) — new: the paging regression (one header, five slot lines across five pages), both Exchange panels naming their owner, column B's independent page, both popups, the split's paired counters, the All direction pinned in both steps AND in the transfer line, and a routing test proving screen mode alone cannot separate the two flows; fuzz extended to all eleven speaking phases. menu_magic_compile OK (0 bad) — new: the help-bar reader against a real mapped module, both "no text" sentinels, and declining off the Magic screen (so the GCW scrape still runs everywhere else). entryaim/trigwalk/trigseg/pathdecimate/vehsig OK; catalog_story and garden_aboard 0 failures; lint_seh OK (89 files); all four harnesses compile.** **BAT: page through the spell list (header once); press "/" on the action row, the list and the sort popup; run Exchange end to end including Split; and on All, listen to which step says "receive" and which says "transfer" and confirm the transfer runs that way. If it does not, the two group-8 strings are mislabelled BY THE GAME and that is itself the finding.**

**Prior (v0.22.0 LOCAL, NOT MSVC-built — #81: THE MAGIC SUBMENU).** Aaron's overnight task. Action row, the 32-slot spell list, cast target select, the sort popup and the discard confirmation, plus an OFFLINE SIMULATOR for the main menu and a bug it found in Ability. **THE STRUCTURAL FINDING, WHICH MATTERS BEYOND THIS SCREEN: everything this mod has called `pMenuStateA + 0x2xx` for two years is a field of a MENU MODULE OBJECT.** Pool at 0x01D76BC8, stride 0x78, 10 slots, MRU list head 0x01D76B48, allocator 0x004BE540, header {next, prev, update fn, draw fn, state u16, in-use u8}. Main menu = slot 1, open submenu = slot 2 — so **`+0x22E` ("focus") is slot2+0x10, the STATE WORD of a 114-state machine** (hence 3/13/20/72: they are jump-table indices), and **`+0x230` ("phase") is slot 2's IN-USE FLAG and never was a phase.** Slot 2 is an allocation-order COINCIDENCE, so the hook WALKS THE LIST and matches Magic by update fn **0x004F02F0** (submenu dispatch table 0x00B87ED8 idx 3 — whose Status/Save/Switch/Junction indices match the +0x1E8 values the mod already observed, confirming the identification from two independent directions). **THE SLOT FORMULA — the one computation that decides whether the right spell is named: `slot = (module[0x38+charId] & 3) + module[0x42]*4`.** 1 col x 4 rows x 8 pages; the cursor is stored PER CHARACTER and lags one frame after a page change, so only its low 2 bits are trustworthy (the game's own draw path masks it the same way at 0x004F0597). **TRAP AVOIDED: pMenuStateA+0x272 is the ITEM menu's list cursor** — in the Magic module that byte is written only during the post-sort redraw, so reading it would have named a plausible but wrong spell on every other frame. **LABELS ARE DECODED, NOT GUESSED** — from Data/lang-en/menu.fs -> mngrp.bin section 1 bank 8 (the "group 8" the code passes to 0x004BD630), via the entry tables the code itself indexes (0x00B88A90 action row = pairs {0,1,11,2}; 0x00B88A9C sort = pairs {15..21}): **Use / Exchange / All / Rearrange** + 7 sort orders. That also settles what disassembly could NOT: action 1 = Exchange (one member), action 2 = All (everyone). Screen says "Exchg."; the mod says "Exchange" (an abbreviation is a pixel constraint, meaningless aloud), and the sort interpuncts are read as "then". **Uncastable spells are GREYED NOT HIDDEN** (draw 0x004F71DB picks colour 1 vs 7) — invisible to a blind player and the difference between a spell that casts and one that silently refuses, so the list says "cannot be cast here", **in the Use flow only** (in Exchange/Rearrange every spell is fair game and the qualifier would be a lie). mmagic.bin (ptr 0x01D2BB10, stride 4, byte0 bit0) gives exactly Cure/Cura/Curaga/Life/Full-Life/Esuna/Dispel — extracted offline, and exactly what FF8 allows. **NEW ARCHITECTURE WORTH COPYING: the wording lives in src/menu_magic_model.inl as PURE FUNCTIONS of a MagicView struct** (no Win32, no SEH, no absolute addresses); src/menu_tts_magic.inl is responsible only for finding the module and reading bytes. Every previous submenu put both in one file, so the only way to test the wording was to play it — which is why Junction is still "partially" done after two years. **tests/menu_sim.cpp drives all six phases offline and CAUGHT A REAL DEFECT ON ITS FIRST RUN** ("No target, 4 of 3" when the target cursor ran past the character mask). MenuSim models the MODULE POOL + a state machine, not "the Magic screen", so Ability/GF/Config can reuse it. tests/menu_magic_compile.cpp is the only pre-MSVC syntax check the hook gets and runs the module walk against a REAL MAPPED POOL (all 10 slots + out-of-pool, misaligned and cyclic lists). **BUG FIXED EN ROUTE: menu_tts_ability.inl's spell table listed Float/Drain/Pain at ids 38/39/40 — those are Blind/Confuse/Sleep** (Float 47, Drain 44, Pain 45), so an Ability refine preview yielding any of the three read a DIFFERENT spell's stock and spoke that number, on the one line whose whole job is "you already have N of these". Canonical 57-entry table now in menu_magic_model.inl; the cross-check that makes it trustworthy is that mmagic.bin's usable bit lands on exactly the 7 castable spells, which a wrong-id table could not produce. **WHAT THE OFFLINE GATES CANNOT DO: prove an ADDRESS is right.** If an offset is wrong the model and the simulator are wrong together and both pass. They prove that GIVEN the right bytes the mod says the right words in every phase at every cursor position. Addresses are what the BAT is for. **Verified: menu_sim OK (0 bad) — 16 spell-id anchors, the slot formula over all 32 slots with a deliberately stale cursor, list wording incl. empties and the greyed qualifier, the castable set + the 0x40 lock, the action row under 4 enable masks, target select over a sparse mask, the sort popup, the discard confirmation and its precedence, phase routing for 11 states, the module walk in all 10 slots, and 20,000 randomised states with no overflow and no silent speaking phase. menu_magic_compile OK (0 bad); entryaim/trigwalk/trigseg/pathdecimate/vehsig OK; catalog_story and garden_aboard 0 failures; lint_seh OK (89 files); all four harnesses compile.** **BAT: open Magic, pick a character, then — action row left/right; Use -> the spell list (up/down in a page, left/right to page, check a Cure reads normally and a Meltdown says "cannot be cast here"); confirm a castable spell for target select; Rearrange for the 7 sort orders; and if willing, trigger a discard and listen to the confirmation before answering. Grep the log for `[MagicTTS] Magic module at 0x… (pool slot N)` — if N is not 2 the module walk just earned its keep — and for the per-line `[MagicTTS] phase=… -> slot N : "…"`, which names the slot the mod thought it read if a spell name is wrong.** **NOT DONE, DELIBERATELY: the two-column Exchange/All screens** (sub-flows "Give All", "Split", "Take All"). v0.22.0 announces which member is being chosen (screen mode 3) then goes quiet on the two-column screen (mode 4) rather than read a layout it has not verified; docs/MAGIC_MENU.md names the offsets to check first.

**Prior (v0.21.8 — **BAT-CONFIRMED 2026-08-16: all four testable retargets arrived — Deling City at 37 units, the Tomb at 2, Winhill and Trabia in; zero real errors in 27,878 lines** — #79: THREE THINGS THE BAT LOG SAID).** **(1) TRABIA ENTERED AT dist=1360 AND THAT WAS THE TELL — ITS DOOR IS TWICE THE SIZE THE TABLE CLAIMED.** v0.21.7 restricted the aim to segment 149 (prog 3, dest 19, no bound) as the conservative choice, but **prog 4 grants the SAME dest 19 in segment 150 whenever Yoff>4096**, and the patch runs straight across the seam — 194 walkable entry tris in 149 and **172 more in 150**. He entered at (49966,-58834) — Yoff 6702, segment 150 — **1,363u from the aim and 818u OUTSIDE its bbox**, getting in only by passing through the eastern half en route to the western one. Taking both halves centres the aim in the real 2048x2048 door and raises the edge margin **418 -> 737, the largest in the table**; the whole rectangle is Yoff 5120..7168, entirely above the split. **Being conservative about a clause is right; being conservative about which HALF of a door to aim at is just aiming at the far side.** **(2) THE D-DISTRICT PRISON DOOR NEVER OPENS.** Aaron: *"it is not possible to enter the D-District Prison. You can see it on the world map but can never enter it from the world map. It is entered via a story sequence triggered in Deling City."* The geometry is REAL and correct — prog 16, 260 walkable entry polys, story>=350, and the drive went onto them — and no field loads. The prison is a mobile drilling rig that relocates and sinks, so its world-map presence is most likely vestigial by disc 3. **THE ROW STAYS DELIBERATELY** for the two reasons he asked for: the drive still delivers the player to the gate for an equivalent experience, and the planner still routes other journeys around a real firing area. **Written down so a future "arrived but nothing loaded" here is NOT read as a regression and re-investigated from scratch.** **(3) WINHILL HAS TWO DOORS AND v0.21.7 PICKED THE RIGHT ONE.** Aaron: *"the one that auto-drive took me to now is the one in the north part of town."* That is dest 14, the high side of the Xoff 6144 split, field 0x029E — the half the v0.21.6 whole-patch aim would have missed. Dest 15 is the other door, uncatalogued by his call. Bbox now from triangle VERTICES rather than the raster samples the aim search uses (raster under-reports by up to one step); west edge Xoff 6145, one unit inside the clause, since exactly 6144 satisfies neither `>6144` nor `<6143` and would open no door at all. **NEW GATE, WITH AN HONEST BOUND: the four positions where the game actually let him through must sit within the 200u position-SAMPLING LAG of their own area — NOT strict containment**, because `lastPos` on the arrival line is the drive's last sample and not the frame the engine ran its polygon test on; checked against raw triangles all four are 59-166u outside one, so asserting containment would assert something the instrument cannot measure. **Verified to FAIL on the v0.21.7 Trabia row at 818u**, so it is a real gate and not a rubber stamp. **CHECKED AND NOT A BUG:** the 9 km prison drive's four [NAVBLK-PRUNE] releases, two on learned blocks at the Tomb 30 km away — the prune picks the block NEAREST THE START and keeps anything FootBlockedCached calls genuinely blocked, so it released the two actually near the route first, then the only others left, and the plan succeeded. Recorded so the next reader does not re-open it. **Loose end, cosmetic:** the arrival line named `glrent1` for BOTH Deling City (field 0x02EE) and the Tomb (0x0318), and gave an EMPTY name for Winhill (0x029E) — two ids should not resolve to one name. Ids are right and nothing depends on the string; the field-name lookup is worth a look sometime. **Verified: entryaim_test OK (0 bad) at 22 rows; trigwalk_test OK; trigseg_test OK; pathdecimate_test OK; vehsig_test OK; catalog_story_test 0 failures; garden_aboard_test 0 failures; lint_seh OK (88 files); all four harnesses compile.** **BAT: Trabia Garden only — it is the one coordinate that changed. Approaching from the east should now enter at a much smaller dist. Nothing else in this build alters a drive; Winhill's bbox moved two units and the rest is comments.**

**Prior (v0.21.7 LOCAL, NOT MSVC-built — #79: THE FIVE HELD-BACK RETARGETS, RESOLVED OFFLINE).** Aaron: *"Can you figure out those five retargets offline using your simulator and available data?"* Yes — and not by measuring distances more carefully. **v0.21.6 bound patches to destinations by NEAREST CENTROID, which is a guess. The authority is: a patch sits in a SEGMENT, a segment has exactly ONE entry PROGRAM, and that program names its DESTINATIONS with clause bounds.** Deling City seg 264 / prog 8 / dest 8 / story>=333; D-District Prison 361/16/10/>=350; Trabia Garden 149/3/19/>=750; Winhill 393/24/dest 14|15/>=750; Great Salt Lake 373/21/24/>=1600. **The first four markers are IN the same segment as their own patch** — icon and door in one 8192u square with one program — so the binding is a fact, not an inference, and the 1-2 km is just how far a city icon sits from its gate. **WINHILL WAS THE ONE WORTH HOLDING BACK: program 24 SPLITS segment 393 at Xoff 6144 (dest 14 high, 15 low), the marker is at Xoff 7059, and the v0.21.6 whole-patch aim came out at Xoff 5825 — the LOW half, a DIFFERENT DESTINATION.** Shipping it would have been a confidently-wrong coordinate wearing a generated-therefore-trustworthy label. New aim AND its whole bbox clear the split (west edge Xoff 6147); the low half is left uncovered so the planner does not treat it as a no-go zone. **GREAT SALT LAKE vs CHOCOBO FOREST 3 SETTLED:** prog 21 has NO chocobo clause and a story>=1600 gate, while every forest program (1,2,5,31,35,36 = forests 2,1,4,5,7,6) has veh-49 and NO story gate — so segment 373 is Great Salt Lake, which fits a post-Lunar-Cry location. **Chocobo Forest 3's marker is in segment 374, which has NO walkable entry polygon at all** — that marker is wrong; the only unclaimed forest-shaped program is prog 14 (seg 279, split at Xoff 2560, dests 22/23, patches at (59348,-31753) and (61205,-30894)) which is 29 km away, so it is a CANDIDATE, not a conclusion. Not touched — guessing is what cost five builds at Edea's House. **footOnly is now DERIVED, not judged:** a veh-132 (FOOT_ALT) clause = car-enterable. Reproduces all seven hand-set flags (Dollet/Balamb Town false; Timber/Fire Cavern/Galbadia Station true) and makes Deling City and D-District Prison car-enterable, which v0.21.6's blanket `true` had wrong. **Confirmed in INSTRUCTIONS: sub_544630 @0x544702 copies bit 3 of the polygon you LEFT FROM into the UNK21 bit** (`mov cl,[ecx+0xe]; shr cl,3; and cl,1; ... mov [eax+0x6d],cl`) — the v0.21.6 BAT's re-entry-inhibit reading, now from the exe rather than from behaviour. **Also mapped: 20 of 38 programs are now bound; the 18 unbound are tabulated in the doc.** Three worth a later pass, all story-gated past the current save: FOUR programs share destination 26 (segs 406/407/438/439, >=1750) which is what Esthar City's four gates should look like (its marker is in seg 374 with the other two strays); Sorceress Memorial (prog 29, seg 441, 248 tris) and Tears' Point (prog 32, seg 506) each have their marker in the RIGHT segment with a real patch, so both are straightforward retargets at disc 4; and Galbadia Missile Base (prog 15, seg 327, story>=350) has an entry program but NO entry polygons in its segment — combined with Aaron's *"the base blows up after the mission on disc two"*, the likely explanation is that the world map swaps terrain blocks by story state and wmx.obj holds the post-destruction version. **Verified: entryaim_test OK (0 bad) at 22 rows — THREE NEW ASSERTIONS: Winhill's aim AND its whole bbox clear the Xoff 6144 split and are not the v0.21.6 coordinate; the four same-segment bindings still aim inside their own marker's segment; plus the existing invariants. trigwalk_test OK; trigseg_test OK; pathdecimate_test OK; vehsig_test OK; catalog_story_test 0 failures; garden_aboard_test 0 failures; lint_seh OK (88 files); all four harnesses compile.** **BAT: four of the five are testable at story 912 — Deling City (>=333), D-District Prison (>=350), Trabia Garden (>=750) and WINHILL (>=750), the interesting one since its aim moved to the other half of a split square. Great Salt Lake is gated at >=1600 and cannot be confirmed until much later; it ships because it is strictly better than the marker it replaces and cannot regress anything that already refuses. Read the `fieldName` on Winhill's `[DRIVE] Arrival via game-mode` line — it names which of destinations 14 and 15 the high side actually is.**

**Prior (v0.21.6 — **BAT-CONFIRMED 2026-08-16: `ehenter2` entered at 171 units from the aim after five builds of refusing, and Chocobo Forest 7 — the control whose aim moved 728u — at **34 units**, dead-on rather than lucky; zero errors, 51 `[ENTRYPATCH]` lines all correct** — #79: EDEA'S HOUSE — THE DOOR IS SEVEN TRIANGLES WIDE).** **TWO THINGS THE BAT SETTLED. (1) `0xFF21` (UNK21) IS A RE-ENTRY INHIBIT, NOT A WORLD-STATE LOCK.** Exactly one tick in the run refused program 34 — `unk21 bit=1` at 00:39:53, the frame he came back OUT of `ehenter2` still standing on the entry polygon; a second later he had stepped off and the bit was 0. v0.21.2 and v0.21.3 both carried "UNK21 might be holding the door shut" as a live hypothesis; it never was, and the gate is proven correct because it bit once, exactly where the engine needs it to. **(2) GALBADIA MISSILE BASE'S MISSING ENTRY POLYGONS ARE CORRECT** — Aaron: *"the base blows up after the mission on disc two, so there is no way to enter it at this point in the game."* Open question closed. **Known imprecision, observed and benign:** `[ENTRYPATCH]` read "standing INSIDE" for four ticks at Chocobo 7 before the field loaded — the bbox is a rectangle and therefore a superset of the triangles, and/or the engine wants a step ONTO a polygon rather than a standstill; aim margin plus a moving drive covers it, but do not read one INSIDE line as "it should have opened this instant".  **The refusal was never a story gate, a master gate or the destination table. It was 234 units of ground.** `sub_545EA0` opens with `mov eax,[0x020409FC]; test byte ptr [eax+0x0E], 8` — **that pointer is the POLYGON UNDER THE PLAYER.** It churned every frame because the polygon under your feet changes every frame; v0.21.3 read that as a broken instrument and retracted it, and it was measuring exactly the right thing and reading it correctly — **bit 3 clear, everywhere Aaron walked.** Being an entry polygon is not enough: it must ALSO be foot-walkable, and that is a different byte (`0x0F` bit 7, the move validator's flag the navmesh feed already uses). Map-wide there are **7,359 entry polygons and 2,877 foot-walkable ones**, and the gap is where this bug lived. **Segment 652 holds 103 entry polygons. SEVEN are foot-walkable** — the other 96 are the orphanage building — covering `x[-29975,-29310] y[69632,70078]`, about 665×446 units, roughly **a three-hundredth of the segment.** The shipped marker `(-28950,70090)` was **234 units east of it, against the wall**: v0.21.1's page-8 archaeology found the right 103-polygon structure and then aimed at the wrong face (*"east is the side the player arrives from"* — east is where the wall is). In the 2026-08-16 BAT the closest approach was `(-29144,70004)`, **287 units short**, with every gate the exe names passing on every frame because they all genuinely did pass. New aim **(-29459,69772)** — the interior point of the seven triangles farthest from their edge, 140 units of margin in every direction. **WHY v0.21.5 SAID MATCH ANYWAY:** programs 34/35 carry no coordinate clauses, so `[TRIGWALK]`'s finest positional unit is the 8192u segment — it printed `MATCH, destination 18` at twenty-three positions spanning 8 km while nothing loaded, and told him *"every condition the exe names is satisfied and the refusal is elsewhere."* **That read as a finding and was a blind spot with a confident sentence attached.** The sentence is gone; the polygon gate now gets its own `[ENTRYPATCH]` line beside every verdict. **THE UNCOMFORTABLE PART: `world_map_trigger_data.inl` has said “a field entry fires only while standing on a wmx poly with byte14 bit 3 set” since v0.18.3.206.** Three sessions then hunted the exe for a gate already documented in the file. The table was never wrong — **it was 7 rows long and Edea's House was not one of them.** **Method rule earned here: when a decode is confidently wrong, check whether an earlier decode in the same file already said the right thing before re-reading the exe.** The table is now GENERATED by `offline/gen_entryaims.py` (entry polys → keep foot-walkable → flood-fill into patches → aim = interior point farthest from the edge), and it reproduces the five aims found BY HAND IN THE FIELD to **Timber 91u, Dollet 54u, Balamb Town 47u, Fire Cavern 83u, Galbadia Station 379u — all inside their own proven bboxes**; Balamb Town's proven box turns out to be 43 entry polys of which 7 are walkable, the same 103/7 shape as Edea's. **Ten new firing areas shipped** (Edea's House, Tomb, Centra Ruins, Shumi, Chocobo Forests 1/2/4/5/6/7 — every marker within 800u of its own door); the seven hand-proven rows untouched. **Five held back for their own BAT** (Deling City 1,966u, Great Salt Lake 1,717u, D-District Prison 1,462u, Trabia 1,443u, Winhill 1,234u), plus two open questions: Galbadia Missile Base has NO entry polygon within 17 km though it is walked into, and Great Salt Lake / Chocobo Forest 3 resolve to the same patch. See `docs/WORLDMAP_ENTRY_POLYGONS.md`. Side effect worth having: planner2 prices non-target firing areas at +4096, so 7→17 areas means ten more real doors it routes AROUND — accidental field entry while passing a town should get rarer. **Verified: entryaim_test OK (0 bad) — NEW GATE: every aim inside its own bbox, names unique, all five field-proven points still inside their areas, Edea's bbox asserted to be the seven walkable triangles with all four historical positions outside it, the Forest 7 control, and no two firing areas overlapping (they double as no-go zones, so an overlap would make a destination permanently expensive). trigwalk_test OK (0 bad); trigseg_test OK (0 bad); pathdecimate_test OK (0 bad); vehsig_test OK (0 bad); catalog_story_test 0 failures; garden_aboard_test 0 failures; lint_seh OK (88 files); world_map_harness, garden_harness, chase_harness and minigame_bgbtl_compile all compile.** **BAT: walk to Edea's House — that is the test. Then re-walk Chocobo Forest 7, the control, whose aim moved 728u (it worked before by luck of the arrival radius, and should now work by aim). If the door still refuses, the `[ENTRYPATCH]` line says whether he was standing on entry ground when it happened.**

**Prior (v0.21.5 LOCAL, NOT MSVC-built — #79: THE ENTRY INTERPRETER RE-IMPLEMENTED FROM THE EXE AND RUN LIVE).** **Reading single addresses from the mod thread has been wrong TWICE** — the .21.3 "master gate" (`test byte ptr [eax+0x0E], 8`) reads a PER-FRAME POINTER that churns every tick (01FAACD8, 01FAC720, 01FB6528, 0201A878...) and printed `bit3=CLEAR` on every line INCLUDING WHILE AN ENTRY SUCCEEDED. A sampled address proves nothing when you cannot tell a stable value from a transient. **The control experiment is what proves things: Chocobo Forest 7, the segment next door, OPENED ON DEMAND** — so the walker runs, entries work, and Edea's House alone is refused. This build evaluates the WHOLE 38-program set with the semantics from the dispatch tables at 0x546CAC/0x546D3C, giving a known-good and a known-bad through IDENTICAL CODE. **RESULT:** `Chocobo Forest 7 -> program 35, destination 38 (the game agrees)` and **`Edea's House -> program 34, MATCH, destination 18`.** Every condition the exe names is satisfied at the position the screenshot puts at the orphanage wall. **That moves the search off the CONDITIONS and onto what happens after them: `sub_545EA0` extracts the destination and hands it to `sub_544630` — DESTINATION 18 and the table that turns an id into a field are the next thing to decode.** New `[TRIGWALK]` line once a second and at every sweep start, reporting only the programs covering the player's current square. Gates proven to bite (story 899 fails the window, UNK21 bit 1 fails, the skip bypasses, a Ragnarok fails the vehicle clause, the old marker fails the segment) so a MATCH means something. **Verified: trigwalk_test OK (0 bad) — NEW GATE. trigseg_test needed a one-line stub once TriggerEvalTick gained the walk call and CAUGHT THAT AS A LINK ERROR IMMEDIATELY. pathdecimate_test, vehsig_test, catalog_story_test, garden_harness, aboard_test, world_map_harness, minigame_bgbtl_compile, lint_seh (88 files) all pass. READ-ONLY — no steering, planning or catalog logic changed.** **BAT: stand at Edea's House a few seconds, then at Chocobo Forest 7 a few seconds, send ff8_world.log. Both MATCH => the model is right and the difference is downstream in the destination table. Disagreement => the model is wrong and the line names the clause.**

**Prior (v0.21.4 — **BAT-CONFIRMED 2026-08-16: Chocobo Forest 7 and then a THIRTY-ONE KILOMETRE single leg from the Galbadia Missile Base to the Tomb of the Unknown King, both arrived** — the longest foot drive this mod has completed, and exactly the length class the silent truncation used to eat. **The missing SFX in that session was NOT this build: the ducker was balanced 82/82, both audio hooks installed cleanly, volumes sat at configured values, and a restart brought SFX back with no mod change.** Worth knowing anyway, from reading it: **`SfxVolumeUp`/`Down` only push the value to the game `if (s_sfxHookInstalled)`** — otherwise they update the number, SPEAK it, and save it to config while changing nothing, so the announcement can never tell the player the control is inert. Originally (NOT MSVC-built — #70: THE FOOT AUTO-DRIVE REVIEWED AGAINST THE GARDEN SYSTEM, AND FIVE DEFECTS IT WAS CARRYING).** Aaron: *"I suspect the walking auto drive system could be improved based on the findings from the B-Garden system research."* Two full read-throughs (planner + executor) written up with file:line evidence in **docs/FOOT_AUTODRIVE_REVIEW.md** and the project doc `claude/FOOT_AUTODRIVE_REVIEW.md`. **SHIPPED, the five that are unambiguous:** **(1) A CUTSCENE IS NOT A STALL.** The Garden has paused on an open dialog since .20.87; `IsDialogOpen()` was declared for that fix and **the foot executor never called it.** The freeze detector fires at ~0.65 s and its FIRST ACT is `CamwLearnBlock`, writing into an overlay that is DELIBERATELY PERMANENT — so every world-map cutscene libelled open ground forever, then pressed keys into a locked engine. Now pauses, releases keys, and on resume bumps `s_driveWatchdogGen` (the mechanism .18.3.216 already built for battles). **(2) THE OVERLAY WENT SILENTLY STERILE AT 256.** `AddNavBlock` returning false is also the "no new knowledge" signal that drives the fence-inflation recovery — whose job is to add blocks. At 256 every recovery was a no-op and every replan sterile, unlogged. Now evicts oldest-first and logs. **(3) THE GRID PLANNER TRUNCATED LONG ROUTES AND MARKED THEM PLANNED.** A 1,500-cell route kept 768 waypoints and **ended 732 cells short in open country**, no log line. Now decimates and always keeps the goal. **(4) `s_drivePathWorld` NEVER RESET BETWEEN DRIVES** — it gates the whole route-watchdog block and is ALSO written by the Garden executor, so whether a foot drive had watchdogs could depend on the previous drive. **(5) DIAGNOSTICS SHIPPED ON** — `WM_MOTION_DIAG` wrote one UNTHROTTLED synchronous log line per poll tick (120-180 file writes/second, every drive); `DRIVE_STEER_DIAG`'s own comment said "set false before push". Both off. **⚠ METHOD NOTE, AND THE REVIEW WAS WRONG ABOUT ITS OWN TOP ITEM.** It ranked "the LOS clamp and lookahead are computed then discarded — one line to fix" first. The discard is real but **DELIBERATE AND DOCUMENTED**: v0.18.3.155 records the far selector oscillating between adjacent corridor cells, the character limit-cycling, and the offline sim carrying it 20 km with sequential follow vs ~700u with the selector. **An audit that reads only the code will recommend reverting fixes whose reasons live in the comments.** The real fix is the Garden's commitment+LOS pair behind a harness — item 4 in the review's order of work, not a one-liner. **Verified: pathdecimate_test OK (0 bad) — NEW GATE, and it earned its place immediately: the FIRST version of the decimation fix picked stride 2 for a 1,536-cell route, every stride-aligned index came out odd, and the goal was never emitted. The test caught it before the build left the machine.** Also trigseg_test, vehsig_test, catalog_story_test, garden_harness 26 ok / 0 bad, aboard_test, world_map_harness, minigame_bgbtl_compile 0/0, lint_seh OK (88 files). **BAT: drive somewhere long on foot — the log should be MUCH quieter (no [MFRAME], no [YAWDRIVE]). Walk into a world-map cutscene if you can: it should say "cutscene ... pausing; this is not a stall" instead of learning a phantom obstacle. Items 2-5 are fault-path only; a clean drive executes none of them.**

**Prior (v0.21.3 LOCAL, NOT MSVC-built — #79: EVERY CONDITION PASSES AND THE DOOR IS STILL SHUT).** **The v0.21.2 instrument came back unanimous across 3 sweep-starts and 215 ticks:** `liveSeg=652` (the segment program 34 requires) | `posFlag=0` so the **LIVE** position is tested, NOT the parked Garden | `story=912` past the 900 gate | `UNK21 skip=0 bit=0` matching the operand | `vehId=0` on foot. **Every condition the exe's own decode names is satisfied and no field loads. Both suspects from .21.2 are CLEARED.** **ONE GATE IS LEFT AND IT IS THE FIRST INSTRUCTION IN THE WALKER:** `0x00545EA3 mov eax,[0x020409FC]; 0x00545EAB test byte ptr [eax+0x0E], 8; jne <walk the 38 programs>; xor eax,eax; ret` — **with bit 3 CLEAR the game never evaluates a single entry program anywhere on the world map.** Every [TRIGEVAL] line now prints `GATE [020409FC+0x0E]=0xNN bit3=SET/CLEAR`. **THE CONTROL EXPERIMENT, and it is cheap: Chocobo Forest 7 is SEGMENT 653 — the square immediately EAST of Edea's House — and its program (35) is `SEGMENT==653` + on-foot/chocobo -> DEST 38 with NO story gate, NO UNK21 and NO coordinate bounds.** It is already in the catalog, ~8 km east. **Forest opens + Edea's does not => the gate is set, the walker runs, and program 34 is refused for a reason still unnamed (a much narrower search). Neither opens => the gate is clear and NOTHING on the world map is enterable right now — a game-state condition, not a location problem.** **Verified: trigseg_test OK (0 bad); vehsig_test OK; catalog_story_test 0 failures; lint_seh OK (88 files); minigame_bgbtl_compile 0 errors / 0 bad; garden_harness 26 ok / 0 bad; aboard_test and world_map_harness pass. Read-only instrument — one extra field on a line that already existed.** **BAT: stand at Edea's House a few seconds (ticks are enough, no drive needed), then auto-drive to Chocobo Forest 7 and walk in. Send ff8_world.log — I only need the GATE field and whether the forest let you in.**

**Prior (v0.21.2 LOCAL, NOT MSVC-built — #79: THE TRIGGER DECODE WAS WRONG, AND THE EXE SAYS SO).** Aaron's F11 shot shows **Squall standing beside the orphanage LIGHTHOUSE** — the marker is right, the door is not opening for another reason. **THE OPCODE MEANINGS ARE THE OTHER WAY ROUND.** `sub_546100` dispatches through the jump table at **0x546CAC** indexed by the byte map at **0x546D3C**; read both out of the exe and the research doc this module was built on does not survive: **`0xFF06` is NOT a location/field id — it is a SEGMENT TEST** (slot 2 -> 0x00546192, which calls **sub_553910**: `row=((Y+0x48000) mod 0x30000)>>13, col=((X+0x60000)&0x3FFFF)>>13, index=row*32+col` — 32x24 squares of 8192); **`0xFF08` is the DESTINATION id, not a region test**; and **`0xFF0F/10/11/12` are the X/Y bounds inside the segment** (0x005463A7 Xoff<, 0x00546406 Yoff<, 0x00546461 Xoff>, 0x005464C0 Yoff>). The transcription reproduces the mod's OWN arithmetic: (-23150,62853) prints as `seg(13,19)` in every PLAN-DEBUG line and 19*32+13 = **621**. **RE-DECODED, PROGRAM 34 IS EDEA'S HOUSE:** `SEGMENT==652` (x[-32768,-24576] y[65536,73728]), `STORY>=900`, `UNK21 0`, on foot -> `DEST 18`. **Aaron's story is 912, he is on foot, and the lighthouse (-29585,70739) is INSIDE that box — every condition the mod can model passes.** And **program 32, which .21.1 reported as the story-locked Edea's House entrance, is SEGMENT==506 = x[81920,90112] y[24576,32768] — the far EAST of the map.** My "the game has the door locked" call two builds ago rode on that mis-decode; Aaron was right to reject it. **The v0.21.1 marker IS inside 652 and the old one was not, so that move was correct even though it did not open the door.** **TWO UNMODELLED CONDITIONS REMAIN, NEITHER DECIDABLE STATICALLY, SO THIS BUILD INSTRUMENTS RATHER THAN GUESSES: (1) UNK21** (0xFF21 @ 0x00546A11): `if ([0x2040A34]!=0) pass; else pass only if ((*(byte*)([0x20403A4]+0x6D))&1) == operand` — program 34's operand is **0**, so that bit must read 0. **(2) WHOSE POSITION IS TESTED:** every position opcode uses cached block coords at `[0x2040A24]/[0x2040A28]` (/4) when **`[0x2040A30] != 0`**, else the live player position — **if a vehicle still owns it the test runs against the GARDEN, parked 6.3 km away in segment 621, a different square, and no amount of walking would ever open that door.** New `[TRIGEVAL]` line once a second on the world map and again at sweep start logs all of it. **Verified: trigseg_test OK (0 bad) — NEW GATE: five points whose segment the mod already printed from its own independently written arithmetic reproduce exactly, segment 652's box is asserted, four segment boxes round-trip, and segment 506 is asserted to be in the far east; vehsig_test OK; catalog_story_test 0 failures; lint_seh OK (88 files); minigame_bgbtl_compile 0 errors / 0 bad; garden_harness 26 ok / 0 bad; aboard_test and world_map_harness pass. READ-ONLY INSTRUMENT — no steering, planning or catalog logic changed.** **BAT: walk to Edea's House and LET IT SWEEP — the sweep is the point now. Send ff8_world.log. `posFlag` non-zero with `cachedSeg != liveSeg` = the game is testing the GARDEN's position; `UNK21 bit=1` = a world-state flag holds the door; both clean = the test fires and something downstream eats it. Standing anywhere in x -32768..-24576, y 65536..73728 for a few seconds produces the same lines.**

**Prior (v0.21.1 LOCAL, NOT MSVC-built — #79: EDEA'S HOUSE WAS 9.6 KM FROM ITS MARKER, AND IT IS THE SHUMI FAILURE REPEATING ALMOST TO THE KILOMETRE).** **v0.21.0 PASSED FIRST:** `[VEHSIG] verdict VETOED ... 23 of 24 ... vehicleId=0 says ON FOOT` twice, no "Vehicle detected.", and **the last twenty distance samples are `dist=5` and `dist=26` — the back-and-forth Aaron heard is the drive STANDING ON THE MARKER.** (Honest caveat: the turn guard alone did NOT suppress those verdicts — 23/24 still voted vehicle — the ENGINE-ID VETO carried it. The physics fallback would still misfire on foot if the id were ever unreadable.) **THE MARKER HAD NOTHING UNDER IT.** Audited EVERY marker against the map's own page-8 settlement patches first, because a method is only worth trusting if it reproduces the known-good cases: Fire Cavern 241u, Shumi 349u, Galbadia Station 502u, Tomb 540u, Chocobo Forests 619-734u, Centra Ruins 741u, Winhill 881u, Timber 900u, Balamb Town 1002u, Trabia 1396u, Dollet 1397u — **nineteen markers in that band, and Edea's House 7,110 units from a patch ALREADY CLAIMED by Chocobo Forest 7 at 728u.** **THE WHOLE SOUTHERN HALF OF THE MAP HAS EXACTLY FOUR PAGE-8 PATCHES** — Centra Ruins 291 (claimed 741u), Chocobo Forest 7 32 (claimed 728u), Chocobo Forest 6 32 (claimed 684u), and **103 polys at (-29583,70090), UNCLAIMED.** Settlement-sized (Winhill 248, Shumi 248, Tomb 64), 800x900 units, **terrain 29 — the Shumi signature**, and the planner's own comment already says that terrain is *"entered via terrain-29 polygon trigger, not wmsetus script event"*. Apron is terrain 29 on all four faces; EAST has the most (7 polys, h -346..-188) and east is the arrival side. **Marker moved (-23150,62853) -> (-28950,70090).** **⚠ I GOT THIS WRONG FIRST AND THE CORRECTION IS THE LESSON:** program [32] `ehhana1` (Flower Field) is gated `story>=1750` against a live 912, which LOOKED like the answer. Aaron: *"Edea's House is now open at this point in the game. It becomes reachable at the start of disc 3, which is where I am at now."* He is right — the orphanage is entered by WALKING ONTO ITS POLYGONS like Shumi, so program [32] is a later scripted visit. **A gate that fails does not mean the door is locked when the door is not that gate.** **AUDIT LEADS, NOT VERDICTS (none touched):** Fisherman's Horizon is 25 km from any patch and is CORRECT (platform in open ocean), as are Cactuar Island and the Missile Base. But these disc-3 destinations are about to matter and have never been driven to: Sorceress Memorial 3,713u, White SeeD Ship 4,203u, Chocobo Forest 3 4,315u, Tears' Point 6,036u, Island Closest to Heaven 6,290u, Lunatic Pandora 6,740u, Lunar Gate 8,321u, Esthar City 9,018u. **Verified: coordinate change only, no executable logic touched. catalog_story_test 0 failures (the only host test that compiles world_catalog.inl); vehsig_test OK; lint_seh OK (88 files); minigame_bgbtl_compile 0 errors / 0 bad; garden_harness 26 ok / 0 bad; aboard_test and world_map_harness pass.** **BAT: auto-drive to Edea's House — it is ~10 km WNW of the old marker, so expect a longer drive, and a FIELD SHOULD LOAD rather than a sweep. If it sweeps, grep `Manual field entry` / `arming entry capture`: any field loading within 3,000 units pins the true coordinate automatically, which is how Galbadia Garden's was found.**

**Prior (v0.21.0 LOCAL, NOT MSVC-built — #79: THE AUTO-DRIVE CALLED A WALKING MAN A VEHICLE, FIVE TIMES, AND ORBITED EDEA'S HOUSE).** Aaron: *"It would get 1-2km out, then announce vehicle detected, then wouldn't finish the approach and the distance would increase once again."* **THE LOG IN SEVEN LINES:** `21:29:27 YAWDRIVE dist=4750` → `21:29:31 dist=1604` → **`21:29:33 dist=16`** (the FOOT law closing 4,750 units without a stumble) → `21:29:33 VEHSIG VEHICLE DETECTED (24 of 24)` → `21:29:34 HDG-DIAG dist=293` → `21:29:38 dist=2469` → **an orbit at 1200-1400 that never closes.** Same shape at 21:21:18 (dist 4), 21:27:06 (dist 84), 21:28:41 (dist 14) — **every verdict lands the instant the drive reaches a WAYPOINT.** **WHY: THE MOD MANUFACTURES THE EVIDENCE ITSELF.** At a waypoint it SLEWS THE CAMERA to the new bearing in one write and the character then rotates to catch up over several frames — through all of them he is still walking the OLD heading, so the motion sides with `mh` by construction. Two adjacent frames say it: `cam=3403 mh=3403` (agree, no vote) then `cam=3855 mh=3403` (gap 452, he has not turned yet), and the turn that follows reads `1977, 1465, 953, 441` — the gap CLOSING as the rotation completes. **Of the 25 voting frames in the ring at the first verdict, EVERY ONE came from a turn.** The old comment claimed the verdict was "unreachable on foot (0/153 disagreement frames)"; that control data was honest, it just never contained a mod-driven camera slew. **TWO GUARDS: (1) a RUN of closing gaps is a turn and cannot vote** — one shrinking frame is noise, two in a row is a rotation; a vehicle's gap persists or wobbles but does not systematically close. **(2) THE ENGINE'S OWN vehicleId VETOES THE VERDICT** — `0x020409E0` read **0, on foot, at EVERY drive start of that session** while the heuristic declared a vehicle five times. **A heuristic does not overrule a direct reading.** An unreadable id (-1) still makes no claim, so the fallback keeps the job it was built for. **⚠ THE REASON THIS SURVIVED: NOTHING IN THE TREE COMPILES `world_map_drive_exec.inl`** — no host harness reaches it, so 66 KB of steering logic had no check before a Windows build. **The policy is extracted to `src/world_map_vehsig.inl` and `tests/vehsig_test.cpp` replays the 25 REAL frames from `ff8_world.log` through it:** the OLD policy latches (asserted first, so a fixture that stops reproducing the defect fails loudly instead of passing vacuously), the new one does not with the id readable or not, a vehicle with an unreadable id is still caught (steady OR wobbling gap), the id veto is announced once not once per frame, the `1977,1465,953,441` rotation contributes 2 votes instead of 4, and `VehSigReset` really resets. **Verified: vehsig_test OK (0 bad) — NEW GATE, add it to the run list; lint_seh OK (88 files); minigame_bgbtl_compile 0 errors / 0 bad; garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass. `world_map_drive_exec.inl` 66,357 → 66,207; `field_navigation.cpp` untouched at 81,645.** **BAT: stand outside Edea's House on foot and auto-drive to it — NO "Vehicle detected.", and the distance falls and keeps falling. Grep `[VEHSIG]`: silence is the pass. Then drive somewhere in the car to confirm `[VEHID] ... steering as VEHICLE from drive start` still fires — that is the path doing the work now.**

**Prior (v0.20.130 — **BAT-CONFIRMED 2026-08-16, THE MINI-GAME IS PRODUCTION READY: the loss/retry path is now played too — `18:57:21 "You are down."` with NOTHING after it (.128), `movie narration resumed (the round is lost)` (.129), field 95 suppressed, then `briefing 2 ... Squall 644/800 Foe 600/600` — the HP+200 bonus applied and the soldier NOT carried over — and F9 from the HOST PHASE skipped cleanly to Galbadia Garden. Across the win run and the loss run: ZERO errors, ZERO exceptions, ZERO phantom [POLL] speech, no [BGBTL] activity outside the fight windows, ~50 log lines per run. The settled state is written up in the project doc `claude/GARDEN_BATTLE_MINIGAME.md`.** Originally — #minigame-bgbtl: THE BRIEFING VETOED THE SOLDIER AND LEFT SQUALL SWINGING).** Aaron, on BAT: *"as I press punch or kick on the controls screen I can hear Squall striking the enemy. When I press enter to actually start, the enemy's HP is not full."* **THE 2026-08-15 LOG HAD ALREADY MEASURED THIS AND NOBODY LOOKED AT THE NUMBER:** `16:12:06 briefing 1 opened / armed Squall 440/600 Foe 0/0` → 34.5 s of tapping W, D, A, X to have the keys named → `16:12:41 briefing ended` → **`Foe 431/600`. 169 damage dealt before "Game start."** The control run is in the same log: briefing 2 lasted 5.4 s, he pressed nothing but Enter, and it reads `Foe 600/600`. **CAUSE — the price of the thing that makes the briefing work at all:** since .123 the field is NOT frozen (freezing `field_main` would stop the dialog box ever being drawn), so **`squ_punchkeyscan0` runs behind the Game Controls**, its four BTNTESTs read the same button word the key learner reads, and every tap REQ'd `squ_punching0`/`squ_kicking0` → `gal_hpcalc0` → `30 + rnd/4` off the soldier. **The calibration step was a free hit on the enemy, and a careful player who tapped each key twice to lock a binding started the round against a soldier already down a quarter of his health — which makes the fight EASIER and makes every "how many heavy punches" answer wrong.** **FIX: the briefing vetoes BOTH fighters.** Squall's three attack scripts redirect to `squall::push` — his own entity's `PUSH8 n ; RET 8`, the trick the soldier's veto has used since .123. **`squ_guarding0` is deliberately NOT vetoed:** it only raises the guard flag and the block key is the one control worth letting the player feel while being told about it. **LABELS DERIVED THEN VERIFIED IN THE SCRIPTS: a `.sym` group (count, start) spans count+1 slots and its names run header, default, talk, push, … so `push` is always start+3.** bg2f_31 squall = (18, 0) → **squall::push = 3**, keyscan 12; bgbtl_1 squall = (14, 22) → **squall::push = 25**, keyscan 28 — both keyscan numbers are what the log prints, and disassembly confirms `PUSH8 3 ; RET 8` and `PUSH8 25 ; RET 8`. The same arithmetic reproduces gal0::push = 45 and g_hei0::push = 48, already proven in play. **Verified: minigame_bgbtl_compile 0 errors / 0 bad with eleven new cases — all six of Squall's attack labels redirect to his push in the right field, the soldier is still vetoed alongside him, the guard AND the keyscan itself come through untouched, and the skip veto still leaves Squall's punch alone; lint_seh OK (88 files); garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass. `field_navigation.cpp` untouched at 81,645.** **ALSO IN .130, FROM THE SAME BRIEFING: "Punch, W." FIFTY-THREE TIMES** — `ff8_field.log` has 53 `[BGBTL-LEARN]` lines, every one of them `Punch`, and `ff8_mod.log` has eleven `"Punch, W." (interrupt)` inside two seconds. **A held key AUTO-REPEATS and every repeat is a fresh rising edge on the button word**, so the learner re-announced it and each announcement interrupted the last — the sentence never finished saying itself. **The learner now debounces per mask at 1200 ms**; a deliberate re-tap still confirms the binding, because pressing a key and hearing nothing reads as "the key is not registering". The probe drives six auto-repeats 100 ms apart and requires EXACTLY ONE announcement, then a re-tap after the window and requires that it speaks — it reports `a held key announced 6 times in 600 ms` against the old code. **BAT: on the Game Controls screen tap every key several times — the mod names each one and NOTHING ELSE HAPPENS, no strike, no impact sound, no HP movement. Then press Enter: the first health report must have the soldier at 100. Sit on the briefing half a minute tapping, which is what caught it.**

**Prior (v0.20.129 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE MOVIE STOPS TALKING OVER THE FIGHT, AND THE PUSH UTILITY STOPS DYING ON A THREE-DAY-OLD LOCK FILE).** **`disc01_33h.avi` plays behind the WHOLE Garden battle and carries its own audio description.** From the 2026-08-15 log: `16:14:07 [FMV_AD] Cue 2: "Turquoise energy and fire flash; Galbadia Garden presses in at the edge."` then `16:14:08 "Heavy punch ready. Let go of block and press D."` — **one second apart, both spoken with interrupt, and only one of them is something the player can act on.** The heavy punch already has a priority path because the HEALTH REPORT was cutting it off; it should not also have to out-shout the scenery. **The briefing has suppressed FMV narration since .123 — but `EndBriefing` resumed it, and `EndBriefing` is what STARTS THE ROUND.** The suppression covered the stretch where nothing happens and lifted for the stretch where everything does. **Narration is now held from the briefing until the round is DECIDED, and given back by whichever of the four things decides it: the win, the loss, F9, or `Disarm` (which resumes unconditionally, so no path can leave a player's descriptions off).** **Nothing is lost — `fmv_audio_desc` consumes each cue on its timestamp rather than queueing it, so a suppressed cue is DROPPED, and every cue worth hearing in that movie (Rinoa on the cable, the soldiers below, the run for the entrance) falls AFTER the win.** **⚠ WHY THREE PUSHES DIED ON `.git/index.lock`, AND THE RULE THAT COMES OUT OF IT: git commands run against the mod folder THROUGH THE COWORK DESKTOP BRIDGE CANNOT UNLINK THEIR OWN LOCK** — `warning: unable to unlink '.git/index.lock': Operation not permitted` — **so every `git status` I ran left one behind, and the next push hit it** (2026-08-02, 2026-08-12, 2026-08-15; the stale files are in `_to_delete/`). **DO NOT RUN GIT THROUGH THE BRIDGE.** The push utility runs natively and is unaffected. `push_to_github.bat` also gains a **Step 0**: if `.git\index.lock` exists AND `tasklist` shows no `git.exe`, remove it and log that it did; if a git process IS running the lock is real, leave it and let Step 1 fail with git's own message. (`Utilities/` is git-ignored, so that stays local.) **Verified: `minigame_bgbtl_compile` 0 errors / 0 bad with the policy written as its own assertion — narration must survive `EndBriefing` (the check FAILS on the old code and passes on this one) and must be back on after both the HP-driven win and the disarm; lint_seh OK (88 files); garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass. `field_navigation.cpp` untouched at 81,645 — 275 from the hard fail.** **BAT: from "Game start." to the moment you win, lose or press F9, the ONLY voice should be the mod's. The instant you win, the descriptions come back and the rescue scene narrates as it always has. Grep `[FMV_AD]` — held cues log as `[SUPPRESSED -- the mod holds the channel]`.**

**Prior (v0.20.128 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE ARC IS DONE, THE SCAFFOLDING COMES DOWN, AND THE LOG HAD TWO WARTS LEFT).** Aaron: *"Please review the log in full and in detail to check for any other problems or issues with the mini-game that we may have overlooked. After that look for any diagnostics, dead code, etc. left over from building this out and do the necessary clean up."* **THE REVIEW (16:12–16:16 run — two attempts, a deliberate loss, a heavy punch 600→148, six blocks, best streak 3, F9 into a rescue FMV that played in full and handed off on the game's OWN MAPJUMP3 at clock 1185): the mini-game machinery is clean. Three things around it were not.** **(1) THE FIGHT ANNOUNCED ITS OWN ARCHIVE FILENAME, once per attempt** — `announced fieldId=0x0098 name='bgbtl_1'` at 16:12:29 and 16:14:17. `FIELD_DISPLAY_NAMES[152]` is a placeholder (`// 152: bgbtl_1 (???)`), so the player heard a filename two seconds before the Game Controls opened. **Field 152 joins field 95 on `NO_NAME_FIELDS` — it is a fight, not a place.** **(2) THE LOSS WAS ANNOUNCED TWICE** — `16:13:06 "You are down."` then `16:13:07 "You 0"`: the outcome path returns, but the next poll saw the rounded percentage move and reported it as if the fight were live. **Health reports now stop at the outcome.** **(3) `+4294933093ms`** — DWORD underflow in the REQ timestamps, because `EndBriefing` restarts the clock while the ring still holds events stamped during the briefing. Gone with the line that printed it. **THE SCAFFOLDING: a run emitted 711 `[BGBTL]` lines and now emits ~75** — deleted `[BGBTL-REQ]` (260/run), `[BGBTL-CLOCK]` (188), `[BGBTL-KEY]` (102), `[BGBTL-BTN]` (57), `[BGBTL-STATE]` (28), the functions behind them (`LogKeys`, `LogKeyChanges`, `WatchButtons`, `WatchState`, `ReadButtonsAnySource` and its `frozen` plumbing, `KeyCand`), **7 orphaned statics and 14 unused constants.** **ONE LINE WAS ALSO JUST WRONG:** `[BGBTL] Squall's guard script REQ'd (label 31, squall::squ_kicking0)` — label 31 IS `squ_kicking0`; the guard is 32. It predates the `.sym` off-by-one fix and printed its own contradiction every run. Deleted rather than corrected: `s_guardSeen` has come from `WatchStreak` since .120, because var 340 increments once per BLOCKED HIT where the flag has one rising edge per HOLD. **⚠ `minigame/` WOULD HAVE BEEN PUSHED BY `git add -A`** — extracted `bgbtl_1`/`bg2f_31` field archives, `chara.one`, disassembled `FF8_EN.exe` handlers: **718 KB of game data, not ours to redistribute. Added to `.gitignore` with the reason beside it.** **`FF8OPC_VERSION` SAID "0.20.118" THROUGH TEN BUILDS** — every banner since .119 named a build that was not running. Bumped to 0.20.128, its 3 KB inline comment cut to a paragraph, one history line written for each of the ten. **Verified: `minigame_bgbtl_compile` 0 errors / 0 bad — the `WatchState` assertions are replaced by ones holding the same ground through surviving code (`GuardVarFor` per field, and `ApplyBlockAssist` writing that byte and NEVER var 1031, the soldier's); the probe's `#include <cmath>` also moved above the SEH macros, since `#define __try if(1)` turns libstdc++'s own `__try/__catch` into a syntax error. lint_seh OK (88 files); garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass.** **`field_navigation.cpp` untouched at 81,645 — 275 from the hard fail. `field_minigame_bgbtl.inl` 71,785 → 64,716.** **NOT CHANGED, YOURS TO CALL: (a) the background movie's audio description plays DURING the fight** — *"Turquoise energy and fire flash…"* at 16:14:07, one second before *"Heavy punch ready"* had to fight it for the channel; the briefing already suppresses FMV narration and extending that to the whole round would clear it. **(b) the game's own legend and name labels ("Punch Block Kick", "Squall", "Galbadian Soldier") are read at each fight start** while the briefing covers the same ground. **BAT: nothing about the fight should feel different. No "bgbtl_1" on entry; a deliberate loss says "You are down." and then NOTHING; `[BGBTL]` in ff8_field.log is ~75 lines, not 700, and the banner reads v0.20.128.**

**Prior (v0.20.127 LOCAL, NOT MSVC-built — #dialog: THE PHANTOM IS FOUR PHANTOMS, AND A FIELD CHANGE LEAVES EXACTLY THE DEBRIS AN FMV DOES).** Aaron: *"there is a repeat of phantom dialogue after Squall finishes his speech... Irvine's dialogue from before where he says to be CERTAIN there is nothing Squall can do to save Rinoa, inadvertently repeats. Irvine is not in the room and this is a phantom."* **THE LOG, IN SIX LINES:** `15:39:56 [GETSTR] Irvine "...until you're CERTAIN that there's nothing more you can do!"` ... three and a half minutes later ... `15:43:33 MAPJUMPO -> destField=255` / `15:43:35 [fieldload] id=255 'bgsido5a'` / **`15:43:35 [POLL] win[0] "the attack."` + `win[1] "l you're CERTAIN that there's nothing more you can do!"` + `win[2] "'s nothing more you can do!"` + `win[3] "an do!"`** — **FOUR of them in a single poll the instant the empty office finished loading.** Aaron heard the second because it is the longest; the others are the same sentence chewed shorter. **Every one starts MID-WORD — "l you're" is the tail of "until you're", "an do!" the tail of "can do!" — which is the tell: the window objects still point into the PREVIOUS field's dialogue buffer, partway through wherever its paging had reached.** The engine does not clear them on a field change and the poller had no way to know the field had changed. **THE FIX ALREADY EXISTED FOR A DIFFERENT TRANSITION:** PollWindows has carried an FMV guard since v04.16 (snapshot the windows as "already spoken" when a movie ends, then suppress and re-snapshot while pointers settle). **A field change is the same event with a different cause and now gets the same treatment — snapshot on the change, keep absorbing for 2 s.** The shared loop is factored into `SnapshotWindowsAsSpoken()` rather than copied a third time, and the FMV paths call it too so the three guards cannot drift apart. **Only the POLLER is held back — the new field's own dialogue arrives through the opcode hooks (MES/AMESW/ASK), which are untouched, so nothing real is delayed and this cannot swallow a line the game actually shows.** **Verified: a dedicated probe for the new helper (two windows holding the exact fragments from Aaron's log are both captured, show_dialog per-window tracking reset, critical section balanced) — `field_dialog.cpp` has no host harness so this is the check that exists; lint_seh OK (88 files); minigame_bgbtl_compile 0 errors; garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass.** **`field_navigation.cpp` untouched at 81,645 — 275 from the hard fail.** **BAT: play the run-up to the Garden battle — after Squall's speech, back in the EMPTY Headmaster's Office lower level, there should be SILENCE: no Irvine, no fragments. Grep `[POLL] field 0x` for each field change it absorbs.**

**Prior (v0.20.126 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE MOD WAS CARRYING THE SOLDIER'S DAMAGE BETWEEN ATTEMPTS, AND AARON FOUND IT BY THE FIGHT FEELING TOO EASY).** *"I only did a single power punch and it caused me to win. I thought it usually took two. The mod isn't causing a problem where the enemy's partial health is being carried over between attempts, is it?"* **IT WAS, AND THE LOG NAMES THE LINE:** `15:46:03 Squall reached -20 -- loss` (soldier left on 248/600) -> `15:49:47 briefing HP frozen at Squall 604, Foe 248` -> **`15:49:54 briefing HP drifted (Foe 248->600) -- restoring`** -> `15:50:07 squ_punching1` -> `15:50:08 foe HP reached -195 -- WIN`. **THAT "DRIFT" WAS NOT A LEAK, IT WAS THE GAME:** `squall::squ_out0` runs `PUSHI 600 ; WRVARW 356` to set the scene up for a fresh attempt, and **HoldBriefingHp reverted it to the 248 the soldier had been left on.** One heavy punch then finished a soldier who started the round at 41%. **HoldBriefingHp is DELETED.** It was written in .118 to stop the enemy scoring hits while the Game Controls were up, back when nothing else could — but **v0.20.123 replaced that job entirely**: the briefing vetoes the soldier's attack REQs, so g0_punching0/g0_kicking0 never run, so they never REQ squ_punched_up0, so squ_hpcalc0 is never reached and NO DAMAGE IS POSSIBLE. Two mechanisms for one job and the redundant one was overwriting the scene's own initialisation. **One mechanism now, and it is the one that cannot get this wrong: if no attack runs, no HP moves, and everything the SCRIPT writes is left alone.** **The Game Controls screenshot is clean** — seven lines, all inside the box, clear space under the last, the game's legend untouched below it. **Verified: minigame_bgbtl_compile 0 errors with the regression written as its own assertion (a briefing opening on a leftover `Foe 248` must let squ_out0's 600 stand, and Squall's retry bonus with it — it fails on the old code and passes on this one); lint_seh OK (88 files); garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` unchanged at 81,645 — 275 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: lose on purpose, then check the retry — the soldier back to full and TWO heavy punches needed again. Grep `[BGBTL-HP] armed` on the second attempt; the foe should read 600/600.**

**Prior (v0.20.125 LOCAL, NOT MSVC-built — #minigame-bgbtl: ONE LINE OF FmvSkip ATE THE WIN ANNOUNCEMENT, AND THE SOLDIER'S HP BAR WAS LYING).** **(1) "SKIPPING VIDEO" INSTEAD OF "YOU WIN", AND BOTH HALVES WERE THE SAME LINE.** Aaron: *"I still did not hear You Win once I pressed F9. I instead heard it say skipping video which was weird since it did not skip any video that I could tell."* `FmvSkip::RequestSkip()` — the CODE path added in .117 so the mod could end the battle movie behind the scenes — set `g_skipRequested`, and that flag is what makes `FmvSkip::OnFrame` say **"Skipping video"** with interrupt on, talking over the mod's own "Skipping. You win…" a fraction of a second later. And nothing appeared to skip because ending `disc01_32h.avi` just moves the host phase along, which reads as the scene continuing. **RequestSkip() is now SILENT — it is a code path, and a caller that wants the player told says so itself. Backspace is untouched.** **(2) HE FALLS — and the bar was wrong.** The 15:26:42 screenshot shows Squall alone on the wire: at clock 580 director0 takes the win branch and REQs `gal0::gal0_timeover0`, the soldier's fall animation, so F9 gives the same ending a won fight does. **But the same shot caught the Galbadian Soldier's bar still FULL RED while the mod had just said "You win."** The bars are NOT the HP variables: **opcode 315 (0x00529BF0) writes `[0x01D9CF5C + id*16]`** (id 0 = Squall, id 1 = foe) and the scripts only call it from inside squ_hpcalc0 / gal_hpcalc0 — i.e. only when a punch actually lands. The skip writes the variables directly, so the bars never heard about it. **Both gauges are now written by the skip and by the win called from foe-HP-zero.** **(3) The box cleared the legend as intended, but the measurer returned `210x108` for seven lines and the last sat on the bottom border — AMES's `+0x11` allowance leaves nothing under a full-height final line, so +8 of slack.** **Verified: minigame_bgbtl_compile 0 errors, probe now maps the gauge page and asserts the soldier's bar is emptied and Squall's refilled by the skip (not just the variables); lint_seh OK (88 files); garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` unchanged at 81,645 — 275 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) F9 — expect "Skipping. You win…" and nothing about video; (2) F11 after F9 — the soldier's bar empty, Squall's full; (3) F11 on the Game Controls — clear space under the last line.**

**Prior (v0.20.124 LOCAL, NOT MSVC-built — #minigame-bgbtl: BOX MOVED UP, DOUBLE-TAP REMOVED, WIN CALLED WHEN IT IS WON, GAME OVER SCREEN UNNAMED).** **(1) THE SCREENSHOTS SHOW NOTHING CLIPPED — the game's own measurer sized the box correctly — but FF8 DRAWS ITS OWN "W Punch / A Block / X Kick" LEGEND AT THE LOWER LEFT and a centred box lands on it**, hiding two lines. **Six lines instead of eleven, no blank spacers, and parked at `y = 8`** instead of vertically centred, which clears both the legend and the two HP bars. **(2) DOUBLE-TAP F9 REMOVED** at Aaron's request — it worked (14:33:14 requested, field 675 by 14:33:18) but existed to save a wait that only mattered while the fight was dangerous, and it cut the rescue to do it. **That also removes the last code in this module that ever wrote var 80, and the probe now asserts it outright: four clock values, twenty ticks each, the fight clock must come out exactly as it went in — the standing guard against .119's 82 seconds of dead scene.** **(3) "YOU WIN" MOVES BACK TO FOE-HP-ZERO, BUT MADE TRUE RATHER THAN OPTIMISTIC.** The reason it ever moved to the field-675 transition is worth keeping in view: **v0.20.111 announced it at foe zero and the foe then sat at 0/600 for SEVENTY SECONDS, kept swinging, and Squall bled 1000 -> -31 and LOST** — zero foe HP does not end the fight, the round is decided at clock 580 on `foeHP < squallHP`. So the foe reaching zero now **engages the same protection F9 uses (attacks vetoed at the REQ, both HP pinned) and THEN says "You win."** F9 marks the win the same way; the field-675 line only speaks if neither already has. **(4) FIELD 95 `testbl6` IS A MENU, NOT A PLACE** — the "Try again / Try again with HP+200" choice — so it is on a short suppression list in `field_announce.cpp`, a list rather than a blanked table entry so FIELD_DISPLAY_NAMES stays a pure id-to-name map with the reason written beside each omission. **Verified: minigame_bgbtl_compile 0 errors with the two new assertions (clock never written anywhere in the round; the win is called from HP AND engages its protection); lint_seh OK (88 files); garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` unchanged at 81,645 — 275 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) F11 the Game Controls — box at the top, nothing over it, the game's legend has the lower left to itself; (2) win on merit — "You win." the moment the soldier hits zero, and nothing lands after it; (3) F9 once — says "You win", rescue plays in full, a second press only says "Already skipping"; (4) lose on purpose — the Game Over options announce with no field name in front of them.**

**Prior (v0.20.123 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE GAME CONTROLS ARE ON SCREEN, AND THE field_main PAUSE HAD TO GO TO GET THEM THERE).** **WINDOW RENDERING HANGS OFF field_main**: `field_main -> 0x00471010 -> 0x0052BC00 -> 0x004A0880`, so the one-byte RET that has paused this scene since v0.20.106 **guaranteed no dialog box could ever be drawn or even finish opening.** The two features are mutually exclusive as built. **THE BRIEFING NOW PAUSES THE FIGHT, NOT THE FIELD** — the soldier's attack REQs are rewritten to the entity's own `push` script and both HP values are pinned, the mechanism the F9 skip proved on 2026-08-15. Three bonuses beyond the picture: the key learner reads the REAL button word instead of the engine-side stand-in it needed while frozen; the FMV keeps playing; and **there is no patched byte to restore any more, so no heartbeat, no guard thread, and no way for a fault in the mod to strand the player's game** — that whole class of danger is deleted. **The one thing the freeze bought that the veto does not is a stopped clock**, so `CLOCK_BRIEF_LIMIT` hands the fight back at clock 420 (resolution is 580) with "Starting now." — nobody loses to a reading speed. **THE BOX IS COPIED FROM AMES (opcode 0x65, 0x005291E0), the auto-SIZING message variant:** `set_window_object 0x004A0410` / `measure_text 0x004A0EC0` (low word = width, high = height) / `w = lo+0x10, h = hi+0x11` / clamp `x+w < 0x130`, `y+h < 0xE0`, min 8 / `set_window_geometry 0x004A07A0` / `open_window 0x004A0620` / `set_current_window 0x0049FD50` / `[0x00B8EE90]+0xD3,+0xD4 |= 1<<win`; WINCLOSE (0x00529B60) reverses it via `0x0049FBF0`. **THE SIZING IS THE GAME'S OWN** — 0x004A0EC0 walks the string with real font metrics; the mod only word-wraps to 34 columns first. **Window 7** (the scene's legend uses 4). Text encoded to FF8's charset (inverse of ff8_text_decode.h: A-Z 0x45+, a-z 0x5F+, 0-9 0x21+, newline 0x02) and it names the player's learned keys. If the window fails to open the briefing is still spoken. **COSMETIC ITEM FIXED: the key table now ships SEEDED** (W punch, X kick, A block, D heavy — FF8 PC's stock bindings and exactly what two runs measured on 0x01CE48B0) and deliberately **UNLOCKED**, so a first real press on a differently-configured machine overwrites the seed. No more "(key unknown)" on the first call of a session. **Verified: minigame_bgbtl_compile 0 errors, and the probe now maps an EXECUTABLE page over the six engine routines filled with `ret`, plus `mov eax,imm32; ret` at the measurer, so OpenBriefDialog runs its real geometry maths on a real measurement rather than a C++ stub; new assertions cover byte-exact FF8 encoding, wrapping that never exceeds its column and does break lines, a surviving source newline, open/close setting and clearing the window bit, the briefing handing back at clock 420 but not 100, and the heavy seed being present and NOT locked. lint_seh OK (88 files); garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` unchanged at 81,645 — 275 from the hard fail. New file `src/field_minigame_bgbtl_dialog.inl` (9,353). SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) LOOK at the Game Controls screen — a proper FF8 box, centred, every line inside it, with your own key names; (2) the scene is no longer frozen behind it, so confirm the movie plays on and you still take NO damage while reading (that is the veto, not a pause); (3) everything else as before. If the box does not appear, grep `[BGBTL-DLG]` — it logs the measurement and the derived box, or the exception.**

**Prior (v0.20.122 — BAT'd 2026-08-15, #minigame-bgbtl: ✅ THE SCENE IS DONE. AARON WON THE FIGHT ON MERIT AND THE SKIP WORKS.)** *"I heard the power punch, and it successfully landed. I did that a couple times and won the battle. I also tried using F9 to skip and that worked, I was able to proceed into G-Garden and no errors occurred and the next scene started as expected."* **The confirming log line is the summary: `11 attacks cued, 9 BLOCKED, best streak reached 6` — two heavy punches at 14:32:42 and 14:32:54, the foe at -281, won on health; SKIP ALL requested 14:33:14, movie ended 14:33:16, disarmed at field 675 at 14:33:18 — four seconds, clean exit; `KEYS LEARNED: punch=W kick=X block=A heavy=D`.** Compare the 2026-08-14 baseline: 75 attacks, 0 blocks, four straight losses. **ONE COSMETIC LEFTOVER, NOT WORTH A CYCLE ON ITS OWN:** the first `HEAVY PUNCH announced` of a session can read `(key unknown)` if the player did not tap the heavy key during the briefing's calibration — it names the key correctly from the next press onward (14:32:33 unknown, 14:32:40 D). The spoken fallback already says "your fourth action key". **Original details of the build below.**

**(v0.20.122 build notes — #minigame-bgbtl: THE RETRY ARMED IN A HALLWAY, THE HEAVY PUNCH WAS TAKEN AWAY BEFORE IT COULD BE THROWN, AND THE SKIP CUED THE ATTACKS IT HAD JUST CANCELLED).** **(1) "TRY AGAIN" DROPS THE PLAYER IN THE B-GARDEN HALLWAY FIRST.** Aaron: *"All of the mini-game accessibility machinery needs to stop and wait to turn on again when the mini-game begins."* The log: `14:10:35 briefing 2 opened by the host field reloading` ... `14:11:28 the battle FMV finally starts` — **forty-six seconds of hallway with the REQ hook live, the assist pinning a guard flag and the block cue armed.** .121 briefed on the host field's RELOAD, which is the walk back to the fight, not the fight. **The module now DISARMS when the host field returns after a Game Over and waits for `disc01_32h.avi`** — the AVI latch clears once the name stops being reported, so the movie re-arms and re-briefs exactly like attempt one, at full health. **(2) THE HEAVY PUNCH: IT WORKS IN FIELD 152 AND THE LOG PROVES THE WHOLE MECHANIC** — `14:10:21 squ_punching1`, `14:10:22 g0_punched_up0`, `Foe 600 -> 66` in one hit. **In field 144 the same REQ fired and nothing happened, for two reasons that both punish the one move the scene requires:** (a) **var 340 is zeroed by any unguarded hit and you must release block to punch** — at 14:10:06 Squall took 332->285 in exactly that gap and `var340 3 -> 0` shut the keyscan's `var340 >= 3` gate, so SIX SECONDS of held D produced one attempt and then nothing; (b) **that attempt collided** — squ_punching1 is REQ'd into the same priority slot squ_guarding0 uses and the guard script was still running its animation tail. **Fixed by holding the gate open at var340 = 4 until squ_punching1 actually fires** (4, not 3, so `director5::sys_mes` does not re-fire every tick) **and by giving the block assist a 1.5 s GRACE WINDOW after the key comes up**, so releasing block to punch is no longer answered with an unguarded hit. **(3) THE SKIP'S TONE SPAM WAS THE RING CARRYING THE ORIGINAL LABEL.** Aaron: *"it caused the beep/block tone to spam repeatedly. I didn't hear any actual punches from the enemy just the beep as if they were about to."* Both halves are right and they are one bug: the veto worked (no attack script, so no punches) but the ring logged the label the script ASKED for, so every cancelled attack rang the tone — and faster than a real fight, because `push` returns instantly and the driver loops straight back. **171 cues in that run. The ring now records the label the engine really ran**, which fixes the noise and the trace together. **`KEYS LEARNED: punch=W kick=X block=A heavy=D` — learner and log line both correct now.** **Verified: minigame_bgbtl_compile 0 errors with new assertions — the hallway must disarm and the movie must re-arm, the heavy gate must survive a streak reset / must not sit at 3 / must release on squ_punching1 per-field, a vetoed attack must not read as an attack, the grace window must hold and must expire; lint_seh OK (87 files); garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` unchanged at 81,645 — 275 from the hard fail; `field_minigame_bgbtl.inl` 78,887. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) after a Game Over the hallway should be SILENT and the Game Controls should arrive when the fight movie starts, at full health; (2) hold A, wait for "Heavy punch ready", let go and HOLD D — it should land even if the first try does not take, and the gap should not cost the streak; (3) F9 should be quiet.**

**Prior (v0.20.121 LOCAL, NOT MSVC-built — #minigame-bgbtl: THREE REGRESSIONS, THREE SEPARATE CAUSES, AND THE NEW KEY LAYOUT).** **(1) THE RETRY BRIEFING WAS DISMISSED BY THE KEYPRESS THAT CAUSED IT.** Aaron: *"I only heard Game Controls once — on the first attempt."* It DID open every attempt (.120 fixed that) and then died: `briefing 2 ... confirm pressed 500ms in`, `briefing 3 ... confirm pressed 93ms in` — **he was still holding X from choosing "Try again" on the Game Over menu**, the host field reloaded, and the briefing opened into a held key. **A press now only counts once the key has been SEEN UP since the briefing opened.** **(2) F10 LOST THE FIGHT BECAUSE ZERO HP IS A LETHAL VALUE.** `13:46:37 SKIP engaged -- Squall parked at 0` -> `13:46:43 field 152` -> `13:46:47 field 95`, a Game Over four seconds in. .120 parked Squall at zero to silence `gal0::g0_fall0` (which gates every attack on `RDVARSW 354 > 0`); it silences him and it also loses, because the resolution is not the only thing that reads that value. **Squall is pinned at FULL again and the soldier is silenced AT THE REQ instead: the hook overwrites the label on the VM stack with the entity's own `push` script (`PUSH8 n ; RET 8` in both fields) before chaining** — same two pops, same priority slot, same bookkeeping, a script that does nothing, so no wind-up, no punch sound, no call into squ_hpcalc0, and nothing forged. **(3) THE HEAVY-PUNCH CALL WAS SPOKEN AND THEN TALKED OVER** — var340 hit 3, `director5::sys_mes` fired on cue, and the health report a fraction of a second later called `Speak(..., interrupt)`. **It now has its own path: never yields to a block cue, holds the floor against health reports for 2 s, and REPEATS every 5 s while the streak stays armed and unused.** **KEY LAYOUT, AT AARON'S REQUEST AND HE IS RIGHT:** *"pressing X confirms it is the kick but also closes the Game Controls"* — **X IS MASK 64, THE KICK**, so the key that dismissed the screen was one of the four the screen exists to teach. **start = ENTER (was X); repeat controls / block-hold toggle = SPACE (was F9); skip = F9 (was F10, which is the Windows system menu key).** **THE KEY LEARNER WORKS AND FOUND THE LAST UNKNOWN: `Heavy punch = mask 32, vk 0x44` = D, locked after two presses.** Full set on Aaron's machine: **W punch, X kick, A block, D heavy punch** — the briefing names them as he taps them. **⚠ SPLIT: `field_minigame_bgbtl.inl` reached 85,190 bytes, OVER the 81,920 hard fail — the skip machinery moved to `src/field_minigame_bgbtl_skip.inl` (10.5 KB) and the parent is back to 75,391.** **Verified: minigame_bgbtl_compile 0 errors with every new assertion written from this log — a confirm already down at open must be ignored until released, the skip must pin Squall at FULL and never write a lethal value, the attack REQ must be rewritten to `push` (45 / 48) while everything else passes through, the clock must stay untouched, the heavy call must fire at 3 and re-arm at 0; every previously silent `bad++` in the briefing block now prints what failed. lint_seh OK (87 files); garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` unchanged at 81,645 — 275 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) every attempt should open with Game Controls, including retries, and WAIT — Enter starts it, Space repeats it; (2) tap your four keys and expect "Punch, W" / "Block, A" / "Kick, X" / "Heavy punch, D"; (3) hold A, and after three blocks expect "Heavy punch ready. Let go of block and press D." repeating until used — then D should take most of the soldier's health, twice wins; (4) F9 stops the punching dead, F9 twice leaves for Galbadia Garden.**

**Prior (v0.20.120 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE CLOCK WRITE WAS MINE AND IT WAS THE WORST THING IN THE SCENE; IT IS GONE).** Aaron: *"After pressing F10 I didn't hear any punches or anything, but then after pressing X it suddenly sounded like Squall was being punched repeatedly. I also could no longer hear any of the background sounds of the Garden battle, it was like the background animation was completely gone."* **THE LOG SAYS IT IN ONE COLUMN: `13:21:16 SKIP: fight clock 115 -> 580` then EIGHTY-TWO SECONDS WITH NOT ONE REQ EVENT, then the punches restarting at 13:22:38 as the movie's counter reasserted itself and dragged the ladder back into the fight.** **WHAT .119 MISSED: FIELD 152'S ENTIRE FIGHT PLAYS OVER `disc01_33h.avi` — ONE MOVIE, ~105 s, WHOSE LAST STRETCH *IS* THE RESCUE.** There is no separate rescue FMV to jump to and no seek. Writing 580 into that movie's frame counter teleported `director0::default` to its resolution at movie frame 117: it released the fight entities (`op76` x4 — that is the silence), set the rescue choreography sixty seconds early, and then desynced. **THE MOD NOW NEVER WRITES VAR 80 DURING THE FIGHT; the stall governor is deleted.** **F10 INSTEAD PARKS SQUALL'S HEALTH AT ZERO**, because `gal0::g0_fall0` gates every attack on `RDVARSW 354 > 0` — that silences the driver without touching a timer — and restores him to full at clock 560, before the resolution reads him at 580. It cannot shorten the scene and now SAYS so: *"You cannot lose now and the soldier stops. The rescue scene still plays. Press F10 again to leave straight away."* **F10 TWICE ends the movie FIRST (the only state in which nothing else writes var 80) and then walks the ladder so director0 fires its OWN MAPJUMP3** — nothing forged, unlike .110 which crashed. **THE "2 BLOCKED" REPORT WAS AN INSTRUMENT BUG AND var340 WAS RIGHT:** with the assist pinning the guard flag there is one rising edge PER HOLD, not per hit, so **the count and the "Blocked." announcement now come from var 340**, which `squ_punched_up0` increments exactly once per blocked hit; the flag edge stays in the trace as proof the guard is set; the summary reports BEST streak. **`punch=W kick=X block=W heavy=X` WAS ALIASING IN MY OWN LOG LINE** — four NameForMask labels sharing two rotating buffers; one buffer per name now. **The real mapping, measured on 0x01CE48B0: W->16 punch, X->64 kick, A->128 BLOCK, 32 heavy (key still unseen).** The original briefing's key names were right all along; the guard variable and the label numbers were what was wrong. **THE BRIEFING'S CALIBRATION LEARNED NOTHING BECAUSE THE FREEZE FREEZES THE BUTTON WORD TOO** (0x01CE48B0 is written inside the routine field_main calls, and the briefing is a RET at field_main's entry) — it now reads the engine's held-buttons word while frozen, one layer up, same bit layout across two runs. **Also: `briefing HP frozen at Squall 440, Foe 0` was the module pinning the PREVIOUS scene's leftovers** (it arms on the FMV; `squ_out0` writes 600/600 after it), holding the fight's own initialisation down — it now refuses to pin until both fighters read initialised; **health reports go quiet while the skip holds the numbers**, or parking Squall at 0 would be announced as "You 0"; the host-phase FMV skip no longer re-fires once field 152 is reached. **Verified: minigame_bgbtl_compile 0 errors with new assertions written from this log — the first F10 press must NOT write the clock and must park then restore Squall, the second must wait for the movie before writing and stop at 1057, the block count comes from var 340 and survives a reset, the guard edge still honours the per-field byte and ignores the soldier's, four key names in one call do not alias; lint_seh OK (86 files); garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` 81,645 bytes — 275 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) play it straight — hold block, expect "Blocked." on most hits now, and when you hear "Heavy punch ready" try S, the one key the mod has still never seen; (2) F10 once — punching stops and STAYS stopped, background keeps running, scene plays to the rescue; (3) F10 twice to leave for Galbadia Garden.**

**Prior (v0.20.119 LOCAL, NOT MSVC-built — #minigame-bgbtl: AARON HAS BEEN BLOCKING CORRECTLY FOR FOUR BATs; THE MOD WAS READING THE SOLDIER'S GUARD FLAG).** **NO BAT WAS SPENT ON ANY OF THIS** — it came out of the exe, the two field scripts and the 2026-08-15 log. **THE ROOT CAUSE IS ONE OFF-BY-ONE: A `.sym` GROUP IS `(count, start)` AND SPANS count+1 SLOTS, because the slot at `start` holds the GROUP HEADER.** bg2f_31 proves it in a line — squall is `(18,0)`, the next group starts at 19, so squall owns nineteen slots and the .sym lists nineteen strings. Read off by one, `squ_punchkeyscan0` lands on a positioning script; read correctly it lands on label 12, which IS the keyscan (four BTNTESTs and a JMP -53). **Everything downstream was wrong: the block is MASK 128, not 64; Squall's guard flag is var 1030 (field 144) / var 1028 (field 152), NOT var 1031 — 1031 is the SOLDIER'S; the soldier's attacks are 55/56 and 49/50, not the trio including the driver `g0_fall0`; and "tap block, holding does not work" is the exact opposite of the truth.** **THE PROOF HE WAS BLOCKING IS IN HIS OWN LOG AND IT IS ARITHMETIC:** `squ_hpcalc0` rolls a byte and reads one flag — **guarded 15+rnd/8 (15..46), unguarded 40+rnd/4 (40..103)** — and **ALL 35 RECORDED HITS FALL INSIDE ONE OF THOSE BANDS, with all nine guarded ones from the single attempt he spent holding the key (6 of his 9 hits blocked).** `BLOCK SUMMARY: 0 BLOCKED` was the instrument, not the player. **AND BLOCKING ALONE LOSES, WHICH NOTHING HAD EVER SAID:** director0 resolves at `var80 >= 580` on `foeHP < squallHP`, and the only thing that moves the soldier's health enough is `squ_punching1` — the keyscan's fourth BTNTEST, mask 32, gated on `var340 >= 3`, doing **300+rnd against 600 HP**. var340 counts CONSECUTIVE BLOCKS (`squ_punched_up0` increments on a blocked hit, zeroes on an unblocked one) and at exactly 3 the game REQs `director5::sys_mes` to tell a sighted player. **So the scene is "block three in a row, heavy punch, twice" — now what the briefing says, with the streak announced.** **THE WARNING IS 133 ms** (`g0_kicking0` WAITs 8 frames between REQ and hit, `g0_punching0` 10; his log agrees to a tick) so no announcement can ever be a prompt — but `squ_guarding0` only gives a ~50% DUTY CYCLE, so **while the block key is held the mod now pins the guard flag the script itself sets.** Offline model, calibrated on his measured 0.72 s attack interval: **nothing held 0% blocked / 100% knocked out; key held, game's own 46% blocked / 7% round wins; key held + assist 100% blocked / 91.8% round wins.** F9 toggles it. **THE MOD NO LONGER GUESSES WHICH KEY IS WHICH, BECAUSE IT CANNOT:** the four masks are PAD BITS and `0x004A2D60` only consults FF8's remap table when `[0x01CFE73C]&0x20` (clear on his machine), so the keyboard mapping lives in the 2013 wrapper, not the exe — **the briefing now names his keys by watching `0x01CE48B0` while he taps them, which is safe because the field is frozen there.** **F10 WAS ONE SHORT, LITERALLY:** `.117` wrote 579 and waited for the engine to carry it over, but **VAR 80 IS THE MOVIE'S FRAME NUMBER** (`0x0052A016` stores `0x005305A0`'s result at `[0x00B8EE90]+0x50`, only while a movie runs) and F10 had just ended the movie — **the log shows clock=579 for FIFTY-THREE SECONDS while Squall was punched from 600 to 147.** Now it writes **580**, and when the clock stalls with no movie behind it carries it on at 15/s (the rate measured off the rescue movie) up to but never past 1057, backing off the moment the engine drives it again. Rescue FMV confirmed as `disc01_33h.avi`. **THE "ENEMY ACCUMULATING HITS ON THE GAME CONTROLS SCREEN" WAS THE BRIEFING ARRIVING 43 SECONDS LATE ON A RETRY** — the AVI latch means a retry could only be triggered by the on-screen legend, and field 144 reloaded at 01:12:16 while the legend appeared at 01:12:59 with Squall already at 498/600. **The pause itself is airtight: across a 47-second briefing the fight clock moved 100 -> 106.** Retries now brief the moment the host field reloads. **NEW OFFLINE TOOLING: `minigame/jsm2.py`** — decoding verified against the engine's decoder at `0x00530760`, which proves a zero-high-byte dword is the WHOLE 24-bit OPCODE, not "push literal N" (every such dword in both scripts is <= 323, inside the 400-entry table at `0x00B8DE94`) — **`minigame/sim.py`** (fight model; only the animation tail is assumed, and it is calibrated and labelled) and **`minigame/handlers.txt`** (every opcode handler either script uses, plus the expression table: ADD SUB MUL DIV MOD NEG EQ GT GE LT LE NE AND OR XOR NOT). **Verified: minigame_bgbtl_compile 0 errors, now mapping a real page at `0x01CE4000` so the assist and learner hit the address the script reads, and asserting the per-field guard byte with the SOLDIER'S flag explicitly not counting, the streak announcing once at 3 and re-arming at 0, the assist pinning only while held and only when enabled, the learner refusing to name a bit with two keys down and four names in one call not aliasing, the skip writing 580, and the governor nudging / capping at 1057 / leaving an engine-driven clock alone; lint_seh OK (86 files); garden_harness 26 ok / 0 bad; catalog_story_test 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` UNCHANGED at 81,602 bytes — 318 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) the briefing should open at FULL HEALTH every attempt including retries — tap your four action keys and it should name them; (2) HOLD the block key down and expect "Blocked." on most hits with small damage numbers; (3) listen for "Heavy punch ready", which names the key — release block, press it, and the soldier should lose most of his health; twice wins; (4) F9 toggles the hold; (5) F10 should reach the Rinoa rescue with no long stretch of punching. Grep `KEYS LEARNED` and `BLOCK SUMMARY`.**

**Prior (v0.20.118 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE BRIEFING WAS COSTING HEALTH, AND THE BLOCK KEY REACHES THE ENGINE).** **AARON FOUND A REAL DEFECT AND IT IS THE WORST ONE IN THE ARC:** *"if you sit on the Game Controls screen the enemy is accumulating hits on Squall -- on one attempt I sat there a long while, then started the game, and the enemy did a flurry of hits that immediately resulted in a game over."* **The freeze stops field_main but plainly does NOT stop whatever schedules the soldier's attacks, and the backlog lands the instant the field resumes. A pause the player CHOSE to take was costing them the fight.** **FIXED BY CONSTRUCTION rather than by chasing the mechanism first: the briefing now snapshots BOTH HP values when it opens and restores them every tick while it is up, and LOGS every drift it corrects — so the run that fixes it also measures how leaky the pause is.** The briefing also now polls HP and the clock AT ALL; `Update()` returned early while briefing, **which is exactly why no previous log could have shown this**. **THE BLOCK KEY REACHES THE ENGINE — THE MAPPING WAS NEVER THE PROBLEM.** `[BGBTL-BTN]` caught **`held=0x0040`** on Aaron's presses: **bit 64, precisely the mask bgbtl_1's fight loop tests to REQ `squ_guarding0`.** **AND YET THE GUARD FLAG NEVER SET ACROSS 75 CUED ATTACKS AND FOUR ATTEMPTS** — so the failure is between the engine's button word and the script, not the keyboard, not the binding, not his timing. **Also measured, and it undercuts the tap-versus-hold framing entirely: THE BIT IS NEVER HELD LONGER THAN 156 ms EVEN WHEN HE HOLDS THE KEY DOWN — 131 button transitions in the attempt he spent holding it.** Either the engine consumes that word on read or something clears it; that is the next thread and it is far better defined than "tap or hold". **F10: THE HP HOLD NOW CONTINUES THROUGH THE ENDING.** `.117` released at clock 601 and Squall was then punched from **383 down to 147** across the rescue scene. The clock write still stops at the resolution (the ending runs on that same counter and holding it would stall the scene) **but the HP hold now runs until the module disarms** — it costs nothing once the winner is decided and it is the difference between watching the ending and being beaten up during it. **Verified: minigame_bgbtl_compile 0 errors and it now asserts BOTH halves of that split — HP still pinned after the resolution, clock left untouched — alongside the host-phase FMV skip (exactly one request), the guard-flag detector and every earlier check; lint_seh OK (85 files); garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` UNCHANGED at 81,602 bytes — 318 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) SIT ON THE GAME CONTROLS SCREEN A GOOD WHILE, THEN START — health should be exactly where it was, no flurry; grep `briefing HP drifted` for how much the pause was leaking. (2) F10 — no damage from the moment it is pressed, all the way through the rescue scene. (3) Blocking is still expected to fail; that one is now a well-defined hunt on my side, not something for Aaron to keep testing.**

**Prior (v0.20.117 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE FIGHT LOOP FOUND, AND THE HOST CLOCK IS FROZEN).** Two answers out of the exe and the scripts, **no BAT spent.** **THE INPUT DISPATCHER IS bgbtl_1 LABEL 28 (dwords 295..350)** — misnamed `squ_punching0` by the SYM, but it is the FIGHT LOOP: reads var 356 (foe HP), loops, and tests FOUR BIT MASKS, REQ-ing one of Squall's actions for each — **mask 16 → REQ 29 squ_punching1 · mask 64 → REQ 31 squ_guarding0 (THE BLOCK) · mask 128 → REQ 32 · mask 32 → REQ 30 squ_kicking0.** The guard is REQ'd from **EXACTLY ONE SITE (dword 319)**, so a block really does go through that script — if bit 64 reaches whichever button word is read. **TAP OR HOLD, from the exe as Aaron asked: HOLDING IS NOT OBVIOUSLY WRONG.** The engine keeps **TWO** words — `0x01CD01F8 valid_buttons` (held, level) and `0x01CD0200 confirmed_buttons` (edge, **PLUS AUTO-REPEAT**) — and the repeat is visible in `engine_eval_is_button_pressed` (0x00468540): from +0x67 it compares an elapsed counter against a threshold and, when exceeded, re-copies the held state into confirmed and sets a repeat flag. **So a held key fires once immediately and again on every repeat — Aaron's instinct may well be right, and holding would look like INTERMITTENT blocking rather than none.** **Which word the fight loop reads is what decides it, and I could NOT settle that from the opcode table — the same table that has now misled me three times (squ_timeover0 as a failure, the per-field labels, the "never fired" guard). Not guessing a fourth time: BOTH words are now logged change-only with the three masks broken out, and one run answers it from data.** **F10: THE HOST FIELD'S CLOCK IS FROZEN — `clock=140` for the ENTIRE host phase in the `.116` log, it never moves.** That is why pressing F10 there left Aaron *"hearing the sound of Squall being punched for quite a while"*: there was nothing to shorten, and bg2f_31's ladder gates only on 1 and 9. **The phase ends when the battle FMV finishes and the script MAPJUMPOs to bgbtl_1, so the only way out is to END THE MOVIE.** New **`FmvSkip::RequestSkip()`** — the same three writes the Backspace path makes, factored out so a code-driven skip and a player-driven one cannot diverge — called ONCE when F10 fires outside bgbtl_1; then the existing clock bump to 579 ends the round and the game plays its own rescue scene. **Verified: minigame_bgbtl_compile 0 errors and it now asserts the host-phase behaviour directly — RequestSkip called EXACTLY ONCE across 30 ticks and the frozen host clock left untouched — alongside the guard-flag detector (two rising edges, two blocks, no double-count) and every earlier check; lint_seh OK (85 files); garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` IS 81,602 BYTES — 318 FROM THE HARD FAIL. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) F10 should now cut through the host phase instead of leaving Aaron standing in it — expect "Skipping video" then the round ending shortly after. (2) For the block, TRY HOLDING A FOR ONE STRETCH AND TAPPING FOR ANOTHER: `[BGBTL-BTN]` records both button words and `[BGBTL-STATE]` records the guard flag, so whichever works will be visible — and so will which word the game reads.**

**Prior (v0.20.116 LOCAL, NOT MSVC-built — #minigame-bgbtl: AARON WAS RIGHT TO PUSH — I MISREAD THE ROUND DRIVER, AND THE BLOCK MAY BE INVISIBLE TO A REQ HOOK).** Aaron, told his 44 rounds contained no blocks: *"is it possible that you are mistaking punches for blocks? I didn't do any punches, but I did do blocks."* **Back to the scripts, and he is right on the substance.** **`squ_timeover0` (label 33) IS NOT "you failed to act":** its body reads Squall's HP (`OP_0x11 354`), branches on ≤ 0, clears the damage flag 1028 and the state bytes 340/1030/1032, and ends in a wait loop. **IT IS THE PER-ROUND DRIVER AND FIRES ONCE A ROUND NO MATTER WHAT HAPPENS.** Reporting 44 of them as 44 failures was wrong — and **the timing said so too: its offset from an attack ranges 172–1328 ms where a genuine response label (`squ_punched_up0`) is tight at 328–563.** I read a round boundary as a miss. **AND THE DETECTION ITSELF MAY BE BLIND:** `squ_guarding0` (label 31) really is the guard — its body writes **VAR 1031 = 1**, exactly mirroring bg2f_31's guard writing 1033 — **but a REQ is only ONE way that flag can be set. If the engine's own input handler sets 1031 directly, a block works perfectly and is COMPLETELY INVISIBLE to a REQ hook.** That fits BOTH Aaron's account and the trace, and it means **the "regression since .105" I was about to chase may not exist. THE PAUSE-ALTERNATION EXPERIMENT I HAD STARTED BUILDING IS DROPPED — its premise no longer holds and it would have degraded his run to test a conclusion I could not justify.** **SO THE FLAGS ARE WATCHED DIRECTLY: `[BGBTL-STATE]` logs vars 1027–1034 per tick, change-only, with the fight-relative timestamp; VAR 1031 GOING 0→1 IS A BLOCK whether or not a script was REQ'd for it, and that is what "Blocked." is announced from now.** The REQ path is kept but demoted to recording which mechanism is in play. **PLUS A CHANGE-ONLY W/A/X TRACE** — the `.115` sampling caught keys only at the instant of an attack cue, which cannot see a tap landing 150 ms later, so *"A held at 2 of 66"* said nothing about whether he pressed anything. **Verified: minigame_bgbtl_compile 0 errors and it now DRIVES the guard-flag detector directly — two rising edges on var 1031 produce exactly two blocks, and a held flag is not double-counted — alongside every earlier check; lint_seh OK (85 files); garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **ALSO CONFIRMED IN THE `.115` RUN: the fight-clock skip works** — `SKIP: fight clock 326 -> 579` then `SKIP done -- clock 601 is past the resolution`. `.114` fired exactly as designed. **⚠ `field_navigation.cpp` UNCHANGED at 81,587 bytes — 333 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: play it as before, blocking on the tone. Expect "Blocked." when it lands. If it does not come, the log now answers why on its own — `[BGBTL-KEY]` shows every press, `[BGBTL-STATE]` shows whether the guard flag responded. Between those two there is nowhere left for this to hide.**

**Prior (v0.20.115 LOCAL, NOT MSVC-built — #minigame-bgbtl: CORRECTION — BLOCKING WORKS. TAPPING WORKS; HOLDING DOES NOT).** **I WAS WRONG, AND MY OWN EARLIER TRACE PROVES IT.** `.113` reported `squ_guarding0: NEVER` and I concluded blocking was unreachable. **THE v0.20.105 WINNING RUN'S LABEL HISTOGRAM — WHICH I GENERATED MYSELF — CONTAINS `8 label=31 squall::squ_guarding0`.** That build armed on field 152 ONLY, so every REQ in it is bgbtl_1 and the name is right. **Aaron blocked eight times in the run he won. Blocking is reachable and always was — I checked the recent logs and not the one that mattered.** **WHAT THE `.113` LOG ACTUALLY SHOWS, read attempt by attempt as Aaron asked:** in attempt 1 he held A **contiguously across six consecutive attack cues** (+19562, +23828, +24265, +26328, +28406, +30875 — A=1 at every one, A=0 at every cue before he started). During that held stretch squall's bgbtl_1 labels were `27 punchkeyscan`, `28 punching0`, then **`32 punched_up` ×4 and `33 timeover` ×4 — label 31 guarding: ZERO.** **The hold was real, sustained, and produced nothing.** **CONCLUSION: THE GUARD FIRES ON A KEY-PRESS EDGE, NOT A HELD STATE. TAP THE BLOCK KEY ONCE PER ATTACK; HOLDING IT DOWN IS EXACTLY EQUIVALENT TO NEVER PRESSING IT.** That reconciles everything — `.105` (normal play, tapping) 8 blocks, `.113` (deliberate holding) 0. Briefing now says it outright: *"Tap block for each attack. Holding it down does not work."* **AND A BLOCK IS NOW ANNOUNCED: "Blocked."** — Aaron has no other way to know his timing landed, the game gives a blind player nothing to distinguish a block from a punch that missed, and **without that feedback a 200 ms window cannot be learned.** BLOCK SUMMARY counts them. **ALSO CONFIRMED THIS RUN, and it is the whole of last round's ask: THE SKIP REACHED THE RESCUE SCENE** — `00:08:56 SKIP engaged` → `00:09:44 disc01_33h.avi` (rescue FMV, with its existing AD: *"Rinoa grips a cable and slides down from the Garden's underside"*) → `00:11:04 G-Garden - Back Entrance` → *"You win."* → Rinoa's *"Squall!" / "Thank you."* **The `.112` HP hold already delivers exactly what he asked for.** **⚠ THIS BAT RAN v0.20.113 — `.114`'s FIGHT-CLOCK WORK IS NOT YET TESTED.** And measured against this run it is worth less than advertised: of the ~2 minutes, only the **13 s inside bgbtl_1** is shortenable; the 35 s before is the battle FMV in the host field (ladder gates on 1 and 9 only) and the 80 s after is the rescue scene itself. **Verified: minigame_bgbtl_compile 0 errors with all `.114` checks intact (clock advance, skip release, 40-hit hold); lint_seh OK (85 files); garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` UNCHANGED at 81,587 bytes — 333 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: TAP A ONCE PER TONE, do not hold. Expect "Blocked." when the timing lands — that is the feedback that makes the window learnable. Grep `BLOCK SUMMARY` for the count; eight was the `.105` score with no cue at all.**

**Prior (v0.20.114 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE FIGHT CLOCK, AND F10 NOW LANDS WHERE AARON ASKED).** Aaron: *"it should essentially bypass the mini-game and go to the FMV where Squall rescues Rinoa. It should not jump all the way to the G-Garden entrance."* **Found offline, no BAT spent.** **bgbtl_1's driver `director0::talk` IS A LADDER OF WAITS ON ONE VALUE:** `OP_0x0E 80` against **1, 20, 580, 750, 840, 1057**, then `MAPJUMP3 675` as the LAST instruction. At dword 995 — immediately after the 580 wait — it reads var 356 (foe HP) and 354 (Squall HP) and branches to REQ either gal0's fall or squall's. **SO THE FIGHT IS A FIXED-LENGTH ROUND: HP IS NOT CHECKED UNTIL THE CLOCK REACHES 580** — the entire explanation for *"the enemy kept punching me after I pressed F10"*, because zeroing the foe changes nothing until the script looks. **AND EVERYTHING BETWEEN 580 AND 1057 IS THE RESCUE SCENE, which is exactly what `.110`'s forged jump to 675 threw away — the Garden entrance is the LAST INSTRUCTION.** **THE CLOCK IS PINNED BY THE EXE, NOT INFERRED:** opcode-table entry 0x0E (table 0x00B8DE94) is handler **0x0051CB70**, body `mov ecx, dword ptr [eax + 0x1CFE9B8]` — a DWORD read at an unscaled byte offset, the 32-bit sibling of the RDVARB (0x0051CBB0) and RDVARW (0x0051CBF0) handlers that pinned HP. **`OP_0x0E` appears six times in bgbtl_1 and twice in bg2f_31 and its operand is 80 in ALL EIGHT** — it reads one thing — and bytes 80 AND 81 were both in the `.103` whole-block trace's "moved during fight" list, counting upward. **THE FIGHT CLOCK IS A uint32 AT 0x01CFE9B8 + 80.** **SO F10 NOW: (1) pins HP so the script picks the player at 580, (2) pushes the clock to 579 once bgbtl_1 is loaded so that moment is NOW, and (3) LETS GO — past clock 600 it stops writing anything, because the ending from 580 to 1057 runs on the SAME counter and holding it would stall the very scene Aaron wants.** The host field gates only on 1 and 9, so pressing F10 there holds HP and waits for bgbtl_1 — which is exactly what he did last BAT. **No forged transition: the game plays its own ending, rescue FMV included, and reaches Galbadia Garden by its own MAPJUMP3.** **NEW `[BGBTL-CLOCK]` line once a second** — the clock is the spine of the whole scene, so logging it makes future timing questions answerable without another BAT. **Verified: minigame_bgbtl_compile 0 errors and it now asserts the clock is advanced to 579 under the 40-hit skip simulation AND that the skip RELEASES once the clock passes the resolution (no more HP writes), alongside every earlier check; lint_seh OK (85 files); garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **The `.113` block instrument is untouched — the same run still answers the hold-A question.** **⚠ `field_navigation.cpp` UNCHANGED at 81,587 bytes — 333 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT, both questions on one run: (1) HOLD A DOWN through the fight, then grep `BLOCK SUMMARY`. (2) On another attempt press F10 — expect "Skipping. Ending the round now, then the rescue scene plays.", the round finishing within a second or two, and THEN THE RESCUE FMV, not a jump to the Garden entrance. Grep `[BGBTL-CLOCK]` and `SKIP: fight clock`.**

**Prior (v0.20.113 LOCAL, NOT MSVC-built — #minigame-bgbtl: ANSWERING TWO QUESTIONS, AND INSTRUMENTING THE ONE I CANNOT).** Aaron: *"isn't there some way to tell when the enemy is about to start their wind-up animation? That is the signal a sighted player uses."* **THE REQ *IS* THE WIND-UP.** The soldier's animation script is INVOKED at the REQ we hook — that instant is the frame the animation starts, and the hit lands 172–375 ms later. **So a sighted player is NOT getting an earlier signal than the tone; they are getting a RICHER one** — which attack it is, and a visual rhythm to anticipate. Their window is the same ~200 ms; they just have more in it. **There is nothing in the script before the REQ to fire on — the director decides and REQs in the same block — so an earlier cue is not available, and claiming otherwise would mean inventing a mechanism that is not there.** **BUT THE ONE THING THAT WOULD SETTLE IT WAS NEVER MEASURED: `squall::squ_guarding0` HAS NEVER FIRED ONCE, IN ANY RUN.** Two very different explanations, indistinguishable from the trace: the presses arrived too late for the window, or they never reached the game at all. **NEW `[BGBTL-KEY]` INSTRUMENT: raw W/A/X sampled on EVERY soldier attack, the guard script watched for directly, and a BLOCK SUMMARY on exit — `N attacks cued, A held at M of them, squ_guarding0 fired: YES/NEVER`.** One run decides whether an auto-block assist is the answer. **WORTH TRYING FIRST BECAUSE IT COSTS NOTHING: HOLD A DOWN rather than tapping at the tone** — if the guard is a held state rather than a timed press, that alone makes the fight playable, and the summary line will say so. Aaron: *"is that going to stop me getting hit by the enemy?"* **NO, AND THE ANNOUNCEMENT NOW SAYS SO.** The `.112` hold pins Squall at full every tick so damage is UNDONE, but **the foe keeps swinging and the game keeps playing its hit sound** — and a player told "skipping" who then hears themselves being punched will reasonably assume it failed, which is exactly what `.111` did to him. F10 now says *"Skip on. He will keep swinging and you will still hear it, but you cannot lose. Wait for the round to end."* **MAKING THE HITTING ACTUALLY STOP MEANS ENDING THE FIGHT EARLY, WHICH MEANS FINDING ITS TIMER — NOT YET LOCATED, AND NOT GUESSED AT HERE.** **Verified: minigame_bgbtl_compile 0 errors with all `.112` checks intact including the 40-hit skip-hold simulation; lint_seh OK (85 files); garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` UNCHANGED at 81,587 bytes — 333 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: ONE RUN, ONE QUESTION — HOLD A DOWN through the fight, not taps at the tone. Then grep `BLOCK SUMMARY`: `fired: YES` → holding works and the fight is playable as-is; `fired: NEVER` with `A held at N>0` → presses reach the game but the guard needs something else, so auto-block is the answer; `A held at 0` → the presses are not reaching the game at all, a different and more fixable problem.**

**Prior (v0.20.112 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE WIN CONDITION IS NOT WHAT IT LOOKS LIKE, AND THE BLOCK WINDOW IS BELOW REACTION TIME).** Three runs, and the log answers all three. **RUN 2 — BLOCKING: ZERO SUCCESSFUL BLOCKS, AND IT IS NOT AARON'S FAULT. `squall::squ_guarding0` NEVER FIRES ONCE** in the whole attempt, in either field; every single attack lands. **The reason corrects a number this module has carried since `.102`: the gap from the soldier's attack REQ to the DAMAGE is 47–375 ms, median ~200** (`+203→+375`, `+891→+1063`, `+1906→+2156`, `+2734→+2922`, `+3359→+3531` in bg2f_31; 265–375 ms in bgbtl_1). **THE "700 ms REACTION WINDOW" WAS MEASURED TO `squ_timeover0`, WHICH IS THE ROUND TIMER, NOT THE HIT.** A 180 ms tone starting ~200 ms before the punch has barely finished when the punch lands — **blocking on the cue is below human reaction time, and a sighted player has the wind-up ANIMATION, which starts earlier than anything the script exposes.** No louder or earlier tone closes this; **there is nothing earlier to fire on. An auto-block assist would — Aaron's call, asked inline.** **RUN 3 — THE SKIP: FOE HP AT ZERO IS NOT A WIN, AND THE MOD SAID IT WAS.** Aaron: *"I heard it say I won but then I still heard the enemy hitting me until my health hit 0."* **The foe sat at 0/600 for SEVENTY SECONDS, kept attacking, and Squall was ground 1000 → −31 and LOST** (`23:38:44 Foe 0/600` win announced → `23:39:53 Squall −31`). **The fight ends when SQUALL reaches zero (immediate loss) or when the TIMER expires with most-HP-wins — the game's own hint said so all along. Zero foe HP only means the player cannot lose the HP RACE; it does not stop the foe swinging.** So **"You win." now fires ONLY on the transition to field 675** — the only win signal ever observed — and foe-at-zero says something true instead: *"Foe out of health. You are ahead."* **THE SKIP IS NOW A MODE, NOT A WRITE: F10 PINS the foe at 0 and Squall at full EVERY TICK** (not on the 250 ms health poll — one hit at low HP is the whole difference) **until the timer ends the fight. Nothing forged, so nothing can desync — which is what crashed in `.110`.** The outcome check also moved ABOVE the "nothing changed" gate in PollHealth: **a decision this important must not depend on a rounded percentage having moved.** **RUN 1 confirmed the good news: the tone fires just before the hit, in the host field, from the first attack — the `.111` per-field label fix works.** **Verified: minigame_bgbtl_compile 0 errors and it now SIMULATES THE FAILURE — engage the skip, take 40 hits of 90 damage while the script re-inflates the foe, and assert Squall is still pinned at 1000 and the foe still at 0 — plus the corrected foe-at-zero reporting, alongside every earlier check; lint_seh OK (85 files); garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` UNCHANGED at 81,587 bytes — 333 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) F10 should now actually work — "Skipping. You cannot lose now. Wait for the fight to end.", health pinned at full, fight running to its timer before Galbadia Garden loads; a minute or two is the game's own ending, not a hang. (2) No "You win." until actually in Galbadia Garden; foe-at-zero says "Foe out of health. You are ahead." (3) Blocking is STILL EXPECTED TO FAIL — that is the open question, not a regression.**

**Prior (v0.20.111 LOCAL, NOT MSVC-built — #minigame-bgbtl: LABELS ARE PER FIELD, AND THE FORGED JUMP IS GONE).** Aaron: *"I sat back and let the enemy hit me in order to listen for the block sound, and did not hear it until I was almost dead."* **THE TONE WAS NEVER LATE — IT WAS NEVER FIRED. LABEL NUMBERS ARE AN INDEX INTO THE FIELD'S OWN ENTRY-POINT TABLE AND MEAN NOTHING ACROSS FIELDS.** `.110` cued on 48/49 (bgbtl_1's `gal0::g0_punching0/g0_kicking0`) **while most of the fight runs in the HOST field**, where those numbers are something else entirely: six attacks in bg2f_31 with **NOT ONE CUE**, then the field changed to 152 at +23562 ms and the tones finally started — by which point Squall was at 11/600. **The correlation in that same log is unambiguous, ~700 ms of warning every time:** `+7468 lbl 55 -> +8203 dmg 440->382` · `+9546 lbl 56 -> 382->280` · `+12546 lbl 56 -> 280->193` · `+14812 lbl 55 -> 193->146` · `+16875 lbl 56 -> 146->106` · `+19546 lbl 55 -> 106->11`. **bg2f_31's own group table puts g_hei0 at labels 45..57, so 54/55/56 are its punching/kicking/guarding trio.** Both tables ship keyed by field id, **an UNKNOWN field never cues blind**, and `[BGBTL-REQ]` now carries the field id, the per-field NAME and a `<- BLOCK` marker **so the next mismatch shows up as a wrong name rather than as silence**. **THE FORGED VICTORY JUMP IS GONE — IT CRASHED.** Aaron: *"I tried using the F10 skip functionality, and it caused the game to crash when I entered G-Garden."* **Writing `push 675, 1019, 3384, 0, 128 ; MAPJUMP3` reproduces the BYTES the winning script writes but NONE OF ITS STATE** — director0::talk reaches that line having finished the fight and torn its scene down, and firing it mid-fight (in the host field, where that script is not even loaded) arrives in Galbadia Garden with the previous scene half dismantled. **THE SKIP NOW USES THE GAME'S OWN PATH: zero the foe's HP, top the player up, and let the script resolve the win exactly as it does on merit — knockout, scene, transition, flags. Nothing forged, so nothing can desync.** Slower (~68 s from zero HP to the field change, measured in the `.110` BAT) but the win is announced immediately from HP anyway. **The transition finding is KEPT IN THE SOURCE as a comment because it is still true and someone will be tempted again.** **THE TONE IS A REAL WAVEFORM NOW:** Beep() has no volume control at all, so it is replaced by a synthesised 22 kHz 16-bit PCM buffer played through `PlaySound(SND_MEMORY|SND_ASYNC)` at full scale — **two rising tones, 1046 Hz then 1568 Hz (C6→G6), 90 ms each with a 4 ms taper**, 180 ms total, well inside the 700 ms window and unmistakably not a game sound. **`winmm.lib` ADDED TO deploy.bat — the one build-system change in this release.** Briefing now says *"The mod plays a tone..."* / *"The mod also calls out health..."* rather than "I will" (Aaron: *"just seems weird for the mod to refer to itself in first person"*). **Verified: minigame_bgbtl_compile 0 errors and it now asserts the two label tables AGAINST EACH OTHER (host accepts 54/55/56 and rejects 48/49/57; bgbtl_1 the reverse; unknown field rejects both), that label 55 RESOLVES TO A DIFFERENT NAME per field, and that the tone buffer actually contains a waveform (peak 21304 in the first 64 samples) — alongside every earlier check; lint_seh OK (85 files); garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` UNCHANGED at 81,587 bytes — 333 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) the tone should be LOUD and obviously synthetic, two rising notes — press A on the briefing screen to hear it. (2) IT SHOULD NOW FIRE FROM THE VERY FIRST ATTACK, in the host field, ~700 ms before damage — sit back again and every hit should be announced in advance. (3) F10 must no longer crash: expect "Skipping. The fight is won.", then "Foe defeated. You win.", then up to a minute to reach Galbadia Garden through the game's own ending. (4) The briefing says "The mod", not "I will". Grep `[BGBTL-REQ]` — every line now shows `fld=` and a resolved name, attacks marked `<- BLOCK`.**

**Prior (v0.20.110 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE TONE, AND TWO THINGS THE MOD GOT WRONG ABOUT ITS OWN NUMBERS).** Aaron after the `.109` BAT (*"that worked well"*): three fixes. **(1) TONE IS NOW THE BLOCK CUE BY DEFAULT** (1000 Hz, 70 ms, still on a worker thread) — he heard both and chose it, **which is exactly what the F9 toggle existed to settle**. The briefing says so, and **PRESSING A ON THE BRIEFING SCREEN PLAYS THE TONE** so the player learns the sound before it matters — the same key that will do the blocking, on a frozen field where it does nothing else. F9 still toggles back to speech mid-fight. **(2) THE MOD LIED ABOUT THE ONE NUMBER IT EXISTS TO REPORT.** Aaron: *"I clearly heard it say the foe was at 0 after a hit, but then the foe still hit me."* The log has it exactly — `Foe 2/600 (0%)`: 0.33% rounded to nearest-5 is 0, so **a fighter with 2 HP left was announced as empty**. **A live fighter now floors at 5%; zero means zero.** **(3) THE WIN WAS ANNOUNCED A MINUTE LATE.** `.109` called it from the transition to field 675, but the BAT shows the foe finished at **22:51:30** and that transition not arriving until **22:52:38** — sixty-eight seconds, because the knockout and the rest of the scene run first. **The outcome is now called from HP the moment it is decided** (*"Foe defeated. You win."* / *"You are down."*), with the field transition kept only as a backstop. **GUARDED ON HAVING SEEN THE FIGHTER ALIVE, BECAUSE HP CARRIES OVER BETWEEN ATTEMPTS: in this same BAT the foe still read 98/600 for FORTY SECONDS into the retry** (`22:50:09 Foe 98/600` → `22:50:49 Foe 600/600`) **before the script re-initialised it — without that guard a retry would announce a win before the first punch.** **ALSO CONFIRMED, CLOSING THE LAST OPEN QUESTION IN THE HP ARC: after "Try again with HP+200" the log reads `Squall 800/800` — so VAR 380 IS THE RETRY BONUS, 600+200, and reading max rather than assuming it was the right call.** **ANSWERED, NOT BUILT:** Aaron asked whether the briefing screen could show an in-game dialog instead of a paused frame. **What is on screen now:** with `field_main` RET'd out the render pass still runs, so it is **the FMV still playing and moving**, with the fighters frozen mid-pose and the legend box and both HP bars overlaid — not a still frame. **It IS possible** — `DialogInject` Phase 1 synthesises a script context and calls `opcode_mes` directly, BAT-proven to render (2026-05-09), and no ASK is needed. **But the dialog state machine is driven by `field_main`, which is precisely what the briefing RETs out**, so it would have to be opened and allowed to reach state 0xD (~450 ms) BEFORE the freeze, and **a dialog that cannot close while frozen would block the fight — a session-ending failure mode traded for polish**, so it is written down rather than shipped blind. **⚠ ALSO: Aaron pressed F11 during the briefing and NOTHING WAS LOGGED in either log for the whole session — the screenshot never happened. F9, X and A all registered, so it is not the key poll. UNEXPLAINED, worth a look.** **Verified: minigame_bgbtl_compile 0 errors and it now asserts the zero-floor (2/600 and 1/800 non-zero, −1/600 zero) and the OUTCOME LOGIC END-TO-END against the mapped variable page — stale carry-over ignored, 2 HP not a defeat, 0 HP a win — alongside every `.108`/`.109` check; lint_seh OK (85 files); garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` UNCHANGED at 81,587 bytes — 333 from the hard fail. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) the briefing should describe the tone and A should PLAY it on that screen. (2) blocking cued by the tone, not speech. (3) "Foe 0" must never be spoken while the foe can still fight — the lowest before a defeat is 5. (4) "Foe defeated. You win." the moment the foe drops, not a minute later at the scene change.**

**Prior (v0.20.109 LOCAL — #minigame-bgbtl: BUILD FIX + THE LINT THAT SHOULD HAVE CAUGHT IT).** `field_minigame_bgbtl.inl(780): error C2712: Cannot use __try in functions that require object unwinding`. **ONE LINE:** `.108` put `const std::string avi = FmvSkip::GetCurrentAviName();` INSIDE the `__try` in `FreezeWatchdog`; std::string needs unwinding and MSVC refuses SEH in any function that does. Split into a tiny `IsGardenBattleAvi()` helper with no SEH — **which it never wanted anyway**, since it reads FmvSkip's own state through a normal C++ call, not engine memory through a raw pointer, so there was nothing there for a structured handler to catch. FreezeWatchdog keeps the heartbeat and the AVI latch and loses the `__try` entirely. **THE REAL LESSON IS THE GAP, NOT THE TYPO: THE HOST HARNESSES COMPILE WITH g++, WHERE `__try` IS A MACRO EXPANDING TO `if(1)` — so this entire class of error is INVISIBLE to every pre-build check the mod has.** That is the same shape as the `.89` declaration-order bug: MSVC-only, and therefore only ever caught by Aaron's build. **Twice is a pattern, so it gets a check: NEW `tests/lint_seh.py`** walks src/, finds every function containing `__try`, and flags any that also contains a type needing unwinding. **The rule MSVC applies is PER-FUNCTION, not per-block** — an object declared OUTSIDE the __try still triggers C2712 — so the lint scans whole brace-balanced function bodies rather than just the guarded region. **Verified BOTH WAYS: it reports the exact `.108` construct as a C2712 risk, and it reports the shipped tree clean across ALL 85 FILES THAT USE __try. RUN IT BEFORE ANY BUILD THAT TOUCHES AN SEH BLOCK.** **Verified: lint_seh OK (85 files, 0 problems); minigame_bgbtl_compile 0 errors with all `.108` behaviour intact — ten-minute no-cap check, FMV arm, unrelated-movie negative, once-per-attempt, Game Over re-arm, AVI latch, patch/restore on every exit, HP replay, legend filter; garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **NO BEHAVIOUR CHANGE FROM `.108` — the pause still has no time limit, the guard thread still watches the heartbeat, and THE `.108` BAT STANDS EXACTLY AS WRITTEN.** `field_navigation.cpp` unchanged at 81,587 bytes, 333 from the hard fail. **SPLIT BEFORE THE NEXT EDIT.**

**Prior (v0.20.108 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE CAP IS GONE; LIVENESS REPLACES IT).** Aaron: *"why have the cap at all?"* **He is right, and the cap was guarding the wrong thing. The danger a timeout protected against was never the player taking their time — it is THE MOD ITSELF DYING while the game's main loop is patched out with a RET. A timeout cannot tell those two apart, so it punished the first in order to protect against the second**, and in the `.107` BAT it did exactly that: the fight started while Aaron was still deciding. **THE RIGHT TEST IS LIVENESS, NOT ELAPSED TIME.** `FreezeWatchdog` — which runs above Update()'s early returns — now **stamps a heartbeat every tick while the field is frozen, and a DEDICATED GUARD THREAD restores the byte if that stamp goes stale for 3 s.** `FreezeField -> patch 0xC3, stamp, start guard` / `FreezeWatchdog -> stamp, every tick, forever` / `guard -> stale > 3 s ? restore and exit`. **That condition is true ONLY when something has actually gone wrong; it is never true because a player thought about it for a while.** The guard writes one byte and clears two flags — it never speaks and never touches the event ring, because by the time it runs the thread that owns those is gone. **SO THE BRIEFING NOW HAS NO TIME LIMIT AT ALL: it holds until X, and the reminder simply repeats.** **WORDING, per Aaron:** the 12-second reminder AND the closing line of the briefing are now the same sentence — *"Press X to confirm, press F9 to hear the controls again."* **ALSO FIXED, QUIETLY: the fight clock restarts when the briefing ENDS**, so `[BGBTL-REQ]` offsets read from the start of the FIGHT rather than from the arm, and **the 5-minute arm cap no longer inherits however long the player spent listening — which would have been a second timeout wearing a different hat.** **Verified: minigame_bgbtl_compile 0 errors and it now asserts that TEN MINUTES OF WAITING leaves the briefing up and the field still frozen (byte still 0xC3) and that X still ends it cleanly afterwards — alongside the FMV arm, the unrelated-movie negative, the once-per-attempt rule, the Game Over re-arm, the AVI latch, patch/restore on every exit path, the HP replay and the legend filter; garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` UNCHANGED at 81,587 bytes — 333 from the hard fail; all of this went into the .inl. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) the briefing should now wait INDEFINITELY — leave it a minute or two and confirm the fight never starts on its own, just the reminder every twelve seconds. (2) X resumes with "Game start." (3) Everything else as `.107`: armed at the movie with 600/600, no mid-fight re-briefing, F10 skips. Grep `[BGBTL] GUARD:` — THAT LINE SHOULD NEVER APPEAR; if it does, the mod's own loop stopped and the guard caught it, which is the only case the old cap should ever have fired in.**

**Prior (v0.20.107 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE PAUSE HELD; THE CAP LET GO).** Aaron: *"the mini-game started on its own without my pressing the X key. It let the battle proceed for a few hits, then the instructions reappeared. I think we need to pause the fmv / background as well."* **THE FREEZE WORKED PERFECTLY AND THE LOG PROVES IT: between `[BGBTL] field PAUSED` at 22:13:00 and `field RESUMED` at 22:13:30 there are ZERO REQ events and ZERO HP changes** — HP sat at Squall 440/600, Foe 600/600 for the entire thirty seconds, and the first REQ (`+30016ms`) and first damage (`22:13:31`) land AFTER the resume. **Nothing about the FMV was driving the fight. WHAT STARTED IT WAS OUR OWN 30-SECOND CAP**, which fired because no confirm ever arrived. **So the fix is not to pause more, it is to STOP GIVING UP: cap 30 s → 180 s, and while the freeze holds the player hears "Press X to begin." every 12 s so silence can never be mistaken for a hang. A SAFETY NET THAT FIRES IN ORDINARY USE IS NOT A SAFETY NET, IT IS A TIMER.** **THE SECOND DEFECT WAS REAL AND AARON SAW IT:** the game re-shows its legend at EVERY phase change and `.106` re-opened the briefing on each one — **twice in the middle of a live fight (22:15:07, 22:15:46)**. **Briefing is now ONCE PER ATTEMPT;** only a pass through the Game Over screen (field 95) earns another. **AND HIS INSTINCT ABOUT TIMING WAS RIGHT EVEN IF THE MECHANISM WAS NOT: `[BGBTL-HP] armed Squall 440/600 (75%)` — HE HAD ALREADY LOST A QUARTER OF HIS HEALTH BEFORE THE LEGEND EVER APPEARED**, in the seven seconds between the movie starting and the UI showing. So the module now **ALSO ARMS ON THE FMV ITSELF** (`disc01_32h.avi`, which plays nowhere else), from `FreezeWatchdog` — above Update()'s early returns — **freezing the scene before the first punch lands**. The name outlives playback, so the arm is **LATCHED**: without that the module would re-freeze the game the instant the fight ended, **and that is a test, not a hope**. **F10 = SKIP, on Aaron's request** (*"map F10 to the skip function during the mini-game and inform the player in the instructions"*), **DURING the fight as well as on the Game Over screen, and the briefing says so** — a player who cannot make the 700 ms window should not have to lose first to get past the scene. **SkipToVictory THAWS FIRST**, because the transition it writes is executed by the field script, which is exactly what the freeze stops. F9 keeps repeat-briefing / cue-toggle. **INSTRUMENT KEPT: `[BGBTL-KEY]` logs the raw X / Enter / F10 state once a second for the whole briefing** — whether a confirm was never pressed or never seen is not something to guess at twice. **Verified: minigame_bgbtl_compile 0 errors and it now ALSO asserts the FMV arms the scene, an UNRELATED movie does not, the legend does NOT re-brief within one attempt, a Game Over DOES, and the latched AVI cannot re-freeze after the fight — plus the existing patch/restore, HP replay and legend-filter checks; garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **⚠ PROCESS NOTE WORTH KEEPING: a `g++ ... ; ./probe` shell line RAN A STALE BINARY when the compile failed and reported OK. The probe is now built with `if g++ ...; then` so a failed build can never be read as a pass. It went unnoticed for one edit cycle.** **⚠ `field_navigation.cpp` IS 81,587 BYTES — 333 FROM THE 81,920 HARD FAIL. SPLIT BEFORE THE NEXT EDIT.** **BAT: (1) the briefing should open AS THE MOVIE STARTS, before any damage — `[BGBTL-HP] armed` should read 600/600 for both. (2) THE GAME MUST WAIT: press nothing and you should hear "Press X to begin." every twelve seconds indefinitely, not the fight starting. (3) X resumes with "Game start." (4) The instructions must NOT come back mid-fight, only after a Game Over. (5) F10 skips straight to Galbadia Garden, from the fight or the Game Over screen. If X still does not register, `[BGBTL-KEY]` answers it outright. CAUTION: F10 is the Windows menu key — inert in a full-screen game with no menu bar, but if it does anything odd it moves.**

**Prior (v0.20.106 LOCAL, NOT MSVC-built — #minigame-bgbtl: PAUSE, BRIEF, CONFIRM).** Aaron after the `.105` BAT: *"it was really hard to hear the controls with everything going on -- the game battle sounds, the punching sounds of the mini-game, the audio descriptions, the instructions, and the block announcement. Would it be possible to basically pause the game when the scene starts to inform the player how to do this mini-game, and upon pressing X the confirm key in-game it resumes."* **FIVE THINGS WERE COMPETING FOR ONE PAIR OF EARS at the exact moment the player has to learn the controls. Speaking louder or shorter does not fix that; removing four of the five does.** **THE PAUSE IS THE ENGINE'S OWN PAUSE POINT, NOT AN INVENTION:** `field_main_loop` (0x0046FEE0) calls `field_main` (0x00471F70) once per frame at +0x148, and the disassembly shows it **SKIPS THAT CALL ENTIRELY** when FF8's own pause flag `[0x01CD2EBC]` is non-zero (`cmp eax,edi / jne 0x470033 / call 0x471f70`). **So not calling field_main is exactly what the game does when it pauses itself.** We do it with a **ONE-BYTE RET at the entry** — atomic on x86, no trampoline, one write to undo — and field_main takes no arguments (the call site pushes nothing), so a bare RET is a correct no-op. **Rendering, input and the frame limiter all live OUTSIDE it and keep running; only field logic stops — the script VM, the entity animation, the mini-game timer AND ITS PUNCH SOUNDS.** **VERIFIED BY SIGNATURE, NOT BY FAITH:** the five entry bytes are checked against `mov eax,[0x01CE4A64]` (`A1 64 4A CE 01`) before anything is written, and **on mismatch the briefing still speaks — it just does not pause**, and logs why. **THE OTHER FOUR NOISES:** the fight and its punch sounds stop with field_main; **FMV NARRATION IS SUPPRESSED** for the duration via a new `FmvAudioDesc::SetSuppressed` (cues still advance and LOG on schedule so the track stays in sync, they just do not reach the screen reader — an AD cue firing mid-briefing would interrupt it, **the same defect class `.103` fixed for our own cues**); and the music ducks by itself because the existing ducker follows `ScreenReader::IsSpeaking`. **THE FLOW:** legend → freeze → *"Garden battle. You fight the soldier hand to hand. W punches. A blocks. X kicks. I will say block when he attacks, and call out health as it changes. Press X to begin. Press F9 to hear this again."* → X → *"Game start."* → thaw. **F9 REPEATS THE BRIEFING** — there are no cues to toggle while frozen so the key is free, and a blind player who missed a word should not have to guess or restart the scene. **THE RESUME WAITS FOR THE KEY TO BE RELEASED, because X is also Kick** — thawing on the press would throw a kick the player did not ask for on the very next frame. A Game Over retry re-shows the legend, which re-opens the briefing. **THE FREEZE CANNOT STRAND THE GAME, AND THAT IS CHECKED RATHER THAN REASONED ABOUT** — a mod that leaves field_main patched has not inconvenienced the player, it has ended their session: **(1) 30-second cap inside the briefing; (2) thaw on EVERY Disarm path including the 5-minute arm cap; (3) a WATCHDOG called from `PollBattlePauseResume`, which sits ABOVE Update()'s `IsOnField()`/`HasFieldStateArrays()` early-returns — because `GardenBattle::Update()` owns the un-pause and lives BELOW them, so if either gate ever went false while frozen the cap would never run.** **KNOWN AND ACCEPTED: freezing field logic does NOT stop the FMV**, so the movie runs on during the briefing and phase one of the fight is correspondingly shorter. That costs the player nothing — they are listening, not fighting, and not being punched either — and the fight continues in bgbtl_1 regardless. Freeze duration is logged so the BAT can measure the drift. **Verified: minigame_bgbtl_compile 0 errors and it now MAPS REAL PAGES at 0x00471000 and 0x01CFE000 so the probe exercises the ACTUAL patch path — signature check, 0xC3 written, original byte restored — and asserts the byte is back on BOTH exits (player confirm, and disarm without confirming), alongside the HP replay and the legend filter; garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` IS 81,489 BYTES — 431 FROM THE 81,920 HARD FAIL (+85 for the FmvAudioDesc forward declaration). SPLIT BEFORE THE NEXT EDIT THAT NEEDS ROOM.** **BAT: (1) when the legend appears EVERYTHING SHOULD GO QUIET and the briefing plays alone — no punch sounds, no FMV narration, music ducked. (2) F9 repeats it. (3) X resumes, with "Game start." (4) Then the fight as in `.105` — Block cues and health. (5) IF ANYTHING GOES WRONG THE GAME MUST UN-PAUSE ON ITS OWN WITHIN ABOUT 30 SECONDS — that is the one failure mode worth knowing about, and it is deliberately bounded. Grep `[BGBTL] field PAUSED` / `field RESUMED`; the gap between them is how far the movie ran ahead.**

**Prior (v0.20.105 LOCAL, NOT MSVC-built — #minigame-bgbtl: HEALTH IS FOUND, ENTIRELY OFFLINE, NO BAT SPENT).** Aaron: *"go back to the exe and game files with what you've learned and see if you can find the health values in the mini-game so we don't have to keep doing BAT cycles to pin it down."* **Done. Four int16s in the field variable block: SQUALL max @ `0x01CFE9B8+350`, SQUALL current @ `+354`, FOE max @ `+352`, FOE current @ `+356`.** **THREE INDEPENDENT STEPS, AND THEY AGREE. (1) THE SCRIPT NAMES THE PAIR:** `bgbtl_1`'s `rinoa::gal_hpcalc0` reads damage byte **1028** — which `squall::squ_punched_up0` writes — and does `WRVARW 354`; its structural twin reads byte **1029** — which `gal0::g0_punched_up0` writes — and does `WRVARW 356`. **So 354 falls when SQUALL is hit and 356 when the SOLDIER is hit, and which is which is not a guess.** **(2) THE HOST FIELD INITIALISES ALL FOUR:** `bg2f_31`, pulled out of `field.fs` by byte range and decompressed here exactly as `bgbtl_1` was — `rinoa::m6  PSHM 600, RDVARW 380 -> WRVARW 350 ... -> WRVARW 354` and `rinoa::m11  PSHM 600 -> WRVARW 352 ; PSHM 600 -> WRVARW 356`. **Both start at 600, and SQUALL'S IS 600 ADJUSTED BY VAR 380 — that is the Game Over menu's "Try again with HP+200" — so MAX IS READ, NEVER ASSUMED.** **bg2f_31 ALSO CARRIES `stc0::squ_hpcalc0`, `stc0::gal_hpcalc0` AND `g_hei0::g0_punching0/kicking0/guarding0/punched_up0`, WHICH INDEPENDENTLY CONFIRMS `.104`'s FINDING THAT THE FIGHT RUNS IN THE HOST FIELD BEFORE bgbtl_1 LOADS** — that was inferred from two screenshots; now the host field's own script says it. **(3) THE EXE PINS THE ADDRESSING:** the RDVARW handler at **0x0051CBF0** is `movsx ecx, word ptr [eax + 0x1CFE9B8]` — **operand is a RAW BYTE OFFSET, no scaling, and the read is SIGN-EXTENDED** (RDVARB twin at 0x0051CBB0). **Signed matters: current HP goes NEGATIVE rather than clamping — the `.103` trace caught the soldier at −18 — so percentages clamp.** **AND IT ALL AGREES WITH THE TRACE WE ALREADY HAD:** across 4,096 watched bytes, **354 and 356 were the ONLY non-counter bytes that moved on damage**, every one of the twelve landed hits moved one of them, and they stopped dead when the hits stopped; at arm time the soldier read **400** and Squall **251**, both of 600 — exactly right for seventeen seconds of unwatched fighting before `.104` learned to arm early. **SO HEALTH SHIPS, in Aaron's own short form and on EVERY change rather than at thresholds**, because the measured rate makes it affordable — twelve damage events in 92 seconds, one every two to seven: `"You 75"`, `"You 60, Foe 45"` when both move in one poll, `"Foe down."` at zero. Percentages round to the nearest 5, and **REPLAYING THE REAL RECORDED SEQUENCES THROUGH THE POLICY** gives foe `65 60 50 45 35 30 25 10 5 0` and Squall `40 35 20 5` — **strictly falling, all distinct, so NO recorded change is lost to rounding and none repeats. That replay is now a test, not an argument.** **REMOVED: "Foe hit." / "You hurt."** (the number says who AND how much in one breath) **and the 4,096-byte change detector** (it did its job and its job is over). **"Foe down." now fires on foe HP reaching zero — a real measurement, unlike g0_fall0 which fires at scene setup.** **Verified: minigame_bgbtl_compile 0 errors and it now REPLAYS the recorded HP sequences and asserts the policy (0 bad) alongside the legend filter, plus −18 clamping to 0 and an unreadable max announcing nothing; garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **`field_navigation.cpp` UNCHANGED at 81,404 bytes — 516 from the hard fail; everything went into the .inl. THE SPLIT IS STILL OWED.** **BAT: (1) health spoken on every change — "You 60", "Foe 45" — in percentages of 600. (2) "Foe down." when the soldier reaches zero. (3) controls still arrive while the FMV plays (`.104`). (4) F9 toggles speech/tone, volume must NOT change. Grep `[BGBTL-HP]` — the `armed` line gives both fighters' raw HP and max at wake-up, and IF SQUALL'S MAX READS 800 AFTER A "Try again with HP+200" THAT CONFIRMS VAR 380 IS THE RETRY BONUS and closes the last open question in this arc.**

**Prior (v0.20.104 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE FIGHT STARTS 15 SECONDS BEFORE ITS FIELD LOADS).** **Aaron took two F11 screenshots at the exact moment he could not hear anything, and they overturn the `.103` diagnosis.** At **21:07:25 and 21:07:28 the mini-game is FULLY ON SCREEN** — legend box, both named health bars, **Squall's bar visibly lower in the second shot than the first** — and **FIELD 152 DOES NOT LOAD UNTIL 21:07:40.** The engine keeps reporting the PREVIOUS field for fifteen seconds while the fight plays over the FMV: `FMV started disc01_32h.avi` 21:07:16 → the game's own `"Punch Block Kick"` legend spoken 21:07:23 → both screenshots 21:07:25/28 → `MAPJUMPO destField=152` 21:07:38 → module armed 21:07:40. **Keying off the field id therefore missed the opening of EVERY fight**, which is exactly *"did not hear the controls or the damage announcements until several hits into the fight"*. **`.103` BLAMED THE FMV AUDIO DESCRIPTION AND WAS WRONG** — the AD genuinely did finish before the field loaded, that part holds, but **THE LATE SIGNAL WAS THE FIELD LOAD, NOT THE SPEECH. Aaron was right that the FMV was involved; the mechanism was not the one either of us named.** **THE TRIGGER IS NOW THE GAME'S OWN LEGEND:** `"Punch"`+`"Block"`+`"Kick"` in one decoded window, fed from `field_dialog`'s `show_dialog` hook **exactly like ChaseAskOverlay and TrainModeAskOverlay** — three strstr calls and out. That is the instant the mini-game UI appears, in whatever field hosts it. **THE HOST FIELD IS RECORDED, NOT ASSUMED** (144 this run) because the lesson of this whole arc is that **the field hosting the fight is not the field the fight is named after**; the field id survives only as a backstop. Disarm on the first field outside `{host, 152, 95}` plus a hard 5-minute cap — **arming on a string means the REQ hook is briefly live in an ordinary Garden field, a REAL WIDENING of the `.102` fence, accepted because that string appears nowhere else in the game and named here rather than buried.** **HP IS NOT IN THE FIELD VARIABLE BLOCK, SETTLED BY MEASUREMENT: the `.103` change detector watched all 4,096 bytes across a full 92-SECOND RUN — AND IT WAS A WIN, THE FIRST EVER RECORDED.** 129 bytes moved; the scans taken at the exact moment of each of the **TWELVE landed hits** show ONLY free-running counters (`soldier-hit 1 changed: [80]26->27`, every single time). Bytes 4/5/8/9/12/80/81/180/181/244/245/3380/3381 step ~20 per tick — frame counters. **Nothing in the block tracks damage.** Percentages need a different hunt (**the HP-bar draw call is the obvious next place**); the differ now filters the counters so the log stays readable. **THE WINNING RUN ALSO KILLED THE LAST BAD CUE: `gal0::g0_fall0` fired EXACTLY ONCE, at +297 ms, IN A RUN AARON WON** — it is never defeat, `.103`'s time gate was treating a symptom, and **"Soldier down." is gone**; the win is announced from the **observed** transition to field 675 instead (*"You win."*). Also measured: **`squ_punched_up0` never fired at all** (Aaron took no damage), and **the soldier attacks RARELY — 3 kicks and 1 punch in 92 seconds against 12 landed hits** — so the announcement load is far lighter than any model predicted, including the offline sim that started this design. **Verified: minigame_bgbtl_compile 0 errors and it now EXERCISES the legend filter (matches the legend, rejects ordinary dialogue) rather than only compiling it; garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **`field_navigation.cpp` UNCHANGED at 81,404 bytes — every line of this build went into the .inl — still 516 from the hard fail, THE SPLIT IS STILL OWED. field_dialog.cpp 16,405 and field_dialog_show_dialog.inl 11,442, both far inside the guard.** **BAT: (1) THE CONTROLS SHOULD NOW ARRIVE WHILE THE FMV IS STILL PLAYING, right after the game's own "Punch Block Kick" is read, not fifteen seconds later — that is the whole build. (2) No "Defeated." and no "Soldier down." at any point. (3) "You win." when the fight ends in your favour. (4) F9 still toggles speech/tone mid-fight and volume must NOT change. (5) On a retry from the Game Over screen the keys should be spoken again. Grep `[BGBTL] ARMED` — its timestamp against `[FmvSkip] FMV started` is the number that says whether this worked.**

**Prior (v0.20.103 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE TRACE CORRECTED ALMOST EVERY GUESS).** The `.102` instrument worked, and what it measured invalidates most of what `.102` announced — which is what an instrument-first build is for. **THE FMV WAS NOT THE PROBLEM.** Aaron: *"Not hearing the announcement of the keys or the block until the game is partway done. This may be because of the audio descriptions for the background fmv."* **The mod log says otherwise: `disc01_32h.avi` and its AD finished at 20:42:07, THREE SECONDS BEFORE the field loaded at 20:42:10, and the last AD cue spoke at 20:42:00.** No overlap; **no AD suppression is needed and none was added.** **THE MOD WAS TALKING OVER ITSELF**, and the four log lines say it exactly: `"Defeated."` (the label-47 bug) → `"Block"` (label 48, +328 ms) → `"Punch, W. Block, A. Kick, X."` (our 400 ms entry delay) → `"Block"` (label 49, +766 ms) **which cut the controls line off ~360 ms in — Aaron heard about "Punch, W."** (Separately: at 20:41:54, mid-FMV, the mod already spoke the game's own legend *"Punch Block Kick"*, buried between two AD cues — the information existed, sixteen seconds early and unlabelled.) **FOUR MEASURED CORRECTIONS. (1) `gal0::g0_fall0` (47) IS NOT DEFEAT** — it fires at **+125 ms in BOTH runs** as scene setup (the soldier drops into frame), so `.102` announced *"Defeated."* a tenth of a second into every attempt; now gated on elapsed > 4 s and logged as setup when it is not. **(2) THE REACTION WINDOW IS 700 ms, MEASURED, NOT GUESSED:** `squall::squ_timeover0` is the per-round expiry and lands a strikingly consistent interval after the soldier acts — **703, 703, 703, 562 ms** across the two runs. **That is the number the cue toggle existed to find.** A one-word cue only just fits inside it; a sentence does not. **(3) THE FIGHT IS ~10 s WITH EVENTS 1.2–1.7 s APART**, not the 900 ms exchange rate the offline announcer sim assumed — **there is far more speech budget than predicted**, which is why hit reports now ship as well. **(4) THE SOLDIER ACTS ON 48/49/50:** 48 `g0_punching0` fires once in the opening, **49 `g0_kicking0` is the recurring attack**, 50 `g0_guarding0` is HIM defending and gets no cue. **THE CONTROLS LINE IS NOW FIRST AND PROTECTED:** spoken at **0 ms** (was 400) as **"W punch. A block. X kick."** — key first, because the key is the part that must survive — and **every cue is suppressed until `ScreenReader::IsSpeaking()` goes false** (hard cap 3.5 s). Gating on real speech state rather than a guessed interval matters because Aaron's NVDA rate is far faster than any default; a fixed timer would waste most of the opening. **Every suppressed cue is logged, so the gate's cost is visible rather than invisible.** **HIT REPORTS SHIP, HEALTH STILL DOES NOT: "Foe hit." on `g0_punched_up0`, "You hurt." on `squ_punched_up0`** — which answers the actual complaint behind the health request, since **the game plays the SAME hit sound for both fighters** and so carries no information about who took the damage. A report will not interrupt a Block cue less than 450 ms old; only the cue is time-critical. **THE HP HUNT RESTARTS PROPERLY: `.102` dumped two guessed windows and BOTH WERE DEAD** — 1020..1040 zero all fight bar a transient 1 at 1031/1032, and 330..350 never moved at all. Replaced by a **CHANGE DETECTOR over 4,096 bytes** that reports only what moves and prints **one summary line of every byte that moved all fight** — HP cannot hide from it, and it stays quiet because almost nothing in there changes. **F7 WAS ALREADY TAKEN AND THE LOG PROVES IT:** one press produced `"Block cue: tone."` AND `"Music volume 30 percent"` (`GameAudio::VolumeDown`, dinput8.cpp). **Rebound to F9** — a full source sweep shows F1–F8, F11, F12 all bound and **only F9 and F10 free**; F9 chosen because F10 activates the window menu on Windows. **Also: the tone cue now runs `Beep()` ON A WORKER THREAD** — `Beep` is synchronous and was stalling the game thread 60 ms, a twelfth of the reaction window, in the one scene where that matters (`PlaySound` would be nicer but `deploy.bat` does not link `winmm.lib`). **STILL A GUESS, AND SAID SO: "Block" on 48/49, and label 47 as defeat once gated — BOTH BAT RUNS WERE LOSSES, so no winning trace exists yet.** **Verified: minigame_bgbtl_compile 0 errors (probe extended with CreateThread/CloseHandle/IsSpeaking stubs); garden_harness 26 ok / 0 bad; catalog_story_test 13 checks / 0 failures; aboard_test and world_map_harness pass.** **⚠ `field_navigation.cpp` IS 81,404 BYTES — 516 FROM THE 81,920 HARD FAIL (+19 for the IsSpeaking forward declaration). SPLIT BEFORE THE NEXT EDIT THAT NEEDS ROOM.** **BAT: same scene, log still matters more than the experience. (1) On entry, "W punch. A block. X kick." COMPLETE, before anything else speaks. (2) NO "Defeated." at the start. (3) "Block" when the soldier attacks; "Foe hit." / "You hurt." on damage. (4) F9 mid-fight swaps to the tone and volume must NOT change — with a 700 ms window the real question is which of the two Aaron can act on. (5) On a loss, F9 on the Game Over screen skips to Galbadia Garden (the game's own menu also offers "Try again with HP+200", and its hint says most HP when time runs out wins — the Skip is a last resort, not the only way through). Grep `[BGBTL]`, `[BGBTL-REQ]`, `[BGBTL-VARS]`; the line that unlocks health announcements is the single `moved during fight:` summary at the end.**

**Prior (v0.20.102 LOCAL, NOT MSVC-built — #minigame-bgbtl: THE GARDEN-BATTLE FIGHT, INSTRUMENT FIRST).** Aaron reached the punch/kick/block mini-game during the Garden battle and it is unplayable blind. **Field 152 `bgbtl_1`; Game Over is field 95 `testbl6`; winning MAPJUMP3s to field 675 `ggback1`.** **ENTIRELY OFFLINE ANALYSIS, NO BAT SPENT:** the field archive was pulled out of `field.fs` by byte range and decompressed here, and **`bgbtl_1.sym` names all 81 script methods** — every event Aaron asked for has a named script behind it (`squ_punching0/kicking0/guarding0`, `g0_punching0/kicking0/guarding0`, `squ/g0_punched_up0`, `g0_fall0`, `squ_hpcalc0`, `gal_hpcalc0`). The fight is driven by entity 12's `director0::talk` REQ-ing them, so **the whole event stream is ONE opcode: 0x014 REQ, operand = target entity, top of VM stack = label. CONFIRMED 18 OF 18** — every REQ in the driver resolves to a label owned by the entity it names. New offline tooling in `minigame/`: `jsmdis.py` (built from the mod's own parser so offline and runtime cannot drift), `SCRIPT_MAP.txt`, `optable.json`, `announcer.py`. **THE ANNOUNCER SIM EARNED ITS KEEP BEFORE A LINE OF C++: announcing every move interrupting, as originally specified, cuts off 97% of lines at a 900 ms exchange rate (34 of 35) — unusable, and known so before anything was built.** Aaron's revised policy — *"just announce 'block' when the player should block an incoming punch, and other than that we just announce health each time it changes ... keep it short like 'You 75, Foe 50'"* — measures at **11%**, and the single casualty is the controls line, **which is why it is now spoken ON ENTRY rather than at the first exchange.** **HOT-PATH SAFETY IS WHY THE HOOK IS FENCED:** `field_nav_mapjump_diag.inl` records that hooking SET3 *"is hot-path enough that ANY interception hangs the infirmary cutscene"*, and **REQ is far hotter than SET3 — it is how every script in every field is invoked.** So the hook is installed **ONLY while bgbtl_1 is loaded**, removed on any field change, and its body reads two words into a **256-slot lock-free ring and chains** — no logging, no allocation, no speech, no locks inside it; everything else happens on the mod's own tick, and the read is inside `__try` so a bad VM context drops one event rather than the game. **THE SKIP IS EXACT, NOT INVENTED:** `director0::talk` ends with `push 675, 1019, 3384, 0, 128` then `MAPJUMP3 77` — field 675 is `ggback1`, and **the `.101` BAT log confirms it independently** (`[fieldload] id=675 name='ggback1'` right after Aaron won). **F7 on the Game Over screen writes exactly that transition block (0x01CE4760)** — no invented story flag, no forced FMV jump; precedent is the Dollet Chase and Timber Train skips. **F7 during the fight swaps the Block cue between speech and a tone (`Beep(880,60)`), because the reaction window is NOT YET MEASURED and one visit to this scene is far too expensive to spend finding out** — both cues ship and Aaron switches in place. **HEALTH IS DELIBERATELY NOT ANNOUNCED YET: the script never reads or writes HP — it is engine-side — and announcing a guessed address is worse than announcing nothing.** This build **dumps the field variable block (0x01CFE9B8) on entry, on every `*_punched_up0`, and once a second**; the HP bytes are the ones that only ever fall, and fall exactly when a hit fires. **Build 1 traces, build 2 announces from measurement.** The same run settles the other unknown: **`g0_punching0` is not a single punch, it is the soldier's LOOP**, REQ-ing `g0_kicking0`/`g0_guarding0` from inside itself before branching back — so 48/49 is a **shipped guess** the trace corrects, and nothing else changes when it does. **CORRECTED ON THE RECORD: `field.fl` index is NOT the field id** (`bgbtl_1` is fl 123 / id 152), which invalidates an earlier claim comparing Galbadia Garden fl indices against trigger-table ids. **Also retracted, and worth keeping written: the mod's `field_archive_jsm_opnames.inl` is CORRECT** — the 394-entry table at `0x00B8DE4C` that appeared to contradict it is a **second-stage expression dispatcher reached through opcode 0x013**; the real field opcode table is `0x00B8DE94`, 18 entries later, verified on four opcodes against the mod's own BAT-resolved log. **Verified: NEW `tests/minigame_bgbtl_compile.cpp` 0 errors — no host harness compiles `field_navigation.cpp`, so this is the ONLY pre-MSVC syntax check the new module gets (the same gap that let the `.89` declaration-order bug reach a build); garden_harness 26 ok / 0 bad, catalog_story_test 13 checks / 0 failures, aboard_test and world_map_harness pass, all unchanged; include order 847 < 893 < 900 < 1260.** **⚠ `field_navigation.cpp` IS 81,385 BYTES — 535 FROM THE 81,920 HARD FAIL. SPLIT BEFORE THE NEXT EDIT THAT NEEDS ROOM.** **BAT — expensive to reach, so this run is designed to answer everything at once; play the fight normally and SEND `ff8_field.log`, the trace matters more than the experience this time. (1) On entry: "Punch, W. Block, A. Kick, X." (2) During: "Block" when the soldier attacks — if it fires at the wrong moments that is the expected outcome of a guess, and the log fixes it. (3) Press F7 once during the fight for the tone cue, and say which one you could actually react to. (4) "Defeated." when the soldier goes down. (5) If you lose, F7 on the Game Over screen should drop you into Galbadia Garden as though you had won. Grep `[BGBTL]`, `[BGBTL-REQ]`, `[BGBTL-VARS]`.**

**Prior (v0.20.101 LOCAL, NOT MSVC-built — #80: THE WHITE SEED SHIP HAS A WINDOW TOO, AND BOTH ITS EDGES ARE ALREADY-VALIDATED SIGNALS).** Aaron: *"the White SeeD Ship -- it only becomes available on Disc 3 and disappears from the World Map when the player receives the Ragnarok."* Until now it was offered **unconditionally**, so on discs 1 and 2 the mod would drive a blind player to a ship that is not there — the same defect class `.99` retired for the two Garden sites. **BOTH EDGES WERE ALREADY IN HAND: opening = disc ≥ 3 (savemap header +0x44, the same field the Galbadia window uses); closing = `ragnarok_pos` going live (WORLDMAP struct +0x18).** **THE CLOSING EDGE NEEDED NO NEW DISCOVERY** — `ragnarok_pos` is the exact analogue of `bgu_pos`, whose *zero while parked, live the moment you have it* behaviour is **proven across all 41 saves**, and Ragnarok is empty in every one of them, correctly, because it is a disc-3 vehicle Aaron does not have yet. **So unlike Mobile Galbadia Garden, NEITHER EDGE HERE IS A GUESS** — both are fields whose semantics were established by measurement earlier in this same arc. That is the return on having read the saves properly. `WmStoryRetired` gains one more name and remains the single predicate every builder asks (the `.88` rule). `[STORY]` now carries both windows on one line, verdict beside evidence: `... -> Mobile Galbadia Garden PRESENT | Ragnarok not held -> White SeeD Ship absent`. **Verified: catalog_story_test 13 checks / 0 failures (static/mobile/aboard + 4 Galbadia states + 5 White SeeD states covering BOTH edges); garden_harness 26 ok / 0 bad; aboard_test and world_map_harness pass; no berth, grid, route or planner change — this build only gates catalog membership.** **⚠ THE WHITE SEED SHIP DESTINATION HAS NEVER BEEN EXERCISED** — disc-3 content, Aaron is on disc 2 — so its coordinate (4887,51285) and berth (2944,53376) came from the original research document and have never been driven to. **DOES IT MOVE? NO** — Aaron: *"the White SeeD Ship is immobile for the time it is on the World Map"* — so a fixed marker is the right model and that question is closed. **WHAT IS NOT CLOSED IS WHETHER THE MARKER IS IN THE RIGHT PLACE:** (4887,51285) is terrain 7 grass, 479 above sea with water 58 cells inside 1,000u (right kind of place), but **texture page 128 with NO page-8 feature near it and NO wmsetus arrival record within 4,472 units** (the nearest is record 16, Centra Ruins' own). **That is the same profile as the two markers that turned out wrong this week** — Shumi's old coordinate and the page-8 candidate for Galbadia Garden. Not proof (the Galbadia trigger had this profile and was right), but it is unverified in exactly the way that has already cost eleven builds. **FIRST THING TO CHECK ON REACHING DISC 3.** **BAT: includes `.100`, which was never built — one build covers both. (1) Load slot2_save30: Mobile Galbadia Garden listed, driving to it starts the Garden battle. (2) Load the post-battle save (disc 3): absent, and `[STORY]` gives the post-battle values of all three candidate bytes. (3) IMMEDIATELY CHECKABLE ON ANY DISC-2 SAVE: the White SeeD Ship must now be ABSENT from the catalog.**

**Prior (v0.20.100 LOCAL, NOT MSVC-built — #80: MOBILE GALBADIA GARDEN, AND IT EXISTS FOR A WINDOW).** Aaron: *"That trigger is where Mobile Galbadia Garden is located on the World Map, so that is how it should be identified in the catalog. Also, once the battle of the Gardens is over, Galbadia Garden disappears from the world map forever."* **COORDINATE (−24982,65761)**, pinned from the `.99` BAT where auto-drive to Edea's House crossed it unasked: `[DRIVE] Manual field entry at (-24982,65761) -- nearest location Edea's House is 3437u away (> 3000), not capturing` + `[fieldload] id=253 name='bgsido_4'` / *B-Garden - Headmaster's Office 5* / Nida *"Squall, take a look."* **Driving onto it loads field 253, so ARRIVAL NEEDS NO SPECIAL HANDLING** — the drive already ends when a field loads. **drive_in like FH: park IS the marker, walk 0**, planner cell (127,414) WALK clearance 5, 16 gexec3 routes from four starts, **zero replans**. **NO MAP ARCHAEOLOGY COULD HAVE FOUND IT: terrain 7, texture page 128, nothing but more of the same for 1,000 units, nearest page-8 patch 5,249 units away. The page-8 method finds places the map DRAWS; a scripted event zone is a pure coordinate test with no geometry — and the pre-BAT candidate (−29632,70108) was 6,365 UNITS WRONG.** It was flagged as a lead not a conclusion, and it was wrong. Route crossing left alone on Aaron's instruction — it is a trigger the player wants. **THE GATE IS A TWO-SIDED WINDOW.** Closing edge given outright: *"by the time the battle of the Gardens is over, you are on disc 3"* — savemap header **+0x44, 0-indexed uint32**, disc-2 transition at slot2_save11 in the 41-save sweep; disc ≥ 3 hides it unconditionally. **OPENING EDGE IS A CANDIDATE, NARROWED NOT CONFIRMED:** bytes identical in ALL 40 saves before slot2_save30 and changed in slot2_save30 (the only save where it is present; every earlier one is either "at its original site" or "vanished") cut an 84-byte diff to **three fields — +0x0B94 (0000→01b7, append-only uint16 list), +0x0E8D (00→02, ISOLATED byte with static neighbours), +0x0FDC (00→03, churning region)**. **+0x0E8D ships** — the only one shaped like a state enum, zero across 100,000 s of play and stepping to 2 exactly when the Garden appears. **THE DISC RULE MAKES BEING WRONG CHEAP: a wrong byte can only show it at the wrong time WITHIN disc 2, never after.** All three are logged every catalog build (`[STORY] disc=.. gg[0x0E8D]=.. alt[0x0B94]=.. alt[0x0FDC]=.. -> PRESENT/absent`) — verdict and evidence on one line, the `.93` rule. **LATENT HARNESS BUG FOUND BY ADDING A BERTH: `garden_harness` iterated `s_gardenParks` with the hardcoded CATALOG count (39) instead of `GARDEN_PARK_COUNT`; the two happened to match, and a 26th berth would have silently skipped one.** **Verified: catalog_story_test 8 checks / 0 failures (static/mobile/aboard + all four window states); garden_harness 26 ok / 0 bad; aboard_test and world_map_harness pass; route regression NOT re-run because the only berth change is one drive_in row, which regress84 skips by construction exactly as it skips FH, and the other 24 rows are byte-identical.** **BAT NEEDS A SAVE RELOAD — the window has closed on the live playthrough. (1) Load slot2_save30: Mobile Galbadia Garden MUST be listed and driving to it should start the Garden battle. (2) Load the current post-battle save (disc 3): it MUST be absent, and the `[STORY]` line gives the post-battle values of all three candidates — THAT is the missing measurement, and it costs a save load.**

**Prior (v0.20.99 LOCAL, NOT MSVC-built — #80: THE CATALOG WAS NOT STORY-AWARE, AND THE SIGNAL WAS ALREADY IN THE FILE).** Aaron: *"The original location for Balamb Garden ... is no longer accessible since Garden is now mobile. This entry shouldn't be in the catalog anymore at this point in the game."* Same for Galbadia Garden, *"essentially vanished from the world map at this point."* **Both were still offered as fixed coordinates — choosing either would drive a blind player sixty kilometres to empty ground.** **NO NEW MACHINERY WAS NEEDED: `bgu_pos` (WORLDMAP +0x24), the same read that already builds "Mobile Balamb Garden", is ZERO while the Garden is parked and live the moment it moves — and that is now MEASURED ACROSS THE WHOLE PLAYTHROUGH.** **FF8 STEAM SAVES READ OFFLINE: an `.ff8` is a 4-byte length + LZS -> exactly 8,192 bytes, savemap at +0x184** (anchored on locID/HP/level/gil/save-count/played-time, cross-checked on two saves 16 min apart: save count 90->92, played 40,780->41,718 s). All **39 of Aaron's saves** read directly, no diagnostic build, no BAT: **bgu=(0,0) in slot1_save01 through slot2_save27 — 37 saves, every one empty — and (20271,−24355) in slot2_save28/29.** That coordinate is the exact `GARDEN_START` the berth generator has used since `.84`, a free second confirmation. Galbadia rides the same signal on Aaron's testimony that *"G-Garden is already mobile by the time B-Garden becomes mobile"*, so no window exists where the Garden is mobile and the Galbadia site is occupied. **ALSO SETTLED, NEGATIVELY AND CHEAPLY: the four unexamined WORLDMAP slots (+0x0C/+0x3C/+0x48/+0x54) are EMPTY IN ALL 39 SAVES.** Galbadia Garden has no position record there; the tidy "one slot is both flag and coordinate" hypothesis **died before a line of code was written for it**, and the Edea's House reappearance will need a coordinate AND a flag of its own. The struct is now pinned as eight 12-byte slots, 0x00–0x5F (+0x60 is not a position record). **ONE PREDICATE, `WmStoryRetired`, ASKED BY EVERY BUILDER** — the catalog assembles in three places and the ABOARD path rebuilds from `s_locations` rather than the compacted `s_catalog`, so it asks a second time. **That is the exact shape of the `.88` bug (a condition armed in one place, asked about in three, which silently vanished Shumi from the catalog it had just been added to)**, so aboard gets its own check rather than being assumed to follow. **HARNESS GAP WORTH NAMING: NO HOST HARNESS COMPILED `world_catalog.inl` AT ALL** — `catalog_harness.cpp` is the FIELD catalog, and the world catalog has only ever been compiled by MSVC, which is the same gap that let the `.89` declaration-order bug reach a build. **NEW `tests/catalog_story_test.cpp`** compiles the real file and checks all three states: `STATIC 39, both present` / `MOBILE 38, both gone` / `Mobile Balamb Garden present` / `ABOARD 37, both gone` — **4 checks, 0 failures** (38 not 37 because the mobile branch also APPENDS the ride you can actually reach). **Verified: world_map_harness PASS; garden_harness and aboard_test untouched and passing — this build touches no grid bit, no berth, no route; all files inside the 80 KB guard.** **BAT: stand on the world map and open the catalog — BALAMB GARDEN AND GALBADIA GARDEN MUST BOTH BE GONE, with "Mobile Balamb Garden" still there pointing at where you parked. Then board and check again — same two absent. Grep `[STORY]`.**

**Prior (v0.20.98 — BAT'd AND CONFIRMED. #80's SHUMI ARC IS CLOSED, eleven builds after it opened).** Aaron: *"Finally made it to Shumi Village!"* **`[DRIVE] Arrival via game-mode (mode=1 MODE_FIELD, fieldId=0x03A5, fieldName='tmdome1', target=Shumi Village, dist=171, lastPos=(12774,-83801), elapsed=563ms)`. THE GAME NAMED IT.** Garden parked (13371,−81750) `parkBit=1 canDisembark=1` walk 2,307 **0 replans**; 67 s for the Garden leg, 3 s for the walk. **AND THE TRIGGER GEOMETRY CLOSES THE `.91`/`.92` LOOP EXACTLY: the field fired at (12774,−83801), 524 UNITS FROM THE DOME CENTRE, while record 20 is 726 out. The entry trigger lives between those two numbers** — which is precisely why standing on the arrival point for fifty seconds did nothing and why crossing the apron does. The arrival point is outside the trigger BY DESIGN; the apron is inside it. **Correction for the record: Shumi's fields start at 933 (0x3A5 `tmdome1`), not 934.** **STILL OPEN, same class, none urgent:** `Chocobo Forest 3` (51893,−3959) has no page-8 patch within 3,936u while an unclaimed 32-cell terrain-1 forest sits at **(51968,−64256)** with arrival record 35 (52145,−63615) — the walkthroughs' *"North of Trabia Garden"* forest; pointing CF3 at that record gives berth (53632,−61568), walk 2,485, corridor clear. `Chocobo Forest 4` (97253,−48250) sits by a 47-cell terrain-2 patch that is not the forest signature. `Deling City`'s marker is not foot-walkable and gets snapped 128u — Shumi's defect, two orders of magnitude smaller, and working today.

**How it was fixed (v0.20.98 — #80: THE MARKER WAS THE BUILDING; IT HAD TO BE THE DOOR).** `.97` BAT, Aaron: *"Garden landed on the land mass and said it was close by, but when I got off the Garden and tried to use auto-drive to walk to the Shumi Village it seemed to run into a wall, and ended up going into the Chocobo Forest nearby while trying to correct."* **HALF OF `.97` IS SETTLED: the Garden crossed the world, landed on Winter Island and parked at (12952,−81477), `parkBit=1 canDisembark=1`, walk 2,572, ZERO REPLANS — the first time any build has put the hull within walking distance of Shumi. (b) IS SOLVED.** The walk is not. **THE WEDGE, in the log's own numbers:** `final approach zone (dist=994)` then `Hard wedge` at **778**, and every un-wedge burst pushes it OUT — 778 → 1,305 → 1,901 → 2,478 — until it drifts south-west and trips the Chocobo Forest trigger at (10729,−80800). The frames before the wedge name the wall: the walker is at **(12137,−83179) → (11962,−83245), moving WEST while the bearing to target reads 139**, on tri #503, ground rising −977 → −1013 as it slides. That is a hull pressed against a face. **CAUSE: the goal was not walkable ground at all — (12274,−83958) is the CENTRE OF THE BUILDING FOOTPRINT**, so the planner snapped to whatever was nearest and its last waypoint (11712,−83392) sat 798 units short on the blind south-west side. **THE DOME HAS ONE DOOR AND IT IS ON THE EAST.** Flood the foot mask from the Garden's own step-off with the ENGINE's gate — the navmesh logs it at load, `66900 engine-gate-blocked connections (>=200 over ~190u)` — and exactly **FIVE cells of the structure come back reachable: (12736, −83776/−83904/−83968/−84032/−84288), all height −878, terrain 9, flat snow.** One 640-unit strip on the east face; everything else around the dome clears the gate and is wall. **And wmsetus record 20 (13000,−83977) sits 264 units due east of that strip's middle — you step out of Shumi, cross the apron, and you are on the world map. THE APRON IS THE ENTRANCE.** Destination is now **(12736,−83968)**. **THE BERTH MOVES TOO, for the reason `.96` already taught:** (12672,−81536) parks fine but sits WEST of the dome, so the walk ran at the blind face — the walking version of the beach, same destination, open from one side and sealed from the other. New berth **(13184,−81536), walk 2,472**, 512 units east of the one that already parked cleanly. **TWO NEW GENERATOR CONSTRAINTS, AND ONLY ONE GETS A VOTE. (5) THE MARKER MUST BE FOOT-WALKABLE UNDER THE ENGINE GATE — real, and it IS the `.97` defect:** `gen_berths.py` has always snapped an unwalkable marker to the nearest walkable cell SILENTLY, which is exactly how a building footprint became a destination; it now prints every time it fires (it also fires on Deling City, 128 units — worth a look later). **(6) CLEAR FINAL-APPROACH CORRIDOR — ADVISORY ONLY.** As a scoring rule it reshuffled seven berths, and audited against the SHIPPED table it flags **Balamb Town, Timber and Chocobo Forest 2 — all of which Aaron has arrived at fine**, because a normal town's trigger fires well before you touch the building. So it is reported, never scored, and Shumi's berth was picked with it BY HAND because here the BAT is the evidence that the approach side matters. **This is deliberately the opposite of `.90`–`.95`: a model that disagrees with a BAT does not get to steer.** **Verified: parity clean; 24 of 39 berths and the other 23 UNCHANGED from `.97`; 25 ok / 0 bad; `Shumi plan from Balamb: OK (381 waypoints)`; `Shumi berth to Chocobo Forest 2 berth: 1055 units OK (same shore)`; aboard_test passes.** **BAT: drive to Shumi Village (the Garden leg is known good — expect a park near (13184,−81536) and "26 hundred units north"), step off, walk. The approach now comes up the EAST side. Either the village loads, or the drive announces arrival STANDING ON THE APRON at (12736,−83968) — and the doorstep is a far better place to be stuck than 778 units out on the back wall.**

**Prior (v0.20.97 LOCAL, NOT MSVC-built — #80: SHUMI VILLAGE WAS NEVER WHERE THE CATALOG SAID IT WAS).** Aaron, after nine builds: *"Go back to the game files and exe … Keep going until you have pinned down a definitive answer on (a) where Shumi is located on the world map and (b) how to reach it using the Garden."* **He was right to push. THE COORDINATE WAS WRONG, and every build from `.88` on was engineering a cliff climb to reach a place that is not there.** **(a) THE WORLD MAP LABELS ITS OWN SETTLEMENTS: byte 14 of a `wmx` polygon is the texture page, and every named place sits on a patch of PAGE 8.** `wmsetus` section 8 is a 62-record table of `int32 X, int32 Z, int16 height, int16 facing` — the spot the game stands you at when you leave a field — and the decode is confirmed by its heights matching the walkmesh under them (record 0: −658 vs −652; record 1: −304 vs −304). **Line the two up and the arrival record is ALWAYS 600–1,300 units OUTSIDE the page-8 patch** — Balamb Town 1,102, Timber 993, Deling City 1,280, Winhill 809, Dollet 1,072, Trabia Garden 1,221 — because if it were inside the entry trigger you would re-enter the town the instant you left it. **Winter Island has exactly TWO page-8 patches: 32 cells of terrain 1 at (10752,−80384), and 71 cells of terrain 29 at (12274,−83958).** 32 cells of page 8 is the **Chocobo Forest signature** (seven such patches map-wide); the 71-cell patch is town-sized (Winhill 68, Balamb Town 135, Trabia Garden 184) and is the only other structure on the island. **SHUMI VILLAGE IS AT (12274,−83958).** The shipped marker (10362,−76967) was **7,248 units north-west, on no feature at all, on a DIFFERENT foot landmass** — the 7,034-cell cliff plateau `.88`–`.96` kept measuring. **THE `.92` CONCLUSION WAS WRONG AND THE NULL RESULT IS WHY: record 20 (13000,−83977), where Aaron stood for fifty seconds with nothing loading, is 725 units due EAST of the dome — exactly where the game stands you when you walk OUT of Shumi. He was outside the door.** *A null result at one point is evidence about that point, not about the hypothesis* — and the offset that explains it was in the same table the point came from. **(b) ORDINARILY. THERE IS NO BEACH TO CLIMB.** Berth **(12672,−81536), walk 2,435, clear 6**, on the **same 6,216-cell landmass the Garden already parks on for Chocobo Forest 2, 1,280 units from that berth** — ground Aaron has already driven to. **16/16 `gexec3` routes arrive, zero replans, no beach exception, no approach point. NO `beach_climb` BERTH REMAINS IN THE TABLE** (the machinery stays in source, inert; the harness prints the count so that stays visible). **BYPRODUCT, NOT CHANGED THIS BUILD:** the seventh 32-cell patch, **(51968,−64256)**, has no catalog entry and matches the walkthroughs' *"North of Trabia Garden"* forest, while `Chocobo Forest 3` (51893,−3959) has no page-8 patch within 3,936 units — **two more wrong markers of exactly the class just fixed, flagged for Aaron rather than swept into this commit.** **Verified: parity clean incl. PARTIAL/CLEAR; 25 ok / 0 bad; `beach_climb berths in the table: 0`; `Shumi plan from Balamb: OK (381 waypoints)`; `Shumi berth to Chocobo Forest 2 berth: 1280 units OK (same shore)`; aboard_test passes; regress84 runs 24 berths with NOTHING SKIPPED.** **BAT: board, confirm the catalog still says 25 with Shumi in it, and drive to Shumi Village. Expect an ORDINARY arrival — `parkBit=1 canDisembark=1`, ~2.4 km walk — and NO `[GDBEACH]` lines at all. Then step off and take the foot drive in: the village should load on the approach from the east, before you reach the dome.**

**Prior (v0.20.96 LOCAL, NOT MSVC-built — #80: THE APPROACH SIDE WAS THE WHOLE PROBLEM, AND THE SCREENSHOTS SHOW IT).** Aaron drove the hull in by hand and photographed it: **Balamb Garden sitting on the snow in a cove with a cliff wall behind it**, and his logged position there was **(3150,-81780) = planner cell (703,524), reachable water, 338 UNITS FROM THE BERTH.** The automated survey, running straight at the berth with every check off, sticks at **(2294,-82228) = cell (705,521), 572 units out on the SOUTH-WEST side.** **Same berth, two sides, one of them open.** The cell map says why: the wedge (704..707, 522..524) is **WALK land the flood cannot reach, ringed ENTIRELY by PARTIAL cells**, and the ONE mouth from reachable water is **(704,524) entered from (703,524) to its north** — exactly where Aaron ended up. **Everything the planner did approached from the wrong quarter, which is why `.90` through `.95` kept measuring a wall: IT IS ONLY A WALL FROM ONE DIRECTION.** Four builds of survey iteration were measuring the closed side. **FIX: `dock_x/dock_y` — unused by anything but a drive_in until now — carry the APPROACH POINT for a beach_climb berth.** `Garden_Plan` routes to **(3200,-81792)** instead of the berth; the beach push arms on proximity to THAT (`GD_BEACH_ARM_DIST` 500) rather than raw distance to the berth, so it starts from the open side with **362 units to cover instead of 572 through a closed one.** **HONEST STATUS: each fix in `.90`/`.93`/`.94`/`.95` was a real defect — bearing from the planner cursor, full throttle, the one-cell grid wall — but none was going to work while the hull kept arriving on the sealed side. THE THING THAT BROKE THE LOOP WAS A PHOTOGRAPH, and it took ten seconds to read. WHEN A SPATIAL QUESTION RESISTS FOUR ROUNDS OF INSTRUMENTATION, ASK FOR A PICTURE.** Also: `offline/regress84.py` now **SKIPS beach_climb berths and says so out loud** — `gexec3` has no `GdBeachOpen` and no approach-point routing, so such a berth is unrepresentable offline and a silent drop would read as "everything passes". **Verified: 25 ok / 0 bad; Shumi plan OK (360 waypoints); aboard_test passes; parity clean including PARTIAL and CLEAR.** **BAT: Shumi Village. Expect the route to end at (3200,-81792) on the NORTH side, then `[GDBEACH]` from there — 362 units to cover, not 572.**

**Prior (v0.20.95 LOCAL, NOT MSVC-built — #80: THE WHOLE SHUMI BEACH WAS SHUT BY ONE CELL, AND IT WAS MY GRID SHUTTING IT).** `.94` measured cleanly at last: three runs all closing **1963 -> 1194 -> 936 -> 678** and then **STICKING at 670-690 for ten seconds**, `berthOff` repeatedly brought to **11, 16, 43** — pointed straight at the berth, throttle down, **sliding sideways**, `gate=208` and `cls=0x31` throughout. Closest approach **670** (was 1,029 in `.93`, so the throttle fix worked). That is the signature of a wall — **and the wall is in my grid.** The stuck position sits on planner cell **(705,521)**: `sub(1410,1042) terr=29 h=-7 NOT Garden-masked`, the other three quarters terrain 9 Garden ground at -355..-380. A 256 cell is WALK only when ALL FOUR sub-points carry the bit, so it reads solid — **and THE ENGINE HAD THE HULL STANDING ON IT.** Everything past it — (705,522), (705,523), the berth (704,523) — is fully WALK terrain-9 at -390..-470. **The only beach onto that island was shut by ONE cell whose fourth quarter is a 128-unit sliver of shore skirt**, and `GdLineClear` refuses every chord through a non-WALK cell, so the executor would not drive the last 670 units of a ramp it was already standing on. **AARON'S MANUAL DRIVING IS WHAT PROVED IT: of 718 logged hull positions in that region, 190 SIT ON CELLS THE MODEL CALLS NON-WALK** — he had been driving across "solid rock" for minutes. **FIX, deliberately tiny: new `GDC_PARTIAL` (0x80)** = 1..3 of 4 sub-points Garden-masked, **6,939 cells map-wide**; `GdBeachOpen(idx)` lets a step or a chord cross one **ONLY when a beach_climb berth is armed and ONLY within 3 cells of it.** Open water, solid rock and every other berth are untouched — the conservative all-four rule still governs everywhere the planner plans. Mirrored in `gsim3` as `PARTIALp`, guarded by `parity_check` (6,939 both sides). **Fourth iteration on this survey and the FIRST where the obstacle was real rather than my steering. Whether the Garden climbs the ramp once the mouth is open is STILL UNMEASURED — but the model is no longer standing in the way of finding out.** **Verified: parity clean including PARTIAL and CLEAR; 25 ok / 0 bad; Shumi plan OK (360 waypoints); aboard_test passes.** **BAT: Shumi Village. Expect the approach to carry PAST 670 units this time — then CLIMBED, or an honest "did not get there" with a closest approach well inside it.**

**Prior (v0.20.94 LOCAL, NOT MSVC-built — #80: THE BEACH RUN WAS ORBITING, AND THE TURNING CIRCLE IS WHY).** `.93` steered at the berth correctly and still could not converge. **The trace is an orbit and says so twice: berthOff starts at 53 and 182 — pointed STRAIGHT at it — the hull closes to 1,194 and 1,035, then berthOff blows out to 932, 1350, 1754, and BOTH RUNS settle into the same wandering loop and end at the IDENTICAL coordinate (1791,-80541).** Two independent runs converging on one attractor is an orbit, not an obstruction. **The geometry was already written down in this very file: the Garden turns at most 9 units of heading per frame and cruises at 32, so its turning circle is ~1,300 units — driving flat out at a target 1,000-2,000 units away means the circle is WIDER THAN THE APPROACH and it can only spiral past.** Throttle is now gated on the same forward cone the normal executor uses, with the `.86` pivot escape still applying since a stationary hull cannot always turn; **the probes and the wall-follow stay OFF**, which is the whole point of the run. **THREE STEERING ITERATIONS, THREE REAL DEFECTS, none of them the engine:** `.90` took its bearing from the PLANNER CURSOR not the berth; `.93` fixed the bearing but kept FULL THROTTLE and spiralled; `.94` disciplines the throttle. **`mv` has been high in every single run (189/899 on the last), so nothing observed so far is a refusal. Closest approach to date: 1,029 units.** **SPLIT: `world_garden.inl` hit 80,357 bytes, 1.5 KB from the 81,920 hard fail, with this survey still iterating** — per DEVNOTES, split BEFORE the edit that needs the room: the collision probes move to **`world_garden_probe.inl`** (4.4 KB), they read only the grid and depend on no executor state, nothing else moves, `world_garden.inl` back to 76,769. **Verified: 25 ok / 0 bad; Shumi beach plan OK (360 waypoints); aboard_test passes; parity unchanged.** **BAT: Shumi Village again. Expect berthOff to STAY small and goal to fall monotonically, then CLIMBED or an honest "did not get there" with a closest approach well inside 1,029.**

**Prior (v0.20.93 LOCAL, NOT MSVC-built — #80: THE `.92` BEACH RUN SAID "REFUSED" AND WAS NOT ENTITLED TO).** The survey fired twice at Shumi and reported REFUSED both times. **The trace refutes that in its first column: `mv=23/45, 46/128, 78/294, 92/351, 110/489, 140/547` — the hull MOVED on roughly half of every frame for the whole nine seconds, so the engine refused NOTHING** — while `hd` swung 2071->1337->2261->2541->1329->865 and the distance went **1970 -> 1286 -> 591 -> 570 -> 911 -> 2277.** It closed to **591 UNITS** of the berth and then drove away from it. **CAUSE, mine: the beach-run block reused `off`/`err`, which are measured against `steerX/steerY` — THE PLANNER CURSOR — so "drive straight at it" drove straight at whatever waypoint the cursor held while the cursor kept advancing and replanning underneath it** (three `OFF-GRID` recoveries in the last four seconds). Now computes the bearing to `s_gdTargetX/Y`, the berth itself, logs `berthOff` and the closest approach, and the window goes 9,000 -> 14,000 ms. **THE WORDING IS FIXED TOO AND THAT MATTERS MORE: "That is the engine's answer, not the model's" was about to write a FALSE FACT into the record.** A moving hull is not a refusal. It now says "did not get there", with the closest approach, and states that the hull moved throughout. **This is the same error as reading the Lunar Cry cutscene as an engine block — an instrument printing a confident verdict its own data does not support. Twice in one week: WHEN A DIAGNOSTIC PRINTS A VERDICT, THE VERDICT MUST BE ENTAILED BY THE NUMBERS BESIDE IT.** What the data DOES say: the hull reaches 591 units of the beach berth on water with `gate=208` throughout, never shallow, and nothing in two runs looks like a refusal. **Whether the Garden can climb that shore is STILL UNANSWERED; this is the first build whose survey can actually ask.** **Verified: 25 ok / 0 bad; aboard_test passes.** **BAT: Shumi Village again. Expect `[GDBEACH]` lines with `berthOff` small and the distance falling monotonically, then CLIMBED or "did not get there" with a closest approach.** ⚠️ `world_garden.inl` is **78,892 bytes — 3 KB from the 80 KB hard fail. Split before the next edit that needs room.**

**Prior (v0.20.92 LOCAL, NOT MSVC-built — #80: THE SHUMI COORDINATE QUESTION IS CLOSED, AND OUR COORDINATE IS RIGHT).** Aaron drove to Chocobo Forest 2, stepped off, and walked the `.91` probe point. **The foot drive reached `pos=(13000,-83984)` — SEVEN UNITS from wmsetus location record 20, the only unclaimed world-map location record in the Shumi island group — and stood there for fifty seconds. NOTHING LOADED:** no field transition, no `ChaseDetector: fieldId changed`, no dialog. **Record 20 is not Shumi Village.** So the catalog coordinate **(10362,-76967) STANDS**: Shumi Village is on the 7,034-cell island that carries the **53-polygon Garden beach**, and that island simply has no world-map location record — the record-bearing island next door is Chocobo Forest 2's. **That also removes the last doubt about the beach: the only Garden-landable shore in the area is on the same island as the marker, which is what it should be.** **THE FILE-SIDE ROUTE WAS CORRECT AND PRODUCED NOTHING, WHICH IS WORTH RECORDING.** `locID` in the trigger programs IS a field index (validated on Winhill 652->643-674, Centra Ruins 279->276-286, Dollet 327->307-345, Esthar 407-466->402-487) and **Shumi's fields are 934-945 while the table's highest locID is 705** — the trigger table is a SPECIAL-CASES table for vehicle/story-gated entries and Balamb Town, Timber and Trabia Garden are absent from it too. The field archive opened cleanly (`tmsand1.fs` and `tmgate1.fs` pulled by byte range with `dd` on the user's machine, LZS-decompressed to exact sizes, 17-file sub-archive unpacked including the `.jsm` and the `.inf` gateways referencing Shumi fields 934/936) — **but a field's gateway to the world map carries no world coordinates.** `tmsand1` = "sand", the beach field, which matches the walkthrough. **METHOD NOTE: one walk answered in fifty seconds what two hours of file archaeology could not. When the question is "what does the game do at this coordinate", the game is the cheapest instrument, and the on-foot auto-drive turns an unreachable-by-sight test into a routine one.** Probe entry removed. **ONE REAL BUG IT EXPOSED: a destination with NO FIELD TRIGGER has NO ARRIVAL CONDITION** — the drive jammed at 7 units and then said "Cannot reach the destination from here". Only diagnostic destinations can hit it, since every real one ends when its field loads; expect it if another waypoint is ever added. **Verified: 25 ok / 0 bad; beach_climb semantics OK; aboard_test passes.** **BAT: the `.90` `[GDBEACH]` run, which has still never been exercised — drive to Shumi Village and let it reach the beach.** Expect the approach, then `[GDBEACH]` lines once a second, then **CLIMBED** or **REFUSED** — no orbiting either way. Grep `[GDBEACH]`.

**Prior (v0.20.90 LOCAL, NOT MSVC-built — #80: THE .89 SHUMI BAT SAID TWO THINGS, AND ONE OF THEM WAS MY MESSAGE LYING).** Aaron: *"Drove to Shumi and the mod said I was 1km out, but I could clearly hear the Garden was still in the ocean. This leads me to think our coordinates for Shumi may be incorrect."* **(1) THE MESSAGE WAS WRONG, NOT (necessarily) THE COORDINATE.** `wall-follow drift at (4203,-82788) but only 1460 units from Shumi Village` prints **`goalDist` — the distance to the BERTH — next to the name of the DESTINATION.** Everywhere else those are within 3 km and nobody notices; at Shumi the berth is a beach **12 km from the marker**, so it announced "1,460 units from Shumi Village" with the hull at sea ten kilometres out. Now reads "from the X berth (marker is N away)". **I checked the coordinate hypothesis properly rather than dismissing it as in `.65`:** our marker (10362,-76967) is terrain 17, foot-walkable, **on the village's own foot landmass**; nearest wmsetus record is Chocobo Forest 2's at **4,082 units**; and there IS an **unclaimed record 20 at (13000,-83977)** on that island — **but on a DIFFERENT foot landmass from our marker**, so swapping would move the destination off the ground it belongs to. **UNRESOLVED. Settle it the way FH was settled: walk in on foot and read the `[DRIVE] Arrival via game-mode` line.** **(2) THE BEACH — one number explains the two-minute orbit: `gate=208` on EVERY sample, so the hull never left the water, with `aim=0` throughout.** `beach_climb` opened the GOAL CELL to the planner (360 waypoints) but the EXECUTOR still has to see across the unmasked terrain-29 skirt; `GdLineClear` refuses every chord, the wall-follow engaged and never released, 1-2.6 km loops through four replans. **Opening one cell was never enough — THE CROSSING is the thing and the model does not have it.** So `.90` stops arguing with the model and **asks the engine**: inside `GD_BEACH_RUN_DIST` (2000) of a beach_climb berth the executor drives **STRAIGHT AT IT** with probes and wall-follow suppressed for up to 9 s, logging `[GDBEACH] mv/gate/cls` once a second and ending in **`CLIMBED`** or **`REFUSED`**. Either is the first real data anyone has for this shore. **Verified: 25 ok / 0 bad; `beach_climb semantics: raw=0 berth=1`; Shumi plan OK (360 waypoints); aboard_test passes; parity unchanged.** **BAT: drive to Shumi Village and let it run to the end.** Expect the approach, then `[GDBEACH]` lines once a second, then CLIMBED or REFUSED — **no orbiting either way.** Grep `[GDBEACH]`.

**Prior (v0.20.89 LOCAL, NOT MSVC-built — #80: THE BEACH EXCEPTION WAS ARMED IN ONE PLACE AND ASKED ABOUT IN THREE).** `.88` BAT, Aaron: *"Did a quick test just now and Shumi did not appear in the catalog."* **The log says it outright: `catalog: 24 destinations the Garden can reach, 15 hidden` -- 24, not 25 -- and no `beach climb armed` line anywhere**, because `.88` set `s_gdBeachGoalIdx` in `Garden_StartDrive` and NOWHERE ELSE. `Garden_BuildCatalog` runs the moment you board, long before a destination is chosen, and it called `Garden_CellReachable` directly; **with the exception clear a `beach_climb` berth is unreachable BY CONSTRUCTION -- that is the whole point of it -- so the catalog hid the destination it had just been given.** The announce path had the same hole. **The exception is a property of the BERTH, not a mode the mod is in.** `Garden_BerthReachable(gp)` arms, asks and restores; the catalog, the announce and the drive all go through it, and the raw call is gone from every berth path so a caller cannot get it wrong by forgetting. **Regression guard in `garden_harness` pins the semantics outright -- `beach_climb semantics: raw=0 berth=1` -- so a future call site that asks the raw question fails a check instead of silently vanishing a destination.** **FIRST ATTEMPT DID NOT BUILD** — the forward declaration went next to `Garden_CellReachable`, which sits ABOVE the `GardenPark` struct it names (MSVC `C4430`/`C2143` at `world_map_state.inl(1025)`, two `C2664`s downstream); it is now below the struct beside `Garden_ParkFor`. **NOTE FOR NEXT TIME: `g++` passed both before AND after** — each harness defines its own `GardenPark` before including the `.inl` files, so a declaration-order error inside `world_map_state.inl` is structurally invisible to them. **The host harnesses check BEHAVIOUR; only MSVC checks the real include order.** **Verified: 25 ok / 0 bad; `Shumi beach plan from Balamb: OK (360 waypoints)`; aboard_test passes; parity unchanged (this build touches no grid bit).** **BAT: board and count the catalog -- it must say 25, and Shumi Village must be in the list. Then drive it: that is still the survey.** If it climbs, expect a park on the beach and "12.3 kilometres north -- a long walk"; if the engine refuses, expect the hull at the water's edge below (2944,-82048) with `[GDTRACE]` running. Also still worth one Winhill -> Trabia leg for the `.88` cutscene pause (`[GARDEN] cutscene at` / `cutscene over`, no reverse, no replan).

**Prior (v0.20.88 LOCAL, NOT MSVC-built — #80: THE TRABIA "WEDGE" WAS A CUTSCENE, AND SHUMI VILLAGE IS RE-OPENED AS A BEACH CLIMB).** **(1) IT WAS NEVER A WEDGE.** Three BATs froze within twenty units of (60993,-44513), and `.86`'s refusal sweep read like a hard engine block: **thirteen consecutive samples over nine seconds, every one `hd=3636`, the heading never moving.** The sweep is built to rotate through sixteen headings and could not rotate at all — **I was one edit from shipping a "pinned hull" heuristic built on that.** Aaron: *"There is a spot on the world map that causes the Garden to go haywire temporarily. Nida even comments on it when you hit that spot. It is part of the game's lore as that spot is where a Lunar Cry happened in the past."* **The dialog log has it one second before the hull stopped:** `[00:12:56] [SHOW_DIALOG-TEXT] win[0] text=""Huh!?""`, then `[00:13:12] Nida "The gauge is going berserk!?"`, **window open across the whole fifteen seconds.** The engine ignores movement input while a field-dialog window is up — exactly `mv=0/57` with a frozen heading. **The FIELD drive has suspended key injection during dialog since `v05.37`; the GARDEN drive never got it.** Same predicate now, keys released, stall/throttle/route-progress/probe/announce all held at the current instant so nothing fires on time that was never ours (`[GARDEN] cutscene at` / `cutscene over at`). The `.86` pivot escape STAYS (it fired legitimately at `(-16825,-1627) off=1539` and the Winhill drive continued); the pinned-hull heuristic is DROPPED. **(2) SHUMI VILLAGE IS BACK, BERTHED ON THE BEACH.** Aaron: *"You go up the beach like any other... get as close as possible... If it runs into walls so be it, but we can at least use that data to survey."* Berth `(2944,-82048)`, inside the **53-polygon terrain-9 wedge (`b15=0xF7`) that is the ONLY Garden-masked ground among the island's 7,034 foot cells** — confirmed at polygon level, not just on the 128 grid: re-rasterising all 473,193 polygons with an ANY-covering-polygon rule gains 208 sub-points map-wide and **none on this island**, so 53 is the real number and the Garden lands and you walk. **The walk is 12,288 units** (village north shore, beach south), which is why every generator rejected it — **the 6,000 `WALK_CAP` was wrong, not the berth.** The shore reads a **295-unit step against the 200 gate** because an unmasked terrain-29 skirt sits between water and plateau; rather than loosen the measured beach rule map-wide (it is what fixed Balamb island in `.84`), a new **`beach_climb`** flag waives BOTH the water->land test and the cliff gate **for that one goal cell**, with `Garden_CellReachable` asking the flood about the water beside it and `Garden_Plan`'s O(1) refusal skipping it. **A\* routes it in 360 waypoints.** Arrivals past 5 km now announce KILOMETRES ("12.3 kilometres north -- a long walk"). If the engine refuses the climb, the drive stops at a KNOWN COORDINATE with `[GDTRACE]` running, which is the survey. **Verified: 25 ok / 0 bad including Shumi with its exception armed, plus a new harness check that A\* can ROUTE to the beach rather than merely that reachability says yes; parity clean including CLEAR; replay on the `.86` log 194/194 on every check; aboard_test passes.** **BAT: (a) Winhill -> Trabia Garden — expect `[GARDEN] cutscene at (60xxx,-445xx)` then `cutscene over`, with NO reverse and NO replan. (b) SHUMI VILLAGE from anywhere — this is the survey. Whatever happens, the log is the result: if it climbs, expect a park on the beach and "12.3 kilometres north"; if it refuses, expect the hull to stop at the water's edge below (2944,-82048) with the trace running.** **LESSON, twice this week: the user plays this game and I do not. Ask what a place IS before modelling why it refuses.**

**Prior (v0.20.86 LOCAL, NOT MSVC-built — #80: A STATIONARY GARDEN CANNOT ALWAYS TURN, SO WITHHOLDING THE THROTTLE IN ORDER TO TURN CAN DEADLOCK).** `.85` BAT: Centra Ruins, Winhill and Trabia Garden all arrived, `parkBit=1 canDisembark=1` on each. **REPLAY CAME BACK CLEAN FOR THE FIRST TIME IN THE PROJECT — 309 samples, `cell`/`cls`/`clear`/`blk`/`aim` all 100.00%, "at every logged position the model computed what the game computed."** The `.85` polar fix is confirmed in the trace: rows 765, 4, 10, 15, 20, 26, 31, 36 on the Winhill->Trabia crossing all read `clear=32` where `.84` read the row index. **WHAT IS LEFT IS ONE WEDGE THAT HAS NOW APPEARED IN BOTH BATS SEVENTEEN UNITS APART** — (60976,-44518) in `.84`, (60993,-44530) in `.85` — with fourteen seconds of identical trace each time: `pos=(60993,-44530) hd=3616 clear=2 off=582 blk=0 aim=1 guard=0/0/0 mv=0/57 keys=--R-`. **`off` is 582 against a 576 cone, so the executor pressed RIGHT WITH NO THROTTLE — and `hd=3616` NEVER CHANGED.** The engine applies rotation as part of a MOVE, so a refused candidate step leaves the hull neither travelling nor turning; nothing changes `off`, nothing restores the throttle. **Turn -> no move -> no turn, broken only by the 15-second route-progress watchdog, twice.** The cone is right for steering and wrong as an absolute veto: with the bow clear the throttle now comes back on after **`GD_PIVOT_MS` = 1500 ms** of going nowhere — the same forward creep the wall-follow already uses to round a headland, and a healthy pivot finishes in ~1.1 s at the measured 9 u/frame. **WHY NO MATRIX COULD EVER HAVE FOUND IT: `gexec3` turns the hull whether or not the move succeeds (`hd = (hd + ...)` unconditional) and the engine does not, so the loop that produces this wedge CANNOT CLOSE OFFLINE.** That is why 356-run matrices kept coming back clean while the same 17-unit patch wedged twice on the machine. The escape is mirrored on both sides and **the rotation coupling is now written down in `gexec3` as a known model gap** rather than left as an accident. **Verified: replay 309/309 on every check; parity clean including CLEAR; 24 ok / 0 bad; aboard_test passes.** **BAT: Winhill -> Trabia Garden again — that is the leg that wedged twice.** Expect either no stop at all, or a `[GARDEN] pivot deadlock at ... adding throttle` line and an immediate recovery instead of fifteen seconds of silence. One ordinary leg as a regression check. **STILL OPEN: Shumi Village** — the beach is real (53 terrain-9 polys at (2368..3264,-81984..-83264), `b15=0xF7`) but the walk to the village is 12,288 against a 6,000 `WALK_CAP` and the shore step is 295 against the 200 gate; and **the engine's rotation/movement coupling is still unmodelled offline.**

**Prior (v0.20.85 LOCAL, NOT MSVC-built — #80: THE CLEARANCE FIELD NEVER WRAPPED AT THE POLE, FOUND BY REPLAY IN A COMPLETELY CLEAN BAT).** `.84` BAT: *"That worked great! It successfully drove to all the locations I tried... arrived at 2-3km from each destination."* **Nine destinations, nine arrivals, two replans in the whole session** — Balamb Town 2366/0, Fire Cavern 2998/1, Centra Ruins 2887/0, Winhill 2637/0, Trabia Garden 2886/1, Timber 2929/0, Edea's House 2666/0, Tomb of the Unknown King 2049/0, and `Docked at Fisherman's Horizon`. `parkBit=1 canDisembark=1` on every one, **zero `plan FAILED`**, catalog a constant **24** at every location so Fire Cavern and Balamb Town stayed visible off-island, and reachability **662,681 on all twelve floods including cell (528,338) at the Tomb — 655,108 and outside the pocket in `.83`.** Both replans were recovery machinery working: Fire Cavern's guard-drift abort fired at 1,063 units against a 1,000 bound and re-planned in 91 expansions. **REPLAY ON THAT LOG: `cell`, `cls`, `blk`, `aim` ALL 590/590 EXACT** — the `.84` shelf-based beach model is now confirmed against the ENGINE on a real drive, not against a simulator that assumed it. **The one divergence names its own bug:** 12 `clear` samples on the polar crossing Winhill -> Trabia where the game said 27, 22, 17, 12, 6, 2, 0, 3, 11, 18, 26 — **those are the ROW INDEX.** `.69` made the world a torus in both axes and missed the clearance chamfer, which still treated row 0 and row 767 as solid wall. **Silent and exactly backwards:** below `GD_TIGHT_CLEAR` the executor switches to short bow probes and the planner adds `GD_TIGHT_PENALTY` per cell, so a 32-cell band of empty sea at the pole was priced and driven as a narrow channel — on the one route where crossing the pole is the whole point. **59,456 cells had the wrong clearance; 12,288 were below the planner's clear target and are not; 4,096 were below the tight threshold and are not.** Rows wrap now, and since a chamfer only converges in two passes on an OPEN grid the forward/backward pair repeats until stable (3-4 iterations, once, at load). **`gsim3` had the OPPOSITE error** — scipy treats the array edge as background, so the model called the pole open while the C++ called it walled — **and the two cancelled, invisibly, until a real drive separated them. `parity_check.py` now compares CLEAR too** and the harness dumps `gcheck/gdclear.bin` for it. Also: the version string still read `0.20.83` in the `.84` build. **Verified: parity clean including CLEAR on all 786,432 cells; 24 ok / 0 bad; aboard_test passes.** **BAT: one polar crossing — Winhill -> Trabia Garden or Centra -> Shumi's latitude — and grep `[GDTRACE] clear=` near rows 0 and 767; it should read 32, not the row number. Plus one ordinary leg as a regression check.** **STILL OPEN: Shumi Village** — the beach is real (53 terrain-9 polys at (2368..3264, -81984..-83264), `b15=0xF7`) but the walk from it to the village is 12,288 against a 6,000 `WALK_CAP`, and the shore step is 295 against the 200 gate.

**Prior (v0.20.84 LOCAL, NOT MSVC-built — #80: `[0x203EE88]` IS THE GARDEN'S ALTITUDE, AND BALAMB ISLAND WAS A ONE-WAY TRAP).** `.83` BAT: Tomb of the Unknown King drove for the first time, **but Balamb Town would not plan and Fire Cavern vanished from the catalog**, and Shumi Village parked in open water and stranded him. **The log names it in two lines:** reachability is `657082` cells from Balamb and `655108` from the Tomb — a difference of exactly **1,974 cells, every one of them on Balamb island** — followed by `plan FAILED (-44090,-37564)->(15232,-25216) after 655108 expansions`, three times. **The Garden could leave the island the save starts it on and never get back.** **Cause: `[0x203EE88]` is not a flag.** `0x54B49F` copies the active entity's position into `0x203EE80/84/88` as `(x,-z,y)`, so the value the whitelist branch tests is the **hull's own altitude**. Measured across **296 `[GDTRACE]` samples** against the polygon under the hull: terrain 32 gives −254..−58 (**46/46 ≤ 0**), terrain 33/34 gives +208/+210 (**126/127 > 0**) — and the **mesh height is 0 on all three**. So altitude follows the terrain CLASS and `.73`'s "allowed terrain above sea level" was a proxy that coincided at the Tomb and **nowhere on Balamb's coast**: 1,285 beaches map-wide, not one of them there. **`GDC_BEACH` is now terrain 30..32 — 9,504 cells — and the flood is symmetric: 662,681 from Balamb and 662,681 from the Tomb.** Balamb Town and Fire Cavern return **without their berths moving a unit**. **Also fixed:** `Garden_CellReachable` accepted a berth if any of the *nine cells around it* was in the flood, so the catalog promised what A* then had to close a whole component to refuse — it tests the berth's own cell now, and `Garden_Plan` refuses an out-of-flood goal in O(1) instead of 655k expansions. **Berths:** `offline/gen_berths.py` applies all four constraints in one pass (reachable / ashore with a step-off / **same foot landmass** / 2-3 km standoff); **17 of 22 shipped berths still pass and are kept verbatim**, including every one Aaron has driven. 24 of 39 have a berth. **SHUMI: Aaron is right and I was wrong twice.** The walkthrough says *"use the nearby beach to take the ship up onto the land"*, and **the beach is in the data** — 53 terrain-9 polygons at (2368..3264, −81984..−83264), `b15=0xF7`, the island's only Garden-masked ground. It stays hidden **this build only** because (a) the beach-to-village walk is **12,288 units against a 6,000 `WALK_CAP`** and (b) the shore step is **295 units against the 200 gate**, with an unmasked terrain-29 skirt in between — the engine climbs it, and I cannot yet say how. **Next: drive to (3200,−81792), hold the throttle at the wedge, log `mv=n/N` and `[0x203EE88]` per frame.** **`world_garden.inl` was 86,261 bytes — over the 80 KB hard fail, and already 84,567 at `.83`, so THAT BUILD COULD NOT HAVE BEEN PUSHED EITHER.** Split at the end of `Garden_Plan` into `world_garden_plan.inl` (21.4 KB) + `world_garden.inl` (65.6 KB). **Verified: parity clean (BEACH 9,504 both sides); replay 296 samples — cell/clear/aim 100.00%, and the ONLY divergences are 44 `cls` that are the BEACH bit alone plus 7 `blk` downstream, i.e. exactly the intended change; route regression 23 berths x 4 starts x 4 headings = 356 runs, ZERO failures, including the Tomb start that broke; 24 ok / 0 bad; aboard_test passes.** **BAT: from Balamb, drive to the Tomb, then FROM THERE pick Balamb Town — that is the exact leg `.83` refused — and confirm Fire Cavern is still in the catalog after you have left Balamb island. One long regression leg (Trabia or Centra) too.**

**Prior (v0.20.69 LOCAL, NOT MSVC-built — #80: THE WORLD MAP IS A TORUS IN BOTH AXES AND THE GRID ONLY KNEW ABOUT ONE).** `.68` BAT: **clear win — Deling City AND Centra Ruins both drove, so the beach model holds.** Then Centra Ruins -> Shumi Village wedged. Aaron: *"make sure the route planner understands that you can go around the world... you go down around the southern pole."* **The inconsistency was ours:** `CalculateWrappedDistance` and `TorusBearing` have wrapped in y since the beginning, but the GRID did not — every row-neighbour test was guarded by `nr < 0 || nr >= GD_ROWS` and `Garden_BuildEnd` hard-wired the last row's south edge shut. **So the planner MEASURED over the pole and could not ROUTE there** — the heuristic pulls toward a shortcut absent from the graph, which is exactly the shape that yields a long confident detour ending in a wedge. **Rows now wrap like columns:** seam is a normal edge (south edges 669,555 -> **670,579**, precisely one row of 1,024); `GdEdgeOpen`/`GdStepOpen`/`GdSnapWalk`/the flood/`Garden_CellReachable`/the A* loop all take rows mod `GD_ROWS`; new `GdRowDelta`/`GdColDelta` give the heuristic and both endpoint tests the shorter way round per axis. **THAT ALONE WAS NOT ENOUGH — the simulation caught the second half:** the route crossed the seam and the hull still would not drive a step of it, wedging 27.7 km short at `(24777,97926)` ON the seam, because **`GdLineClear`'s cell-transition check wrapped `dc` but not `dr`** — a chord over the pole read as a 767-row jump and every probe returned blocked. Both deltas now wrap. **LESSON: a wrap-around assumption has to be made everywhere at once — distance, bearing, grid edges, flood, heuristic AND the line probe — or the halves disagree and the disagreement looks like a wedge.** That is twice in two builds that fixing the planner exposed a matching gap in the executor. **Verified: Centra Ruins <-> Shumi Village arrives in BOTH directions over the pole, 0 replans; FULL MATRIX 26 destinations x 6 starts x 3 combos = 450 runs, ZERO failures; offline model and C++ agree cell-for-cell on all three grid totals (678,223 / 671,236 / 670,579); 26 ok / 0 bad; aboard_test passes.** **BAT: Centra Ruins -> Shumi Village, the leg that failed** — expect it to head SOUTH over the pole, which will sound wrong and is correct. Plus one ordinary leg (Balamb -> Trabia / Deling City) as a regression check. **STILL OPEN: the coordinate audit** — 19 catalog entries sit >600u from any wmsetus location record (Edea's House 6.4 km, White SeeD 4.5 km, Shumi 4.1 km, Esthar City 7.1 km, Lunar Gate 9.1 km, Cactuar Island 17.6 km); FH proved that class of error is real, silent and expensive.

**Prior (v0.20.68 LOCAL, NOT MSVC-built — #80: YOU GET ONTO A LANDMASS BY DRIVING UP A BEACH, AND BEACHES ARE RARE).** Aaron supplied the model the grid was missing: *"to get up on land masses you must pilot the Garden up a beach coastline... once you go up the beach you pilot the Garden overland to get close to the location."* **Counted in the data: only 8,565 cells on the whole map are a Garden-navigable sea cell meeting Garden-navigable land across a step under the 200 gate.** Everywhere else the coast is a 200-400 unit escarpment the engine refuses. **Centra Ruins' berth is a LAND cell (20 of 26 are) and the nearest climbable beach is 4,574 units away at (5824,50752)** — flooding from that beach with the engine gate DOES reach the berth, so the route was always there. **Two faults stopped it being driven.** (1) **EXECUTOR cut the corner**: a beach is 1-2 cells wide and the 1536u lookahead let the hull aim past the gateway and arrive at the land cell across a boundary that is NOT climbable — engine refuses, hull holds UP at the water's edge. That is the `stalled at (3446,36029)` / `stalled at (-8879,56836)` signature from BOTH BATs. **Lookahead now collapses to one waypoint when clearance < 3 cells.** (2) **PLANNER could not get ashore at all**: `.67`'s sea-first mask banned land beyond six cells of an endpoint, and the beach is 4,574 out. **Land is now biased (x2.5), not banned** — A* stays healthy, plans 22-34 ms. **PROCESS FAULT OWNED: the offline model and the shipped C++ had DIVERGED** — `gsim3.py` kept a x12 penalty while `world_garden.inl` carried a hard sea-first mask, so the offline matrix was passing a build that did not exist. That is how `.67` shipped broken. **They now carry the identical rule, and the matrix gates the release.** **Verified: FULL MATRIX 26 destinations x 6 starts x 3 speed/turn combos = 450 runs, ZERO failures**, Centra arriving from both BAT stall points with 0 replans; 26 ok / 0 bad; aboard_test passes. **BAT: Timber -> Centra Ruins and FH -> Centra Ruins.** Expect the hull to run open water, then slow and thread a specific beach, then drive overland to the berth. Also one previously-good long leg (Balamb -> Trabia / Deling City) as a regression check. **STILL OPEN: the coordinate audit** — 19 catalog entries sit >600u from any wmsetus location record (Edea's House 6.4 km, White SeeD 4.5 km, Shumi 4.1 km, Esthar City 7.1 km, Lunar Gate 9.1 km, Cactuar Island 17.6 km). FH proved that class of error is real, silent and expensive.

**Prior (v0.20.67 LOCAL, NOT MSVC-built — #80: THE PLANNER WAS ROUTING THE GARDEN OVERLAND).** `.66` BAT: FH works (confirmed twice), Centra Ruins failed from every start — stalled at `(3446,36029)` from one, `(-8879,56836)` from another. **`stalled` means the executor is holding UP and the hull is NOT MOVING: the engine is refusing.** Both points sit exactly at a water's edge with terrain rising inland (0 -> -27 -> -381 over two 128u steps in one case). **Replaying the leg offline says why: the route from (-8879,56836) was 66 of its 67 waypoints ON LAND, straight across the Centra continent.** The Garden mask admits a lot of land (47% of terrain 7 carries bit 0x20) and the planner took that literally — **but the mask says where the hull may BE, not how it may GET there.** Every Centra coast is an escarpment of 200-400 units per 128u and the 200 gate refuses the climb, so the hull drove to where the route left the sea and stopped. **Fix: the A* is SEA-FIRST** — foot-walkable cells excluded outright, with a permissive retry if no sea route exists at all. **TWO alternatives were implemented and REJECTED ON MEASUREMENT: (1) banning land globally loses ALL 26 berths** (every berth is a coastal cell that is both Garden- and foot-walkable — that is what a berth IS; 665,412 reachable cells -> 184,745); **(2) a x12 cost multiplier routes correctly but wrecks A*'s heuristic** — the search degenerates toward Dijkstra and one plan took MINUTES, a frame-rate hazard in-game, not just a slow harness. The hard mask keeps every edge cost at 1.0 so planning stays **12-28 ms**. Land is exempt within **six cells** of either endpoint because many berths ARE land cells (every walk=0 berth — the hull parks on the destination). **The failing leg: 17.5 km with 66/67 overland -> 38.8 km with 17/127, running the coast by sea.** Coordinate re-checked FIRST per the `.65` lesson — Centra matches wmsetus record 16 exactly, so this really was the route and not another bad input. **Verified: Centra Ruins arrives from 8 starts x 2 combos, 0 replans, INCLUDING BOTH BAT STALL POINTS.** 26 ok / 0 bad; aboard_test passes. **CAVEAT: the broader 26-destination matrix did NOT finish this session** — the targeted Centra run is what backs this build, so watch for regressions on other long legs. **BAT: Timber -> Centra Ruins and FH -> Centra Ruins, the two that failed.** Expect noticeably longer routes that hug the coast by sea. Also re-drive one previously-good long leg (Balamb -> Trabia or Deling City) to check nothing regressed. **STILL OPEN: the coordinate audit** (19 entries >600u from any wmsetus record) and the unfinished matrix.

**Prior (v0.20.66 LOCAL, NOT MSVC-built — #80: LIMIT-CYCLE BRAKE on the wall-follow).** `.65` BAT: **the FH coordinate fix WORKS** — Aaron drove Balamb Garden to Fisherman's Horizon, then FH to Timber, both clean. Then it wedged 19 km short of Centra Ruins, and that is a separate, older fault. **The position track names it outright: the hull ran back and forth along the SAME 1100-unit diagonal between (2996,36650) and (3653,35747), ~5 s a lap, for 90 s — and NO `bow blocked` was logged at the wedge, so it was never against terrain. It was steering itself in circles.** **Mechanism: the `aimClear` half of the Bug2 leave condition.** Wall-follow turns the hull away; from the new heading the aim is LOS-clear so the guard releases; the hull swings at the goal; the bow blocks; the guard re-engages and turns it away. **Nothing in that loop requires the hull to have got CLOSER, so it runs forever.** `aimClear` is a statement about geometry from where you stand; leaving a wall needs a statement about PROGRESS. **THE OBVIOUS FIX WAS IMPLEMENTED, MEASURED AND REJECTED** — demanding progress before any aim-clear release **regressed four routes** in the offline matrix, because in a tight coastal approach the guard then holds on past the berth. Recorded because it looks right and is not. **Shipped instead: arm the brake by EVIDENCE.** Count guard engagements that happen without the hull having got closer since the last one; only when **three** stack up call it a cycle, then flip the side and commit for **3 s with no early release**. A normal wall-follow never reaches the counter and is unchanged. Plus a **15 s wall-follow timeout** so no orbit is unbounded. Log: `wall-follow to the left (limit cycle -- reversing the side and committing)`. **Verified: offline matrix 26 destinations x 6 starts x 3 speed/turn combos = 450 runs, ZERO failures** (the rejected variant: 4 failures on the same matrix). The shipped C++ and `offline/gexec3.py` carry the identical rule, so the matrix tests what ships. Grid/berths/coordinates unchanged from `.65`; 26 ok / 0 bad; aboard_test passes. **BAT: drive Timber -> Centra Ruins, the leg that wedged.** If it hits the brake you will see the `limit cycle` line in the log. Worth also repeating FH and one long crossing to confirm nothing regressed. **STILL OPEN: the coordinate audit** — 19 catalog entries sit >600u from any wmsetus location record (Edea's House 6.4 km, White SeeD Ship 4.5 km, Shumi Village 4.1 km, Esthar City 7.1 km, Lunar Gate 9.1 km, Cactuar Island 17.6 km). FH proved that class of error is real and silent.

**Prior (v0.20.65 LOCAL, NOT MSVC-built — #80: THE FH COORDINATE WAS WRONG BY 30 KM).** Aaron: *"You can drive Garden back to FH at any point after it becomes mobile"* — then he drove in by hand, and that log answered in one line what four builds could not: `Exited world map ... lastPos=(18895,-2122)` then `ChaseDetector: name debounce settled: id=0x025A name='fhdeck2'`. **The catalog had FH at (48811,-1653). The real FH is (20480,-2560) — wmsetus location record 13, 1644 units from where the transition fired.** The old point is terrain 7 on the Esthar west coast, not Garden-navigable, no disembark bit within 15 km; the real one is **terrain 28, a platform in OPEN OCEAN, Garden-navigable, with 122 Garden-disembark cells clustered around it.** **Every geometric conclusion from `.59` onward was correct ABOUT THE WRONG PLACE** — the 2273-unit standoff, the eight contact cells, the coastal wall gap, the railroad-side reasoning: all of it accurately describes a piece of Esthar coastline that has nothing to do with FH. **The `.64` trigger-table reading was ALSO sound and is kept** (the programs really do gate on vehicle/story/region, and there really is only one Garden clause) — it was simply fed a coordinate 30 km off. **FH's real segment (col 18, row 12) carries NO region byte, so no trigger program and no vehicle restriction: the engine let the Garden in at ~1650 units.** **THE LESSON, recorded in the source: applying a correct analysis to a wrong input is how you ship a confident wrong answer — validate the INPUT as hard as the analysis.** FH is now an ordinary berth at (20480,-2560), walk 0; **26/39 reachable**. Verified from 8 starts x 4 speed/turn combos: all arrive, 0-1 replans, and **every run crosses the ~1650u trigger radius before arrival**, so the field should load on approach. `drive_in`/nose-in/dock-site/patrol machinery retained but unused. **COORDINATE AUDIT: all 39 catalog entries cross-checked against the 64-record wmsetus location table — FH now matches exactly, but 19 sit >600u from any record.** Some may be legitimate (Alien Ship sidequest points, offshore islands may not be in that table) but **Edea's House 6.4 km, White SeeD Ship 4.5 km, Shumi Village 4.1 km, Esthar City 7.1 km, Lunar Gate 9.1 km, Cactuar Island 17.6 km deserve the same scrutiny FH just got** — logged, not touched. **BAT: drive the Garden to Fisherman's Horizon. Expect the field to load on approach, before the berth.** Then one ordinary coastal drive (Dollet / Shumi Village) to exercise the `.63` off-grid recovery.

**Prior (v0.20.64 LOCAL, NOT MSVC-built — #80 ANSWER FOUND IN THE GAME FILES: THERE IS NO GARDEN ENTRY FOR FISHERMAN'S HORIZON).** Aaron: *"Go back to the game exe and world map files and pin this down."* Right call — four builds went into geometry and the answer was never in the geometry. **The world-map field-entry programs (wmsetus.obj mod-section 8 @ offset 2928, decoded at v0.14.93, sitting in `src/world_map_trigger_data.inl` and already trusted by the foot planner) contain EXACTLY ONE clause in the whole 38-program table that names the Garden: program 20, locID 0x0172, topVeh `TRIG_VEH_GARDEN`, story 636..3899, clause region 0x0C.** Mapping the 32x24 segment-region byte map (mod section 2 @ offset 588, one byte per 8192u segment) onto the catalog: **region 0x0C is the northern sea whose only catalog destination is TRABIA GARDEN. FH is region 0x0A, and every clause on 0x0A is FOOT or FOOT_ALT.** No Garden clause for FH exists anywhere, so no amount of steering can fire one. **Four independent facts now agree:** no Garden clause in the trigger table; FH's platform carries no Garden disembark bit within 15 km; it DOES carry the Ragnarok landing bit; and the hull cannot come within 2273 units of the marker from any bearing, with all eight water/land contact cells now swept without a handoff. **The docking Aaron remembers is the scripted story event, not a repeatable trigger.** FH now gets its own honest catalog line (*"the world map has no Garden entry for it -- you reach it on foot or in the Ragnarok"*); reachable 26 -> 25. **NEW `[GARDEN] trigger state` log on boarding** — the live story word, whether program 20 is inside its 636..3899 window at all, and the hull's region byte — so this is checkable from a log instead of trusted. **If that line says EXPIRED, program 20 is dead at Aaron's story point and even Trabia Garden has no live Garden drive-in.** Also fixed the `.63` patrol, which set wantUp=false on a blocked bow and so spun on the spot for 82 s. Dock machinery kept and wired to Trabia Garden (the one real Garden clause) but NOT enabled — Trabia's berth already lands on the destination with 0 walk, so there is nothing to gain by risking it. Grid and berths unchanged; 25 ok / 0 bad; aboard_test passes (its `WmSafeReadBytes` stub now refuses unplanted addresses instead of dereferencing them). **BAT: board the Garden and read the `[GARDEN] trigger state` lines — that is the whole point of this build. Then try FH from the catalog and confirm the honest refusal, and drive one ordinary coastal destination (Dollet / Shumi Village) to exercise the .63 off-grid recovery.** **NEXT, if the story word says program 20 is live:** the one experiment left is driving the Garden into Trabia Garden to see whether a Garden field-entry clause fires at all — that would confirm the mechanism and close the model.

**Prior (v0.20.63 LOCAL, NOT MSVC-built — #80: OFF-GRID RECOVERY; the hull can stand on a cell the planner calls blocked and be completely paralysed by it).** `.62` BAT gave **two results**. (1) **The wall gap is RULED OUT** — the run reached site 0 and swept it properly (`nose-in site 0 phase N` at (45643,507)/(45653,455)/(45539,811)/(45574,692), each within 60-80 units of the three contact cells) and nothing fired. Dropped from the table; the untried **south tip goes first now**. (2) **A real bug with nothing to do with FH**: on the way to site 1 the hull froze at `(-16605,-1050)` for 90 s — `bow blocked` on EVERY heading, `remain` unchanged across four replans, position constant. **Its FINE cell there is terrain 33 at height 0, open water; it is the 256 cell around it that is blocked, by a cliff in another corner.** The planner grid is deliberately conservative (a cell counts only if all four 128u sub-points do), so **every coastline carries a fringe of cells the engine lets the hull skim and the grid calls solid** — and once inside one, EVERY probe fails: `GdLineClear` rejects its first sample because that sample is still in the blocked cell, and `GdStepOpen` reads edge bits a blocked cell never carries. The wall guard then finds no clear heading anywhere and nothing moves. **There was no escape from that state at all.** **Fix: the executor checks its own cell first** — off-grid it stops asking the grid anything, steers at the nearest traversable cell centre with the guard bypassed, reverses after 6 s if that fails, and re-plans on return (`[GARDEN] OFF-GRID at` / `back on the grid at`). **This is general robustness, not an FH fix — any coastal berth could have produced it.** **Also new: the SHORELINE PATROL.** When every discrete contact point has been pressed, the run hugs the coast for 90 s aiming at the marker, tracing once a second. **If that finds nothing, there is no world-map proximity trigger for FH anywhere the hull can reach** — the eight-cell contact list plus a full coast patrol leaves nowhere else — **and the search moves into the world-map event scripts.** **world_garden.inl also breached the 80 KB CI hard fail (82,918 bytes) and is SPLIT** at the end of `Garden_BuildEnd` into `world_garden_grid.inl` (grid + build, 25.7 KB) and `world_garden.inl` (everything that reads the grid, 58.6 KB); no code changed in the move, `world_map.cpp` includes grid first, both harnesses reproduce the same totals. Verified: south tip approach 35.6 km direct, drivable 6 starts x 4 combos, zero failures; grid/berths/harnesses unchanged (26 ok / 0 bad, aboard_test all pass). **BAT: drive to Fisherman's Horizon and let it run to the end** — south tip sweep, then "Searching along the shore", then either a dock or the honest "Searched the whole shore". **ALSO worth one ordinary drive somewhere coastal (Dollet or Shumi Village) to exercise the off-grid recovery**, since that is the change most likely to matter elsewhere. **QUESTION OUT TO AARON:** whether the FH docking is available at his point in the story at all, and whether re-docking after the FH story beat is a thing the game still offers — if it is not, no amount of driving will ever trigger it and FH should be re-labelled in the catalog rather than chased.

**Prior (v0.20.62 LOCAL, NOT MSVC-built — #80: stop betting on one dock site; ask the walkmesh which points of FH the hull can actually TOUCH).** `.61` BAT: *"Still failed to reach FH."* **The drive was CLEAN** — 107 waypoints, direct 35 km route, approach reached in 19 s, no cliff wedge, `[GARDEN] cliff gate: 671236 east edges and 669555 south edges open` exactly as predicted. **The `.61` grid fix holds.** What was wrong was the press point: the screenshots show the hull grinding against a bare grey **terrain-29 cliff face** with the rail bridge marching out over the ocean behind it. No city, nothing to dock with. **The question I should have asked two builds ago is not "how close to the marker can the hull get" but "which points of FH can the hull TOUCH".** Take FH's foot landmass (the label containing the marker — **1,842 cells, its OWN component, not Esthar's**) and list every Garden-water cell on the map directly adjacent to it with no wall cell between: **exactly EIGHT, in two clusters** — the **wall gap** `(45632,576)/(45632,704)/(45760,320)`, 3.6-4.0 km from the marker on the FAR side of the rail bridge, and the **south tip** `(48832,-7360)...(49472,-8128)`, 5.7-6.5 km on the Balamb side. **Nothing else on the map touches FH at all, and the `.61` press point `(46850,-2970)` is in neither cluster** — it could never have worked. **`.60` picked the gap correctly and NEVER REACHED IT** (the cliff bug wedged the hull 50 km short), so the gap has still never been tried, and the "120 km detour" `.61` rejected it for is **~70 seconds** at the hull's real speed (the `.60` log clocks 30 km in 17 s) — a bad reason. **Fix: `s_gardenDocks[]` lists EVERY touchable site per drive_in destination and the run works through them** — sweep site 0, and if nothing fires drive to site 1 and sweep that (*"No dock here. Trying the other side."*) instead of reporting failure. Nose-in now traces the hull once a second, so a sweep that finds nothing is still evidence. **Verified:** both approaches drivable from 5 starts x 3 combos (site 0 route 120 km, site 1 36 km); engine-grid sim puts the sweep **within 15-65 units of the real contact cells at both sites**, and site 1 covers all five of its contacts. Grid/berths/harnesses unchanged from `.61` (26 ok / 0 bad, aboard_test all pass). **BAT: drive to Fisherman's Horizon and let it run — it will sail around the rail bridge (about a minute), sweep, and if that fails announce "Trying the other side" and sweep the south tip. Either it docks or we get the honest "tried every point on its shore the hull can reach".** In the latter case grep `[GARDEN] nose-in trace` — **if both sites come up empty the docking is not a world-map proximity trigger at all, the eight-cell list leaves it nowhere to hide, and the search moves into the world-map event scripts.**

**Prior (v0.20.61 LOCAL, NOT MSVC-built — #80: the 800 gate is right for a RAMP and wrong for a CLIFF).** `.60` BAT: *"Got stuck ~50 km away."* **The F11 screenshots cracked it in seconds — the hull was not adrift, it was pressed against a two-tier CLIFF COASTLINE with the rail line along the top**, at `(-5127,-5196)` on the Galbadia east coast; the log has it holding UP for 90 s across nine stall/replans without moving 400 units. Those coastal polys **do** carry the Garden mask; what stops the hull is the 200 gate, climbing 227 units from sea level in ONE move step. **The `.57` derivation (256u cell = four 64u steps ⇒ budget 800) is correct for a ramp, whose drop accumulates across the cell, and wrong for a cliff, where the whole drop is one step however you quantise — and averaging four sub-heights into one cell height ERASES the cliff.** That is how a course got planned into a coastline. **Fix: apply the gate where the cliff is still visible — between the 128u SUB-POINTS the rasterizer already samples, at the engine's own 200.** Cell traversable only if none of its four internal sub-pairs is a cliff; edge open only if at least ONE of the two sub-point crossings is climbable (new bits `GDC_OPEN_E`/`GDC_OPEN_S`, honoured by the flood, the A* and `GdLineClear`; diagonals need one of their two L routes). **This satisfies BOTH BATs and nothing else does** — at the Centra shelf every sub-pair is far under 200 so the `.56` wedge does not return; at the Galbadia cliff they are 227/249/251 so the edge closes. Map-wide it shuts **14,401 of 5,466,034 sub-edges (0.26%)**; 678,223 cells traversable, 665,412 reachable, and **C++ and the independent Python model agree cell-for-cell on all three**. **Whole berth table re-derived and re-validated: 25 park-and-walk berths, each driven from 8 starts x 4 speed/turn combos, ZERO failures** (Balamb Town 384→256, Trabia 128→0, Tomb 256→286, Edea's House 724→1159). Reachable set unchanged at 26/39. **FH back on the BALAMB side:** `.60` read Aaron's railroad warning as a diagnosis and moved the approach past the far end of the rail bridge — the only wall gap is there, but reaching it is a **120 km route for a place 35 km away**, and the wedge happened en route. From the Balamb side it is 35 km direct, 0 replans; the press point is the FH marker and the hull comes to rest in the cove at ~`(46850,-2970)`, **2273 units off it, the closest the Garden mask allows from ANY bearing on the map**. `.60` never actually pressed — it reported honestly from 3204 units out — so **if the docking trigger is proximity anywhere in the 2300..3200 band, this reaches it**. **BAT: drive to Fisherman's Horizon, and drive one long crossing that used to hug a coast (Balamb → Centra Ruins or Winhill → Balamb Town) to confirm no cliff wedge.** Expect `[GARDEN] cliff gate: 671236 east edges and 669555 south edges open` at grid build. **NOTE the foot side still uses 800 and is deliberately untouched** — 200 at 128u over-blocked it in `.58` (severed Edea's House) and there is no finer foot data to sub-sample; **Chocobo Forest 4 / the Esthar coast merge is still open and is now the only place that model is suspect.**

**Prior (v0.20.60 LOCAL, NOT MSVC-built — #80: the FH dock is a GAP IN THE COASTAL WALL).** `.59` BAT: *"Failed to dock at FH... Garden needs to steer into the FH model, not pull up alongside it - drive right into it. Also, make sure Garden is on the correct side of the transcontinental railroad."* **Both constraints are one geometric fact.** `.59` pressed at FH's location marker `(48790,-1788)`, which sits **2.6 km inside ground the Garden mask never permits** — the nearest Garden-navigable water to it on ANY bearing is 2560 units, so that press could never have worked from anywhere. And the transcontinental rail bridge (terrain 27) **severs the ocean in a solid 512-unit band at z ~ -1536..-1920**; the `.59` approach `(46016,-3392)` was SOUTH of it, the dock is north — exactly Aaron's warning. **The data names the dock unambiguously:** an adjacency scan of every Garden-water cell within 7.7 km of the marker, for water touching FH's landmass with no wall cell between, returns **exactly one place** — a ~450-unit gap at x~45700, z~+320..+770 (contacts at z=320/576/704). Every other cell of that coast is terrain 29, the Esthar mountain wall. One opening in a continuous wall, at the one destination you are told to drive INTO. **Fix:** `GardenPark` gains `dock_x`/`dock_y` (press point, named separately from the marker; zero = use the marker, so no other row changes meaning); FH approach `(46016,-3392) -> (45440,640)`, dock point `(46400,560)` straight east through the gap, `walk_units` 0. **The NOSE-IN phase now backs off between sweep positions** — simulated on the engine grid a JAMMED HULL CANNOT RE-AIM, and without a back-off phases 1..6 moved it zero units and re-pressed the identical spot; each phase after the first now reverses 900 ms first. **Verified:** approach reached 28/28 (7 starts x 4 speed/turn combos), 0 replans, worst error 283u; nose-in sim presses the shore at z = 618, 718, 502, 861, 330, covering all three contact cells; `gridtest` 26 ok / 0 bad; `aboard_test` ALL CHECKS PASSED. **BAT: drive to Fisherman's Horizon.** Either *"Docked at Fisherman's Horizon."* or the honest *"could not dock... stopped alongside it"* — in the latter case grep `[GARDEN] nose-in phase` for the exact points tried, which tells us whether the trigger is proximity-based at all. **Also still open — Chocobo Forest 4** and whether the 800 foot gate over-merges the Esthar coast.

**Prior (v0.20.59 LOCAL, NOT MSVC-built — #80: FH is a DOCKING destination).** Aaron: *"You dock the Garden at FH. Driving the Garden into FH triggers the docking."* That explains what `.58` could not: **FH's whole platform carries the foot bit and NEVER the disembark bit `byte15 & 0x02`** — not one such cell within 15 km. Not missing data: you don't step off there, so nothing is marked. **The bit's meaning is now corroborated map-wide — every landmass with NO disembark bit is one you can't step onto from a vehicle, and all of them carry the Ragnarok LANDING bit (`byte14 0x80`) instead: FH, Cactuar Island, both Islands Closest to Heaven/Hell, the Great Salt Lake shore.** New `drive_in` class: park_x/y is an APPROACH point, arrival is the world map handing off to a field (`Poll`'s exit edge asks `Garden_LeavingIsArrival()` first), and reaching the approach point with nothing fired reports honestly instead of faking an arrival. Approach (46016,-3392) chosen by the same validate-by-sim pass as the berths (the two closer candidates wedge 8/28 and 7/28). **26/39 reachable.** NOTE the docking is NOT in the decoded wmsetus §8: its only Garden clause is program 20 locID `0x0172`, and **field 370 is Lunatic Pandora, not FH** — so either the docking lives outside §8 or locID numbering ≠ field-id numbering. Recorded so nobody repeats that search. **OPEN — Chocobo Forest 4:** at the 200 foot gate it is its own 25,999-cell landmass; at the faithful 800 it MERGES into the 134,828-cell Esthar mass, and the merge is what excludes it. Aaron says the Esthar continent is walled by mountains you can't cross on foot either, so 800 is over-merging somewhere on that coast — worth a look before trusting any Esthar-adjacent verdict. **BAT: drive to Fisherman's Horizon — either "Docked at Fisherman's Horizon." or the honest "as close as it gets" line, which tells us where the approach point needs to be.**

**Prior (v0.20.58 LOCAL, NOT MSVC-built — #80 berth model corrected; Aaron caught it).** `.57` reported Shumi Village, Edea's House and Fisherman's Horizon as Ragnarok-only. **Two errors.** (1) **The SAME 4x gate mistake as `.57`, applied to the FOOT side** — foot components used 200 between 128u cells, but the foot move step is `0x20` = 32u so a 128 cell is four steps and the budget is **800**. At 200 it severed real landmasses (Edea's House cut off from Chocobo Forest 7's). Fixing the Garden side and not the foot side was an oversight: it is ONE derivation and applies to every vehicle. (2) **A berth is a PAIR of cells, not one.** The old rule wanted one cell to be Garden-traversable AND carry the disembark bit; physically the hull stops on water and the player steps off onto adjacent ground. **Shumi Village's own cell carries the disembark bit and is NOT Garden-traversable** — the old rule could never see it at any gate. Berth = hull cell + step-off cell (bit 0x02, foot, destination's landmass) within 384u, 6 km walk cap. **The table is now VALIDATED BY CONSTRUCTION: every candidate is simulated from 7 starts x 4 speed/turn combos and the first drivable one ships** (3 needed a fallback: Chocobo Forest 5, Shumi Village, Alien Ship 1). **25/39 reachable and the unreachable set is exactly Aaron's description** — the Esthar continent behind its mountain wall, the offshore islands, and DSRC. **Walks collapse:** Dollet 3415→1336, Galbadia Station 4891→0, G-Garden 4529→0, Choco 5 3238→1669; 14 of 25 berths are now within 400u and 9 are on top of the destination. Matrix **200/200**. **OPEN — Fisherman's Horizon:** still reports unreachable, and NOT for either reason above — there is **no disembark-bit cell within 15 km of FH**, its whole platform carries only the foot bit, so no rule built on that bit can see it; loosening to "any foot ground beside Garden water" re-admits the Esthar coast and contradicts the mountain wall. **Ask Aaron how you actually step off the Garden at FH.** **BAT: re-drive Shumi Village and Edea's House (both were refused before), plus Galbadia Station whose berth is now on top of the destination.**

**Prior (v0.20.57 LOCAL, NOT MSVC-built — #80 fix from the .56 BAT: the CONTINENTAL SHELF was reading as a wall).** Centra Ruins drive oscillated between (2885,36904) and (3920,35354) five times, logging `bow blocked` continuously **in open ocean**, `remain` stuck at ~27,000. **Two quantisation errors.** (1) `GdProbe` compared the hull's 256u cell to the cell one WHOLE 640u probe away and applied the 200 gate to that jump — a 640u probe spans 2.5 cells, so any slope longer than the probe accumulates past 200 though every cell step is legal. At the wedge the per-cell steps were 0/107/166/85/48/62/32/17 and the 2.5-cell jump 299. **All 15 logged `bow blocked` positions reproduce exactly** under that rule (`offline/garden_probe_repro.py`). `GdProbe` is now `!GdLineClear()`, which already marches the gate. (2) **The gate is 4x too strict at cell scale**: the engine tests |dH| >= 200 between polys ONE MOVE STEP apart and the Garden step is `0x40` = 64u, so a 256u cell = FOUR steps and the faithful budget is **800**. Map-wide, 200 blocks 2,955 edges + 3,990 cells; 800 blocks **ZERO edges** and one degenerate cell — the Garden's permission is the MASK (byte15 bit 0x20), not a slope rule. **Berths improved as a by-product: Galbadia Garden 4529 -> 0 units of walking, Galbadia Station 4891 -> 362.** Reachability unchanged (23/39). **METHODOLOGY FIX: `offline/garden_exec.py` probed the 128u grid, not the 256u grid the shipped code reads — "finer" is not "stricter", it is DIFFERENT, and it is why the .56 matrix missed this. `offline/garden_exec2.py` is fidelity-matched.** Full matrix now **184/184 pairs, was 175/184**; the BAT's own route (Winhill->Centra) goes from 7 replans / 2047u off to 0 replans / 257u. **Unreachable-by-design, confirmed:** Shumi Village, Edea's House, Fisherman's Horizon and 13 others sit on landmasses with ZERO Garden-parkable ground, and in every case the nearest berth is on a DIFFERENT landmass so it isn't walkable either — Ragnarok destinations. **BAT: re-drive Winhill -> Centra Ruins (the leg that failed) — expect a clean crossing with no `bow blocked` in open water. Also re-drive Galbadia Garden, whose berth moved onto the destination.**

**Prior (v0.20.56 LOCAL, NOT MSVC-built — #80 fix from the .55 BAT: you disembark the mobile Garden WITHOUT leaving the world map).** `.55` BAT: Timber drive parked fine, then after hopping off he heard "car" x3 and "Garden" x1 and the town was never entered. **Two independent faults.** (1) The `.55` aboard latch cleared only at a world-map ENTRY — and there ISN'T one when you step off the hull. Log has no entry between the park at 13:24:28 and walking away; backslash at 816u from the berth started a SECOND Garden drive. **Fix: continuous disembark detection that does NOT use the savemap mirror** (its refresh rate is still unproven) — while a Garden drive runs or the engine names the Garden in motion, the live position IS the hull, so record it; a sustained 450u separation from that recorded spot afterwards (700ms hold) = climbed out. Entry classification still runs and still wins when available. (2) **`CheckVehicleChange` committed locomotion 3/33/36/39/48 in three seconds while merely WALKING** — announcing the names AND repointing `GetWorldMapPosition_Active` at those savemap mirrors (the car's is 50 km away at the Missile Base; only the `.217` foot-motion override kept position sane, and only while moving). DEVNOTES has called that byte non-authoritative since `.255` but the commit path still trusted it outright. **Non-foot values now need corroboration** — the engine in-motion id naming the same family, or that vehicle's mirror within 600u of the player — else `[VEH-REJECT]`. Also skipped entirely during a Garden drive (the foot/car drive already had that exemption). Smaller: stall detector doesn't arm in the first 2.5s (the Timber run tripped it 3s in at its own start coord; the spurious replan widened arrival and cost 500u of berth accuracy), already-parked prompt 700→1500u. **`tests/garden_aboard_test.cpp` now replays BOTH BATs verbatim — 13 checks, all pass under g++.** **BAT: repeat Timber. After parking, step off and walk a few paces — expect `[GARDEN] disembark:` and NO vehicle announcements (any that appear should have a matching `[VEH-REJECT]`). Then backslash should give `[DRIVE] Start`, not `[GARDEN] start`. Also try boarding and pressing backslash WITHOUT moving first — that's the one path still resting on the entry classification.**

**Prior (v0.20.55 LOCAL, NOT MSVC-built — #80 fix from the .54 BAT: the engine vehicle id is a VEHICLE-IN-MOTION register).** `.54` BAT: catalog never showed the Garden set, everything said "not reachable", and the post-park backslash started a foot drive. **All three are one wrong assumption.** `[0x020409E0]` reads **0** at every world-map entry and every catalog build and **48 exactly once — four seconds INTO an active drive** (13:08:51 `[VEHID]`), then 0 again while parked. It names a vehicle only while one is MOVING. `.54` asked it at the two moments it is always 0. **Fix: latch, don't sample.** A read of `0x30` is a one-way confirmation; the latch clears only on positive on-foot evidence at a world-map transition, where the savemap mirrors ARE fresh (13:10:34, just disembarked: `|P-char|=0`, `|P-bgu|=1015`) — three-way test (clear / set / **leave alone**) so an ambiguous entry never flips it. Aboard transition forces `s_catalogBuilt=false` and speaks "Piloting Balamb Garden."; `Garden_CellReachable` builds its flood on demand instead of answering false from an empty map; backslash at a berth you're already at says "leave the Garden first"; arrival 384→288 (the .54 park stopped 379u short, turning Fire Cavern's 362u walk into 666u). **`tests/garden_aboard_test.cpp` replays the BAT id sequence + real mirror values as a regression guard (g++, host-compilable, ALL CHECKS PASSED).** NOTE `[VEHPOS32]` never appeared in the .54 log — the locomotion byte never commits to 48, which is expected and is why nothing depends on it now. **BAT: listen for "Piloting Balamb Garden." right after the world map appears (or on first nudge); catalog should be long with the Esthar-side entries saying the Garden can't reach them; at the Fire Cavern berth press backslash BEFORE getting off (expect "Already parked... leave the Garden"), then disembark and press again for the walk-in. Grep [GARDEN] entry classify / aboard / catalog:.**

**Prior (v0.20.54 LOCAL, NOT MSVC-built / NOT BAT'd — #80 mobile Balamb Garden auto-drive).** New self-contained subsystem `src/world_garden.inl` (48 KB): its own 256u traversability grid, its own clearance-weighted A*, its own executor, gated on `GetActiveVehicleId() == 0x30` so nothing runs unless the player is piloting the Garden — the BAT-proven foot/car drive is untouched. **Engine facts read out of FF8_EN.exe this session (primary source, addresses in the file header):** `0x53E6B0 IsPolyWalkable(triple, veh)` is the whole per-vehicle table — foot `byte15&0x80`, car `0x40`, **Garden `0x20`**, chocobo `0x10`, Ragnarok always; `0x53E730` (via `0x54B860`) is the SET-DOWN table — car `0x04`, **Garden `0x02`**, chocobo `0x01`, **Ragnarok `byte14&0x80`**; the 200u step gate is NOT vehicle-dependent. Cross-checked over all 473,193 wmx polys: bit `0x40` is 0-1.1% on forest (re-derives cars-cannot-enter-forest), bit `0x20` is 99% on ocean / 13% on terrain 29 (the Garden sails, mountains stop it), `byte14 0x80` is 0% on ocean+mountain+forest and 97-100% on plains (an airship's landing rule). **23 of 39 destinations have a park point;** the other 16 are the Esthar continent + offshore islands = the engine's own "you need the Ragnarok". Park-and-walk is FORCED: only trigger program 20 (locID 0x0172, region 0x0C) has a Garden clause, so no destination can be driven into. Catalog keeps unreachable entries (announced as such) rather than hiding them; on foot the catalog gains **"Mobile Balamb Garden"** at the hull's live position. **Latent bug fixed on the way:** savemap vehicle mirrors are int32 X / int32 Y / int16 Z / int16 rot, NOT six int16 — a live `[VEHDUMP]` `car_pos` decodes to (-71100,-14815) (Missile Base coast) as int32 and (-6076,...) as int16; the old reader was wrong everywhere |X| > 32767, i.e. all of Galbadia/Esthar. `[VEHPOS32]` logs both. **Offline: C++ and Python grids agree cell-for-cell (WALK 677,934, PARK 65,035, zero disagreements)** — that validates the mesh transform `mz = 196608 - vwy`. Executor simulated on the 128u grid (finer than the planner's) from 8 starts x 4 speed/turn combos: **175/184 pairs, 95.1%**; the 9 failures are final-approach geometry into coastal berths, fail safe, and are listed in `offline/GARDEN_MATRIX.md`. **NOTE for the foot drive: the wall-guard fix in world_garden.inl is the #100/H2 limit cycle** — the existing `world_map_drive_exec.inl` rule turns toward the target side and never moves, which oscillates +-1 turn step against a wall forever. Porting it back is a separate, deliberate change. **BAT: board the Garden -> world map. Confirm `[GARDEN] aboard vehicleId=48` FIRST (this single gate is the biggest unknown). Then cycle the catalog, drive somewhere near (Balamb Town / Fire Cavern), leave the Garden, press backslash again to walk in. Then a long crossing (Deling City / Trabia) and one refusal (Esthar City). Send ff8_world.log; grep [GARDEN] and [VEHPOS32].** Docs: `Plan & Research Documents/Balamb Garden Auto-Drive - offline analysis.md`, `offline/GARDEN_README.md`, harness `tests/garden_harness.cpp`.

**Prior (v0.18.3.309 LOCAL, NOT pushed/BAT'd — #114 pin-escape + #110 RAWCAM diagnostic).** `.308` BAT (Aaron, manual): reached floor 3, "better at actually going down the stairs" but "still gets hung up quite a bit." Log root cause: after .308 fixed WHICH edge the nudge uses, the player still pins at moveDist=0 on rail/knee-wall corners and the recovery REPEATED the same nudge (`tri 9->8 dir=(-0.25,-0.97)` at rec 2/4/6). Offline geometry: the walkmesh path there is clear → the obstacle is COLLISION the walkmesh lacks (rails/hole wall). **#114 fix:** when the player hasn't moved since the last nudge, recovery ROTATES the escape (opposite perp / triangle centroid / back along corridor) and lengthens the nudge 8→32 ticks; resets on real movement. Pin statics in `field_nav_autodrive_helpers.inl`, cleared at drive start; log shows `escapeMode`/`pinned`. **#110 RAWCAM (passive):** disassembly this session established field movement = integer/table math (no FPU trig; sine LUT 0x479950), entity struct (pos +0x190/+0x194, facing candidates +0x08/0x0A/0x0C, tri +0x1FA, stride 0x264), controllable-idx 0x1CE475C, entity table 0x1DCB340. **Camera-0 `.ca` does NOT reproduce measured motion** on gpbig1a/gpbig2a (both `num_cameras=1` → NOT multi-camera). `FIELD_CAM_DIAG` logs per-frame raw pos/delta/tri/facing + a `.ca` dump so we can derive the true transform. Full RE notes: `FIELD_CAMERA_RE_FINDINGS.md`. **BAT diag: gpbig1a then gpbig2a, walk each arrow ~1s on OPEN ground, send ff8_field.log, grep [RAWCAM].** Auto-drive pin-fix BAT'able same session (diag is passive).

**Prior (v0.18.3.308 LOCAL — #113, the recovery nudge was perpendicular to the WRONG EDGE).** `neighbor[e]` pairs with edge **(v[e], v[e+1])** — the v0.17.9.14 FindPortal convention — but the even-phase recovery nudge still used the old **(e+1, e+2)** pair, so every nudge was perpendicular to the wrong segment (often a wall). **Log-proof, no inference:** the .306 wedge's 20 identical `tri 72->71 dir=(0.90,-0.44)` nudges == the old-rule output exactly; the true shared edge `(-1433,-1590)-(-781,-1402)` gives `(-0.28,0.96)` → points at tri 71. Convention check across gpbig1a+gpbig2a: old pair fails shared-vertex on **184/186** links, fixed pair **0/370**. This closes #100's third part (part 1 = #110 axes, part 2 = #112 goal instability). Also corrected the same derivation in the two DISABLED blocks (corridor steering, wall-bias) so re-enabling can't resurrect it, and in `field_nav_settriangle.inl`'s COORD diagnostic (log-only). **NEW OFFLINE HARNESS (this session):** g++-compiles the REAL `field_nav_pathfinding.inl` against the real gpbig1a walkmesh extract and simulates the drive loop (wall-sliding kinematic player); it reproduces .306's goal flip-flop (20/1/22 vs stable 48) and under .307+.308 **all 84 triangle-center starts reach the stairs line**. Point-player sim ≠ engine collision radius — machine BAT stays ground truth. **CONTEXT: Claude is running the BAT loop via computer use this session (Aaron away); .308 authored before .307 was machine-tested, so .308 is the first build actually BAT'd — log attribution stays clean (goal lines vs NOT-reset lines vs nudge-direction lines).** **BAT: calibrate (~2 steps) then drive to the stairs; in any wedge expect VARIED, corridor-pointing nudge directions; .307 expectations still hold (same [A*] goal every re-path, honest Stuck bound).**

**Prior (v0.18.3.307 LOCAL, NOT machine-BAT'd — #112 + the #100 half, ONE change: recovery must CONVERGE).** `.306` BAT: **#111 CONFIRMED — zero `Player position lost` all session.** Aaron: *"navigation really struggled to actually get around... stuck on objects, like the rails around the stairs and the short wall around the hole in the middle of the floor."* The log splits that into TWO faults. **Fault 1 (pre-existing, #110, deliberately untouched):** every gpbig1a drive before the 15:11:44 calibration ran on ca-quantized axes the calibration itself proves were 90° wrong (`camRight (1,0)->(0,1)`; second `[CAM-SNAP-VERDICT] ALTERNATIVE CORRECT` — but still gpbig1a, still consistency not independence). Wrong axes = keys rotated a quarter turn = the drive grinding INTO the rails/knee wall: drive 1 ran EAST while the stairs were SW (dist 1705→1948 while "driving"); drive 4 pressed UL into the wall for 27 s, moveDist=0, **20 IDENTICAL nudges** (`tri 72->71 dir=(0.90,-0.44)` — the #100 signature). Control group: the SAME build drove gpbig2a (flipBand=0, axes right at load) cleanly through 6 wp to the Bottom-crossing exit. The .305 cache cannot help a session's FIRST load (dies with the process); the threshold change stays gated on `gpgmn3`/`gpbrdg1`/`gppark1`. **Fault 2 (NEW = #112, fixed here):** both post-calibration drives had right axes and STILL failed — the final drive ping-ponged tri 8↔9↔10 for 40 s. Mechanism, read straight off the goals: for trigger targets the per-tick **"+300 past the line" offset moves WITH the player**, and the odd-phase/nudge-fallback recovery re-paths seeded `FindNearestTriangle()` with it → the A* goal depended on where the player stood. Drive start & wiggle-completion agree on raw-center goal **48**; recoveries produced **20/1/22** — goal-1's wp `(-2361,-503)` pulls WEST, goal-22's wp `(-1683,69)` pulls NORTH-EAST, so each recovery routed the player toward the opposite cluster, forever ("check ALL passes writing the same field", caught again). **Fix A: capture `rawTx/rawTz` before the offset; every recovery re-path seeds A*+funnel with it.** **Fix B (inseparable — it is what makes "Stuck." reachable, the #100 half):** every ping-pong hop changed triangle, and the v0.17.6.1 tri-advance reset refilled the budget on each hop → `phase=1` on EVERY recovery of the final drive, `MAX_RECOVERY_PHASES` unreachable, silent bouncing until the 40 s timeout. New **4-entry ring of recently-stuck triangles** (statics in `field_nav_autodrive_helpers.inl`, cleared at drive start in `handlekeys`): a fresh triangle still resets, a revisit logs `recovery counter NOT reset` and lets the phase climb to the cap → honest **"Stuck. Distance remaining: N."** Waypoint advances keep their own independent reset, so genuine corridors keep their budget. Nothing landed in `field_navigation.cpp` (still 81,792/81,920 — #37). **BAT: on gpbig1a walk a couple of steps FIRST until "Camera calibrated." (~2 steps today), THEN F9 → Stairs down to Floor 5, from two different spots.** Expect every recovery `[A*]` line to show the SAME goal, `NOT reset` lines where it used to ping-pong, and either the stairs or an honest Stuck within ~40 s. A Stuck log (stable goal + pin location) is exactly the evidence the next change needs — likely the stair-mouth approach geometry. Steps on `gpgmn3`/`gpbrdg1`/`gppark1` give #110 its independent verdict.

**Prior (v0.18.3.306 — BAT'd, #111 CONFIRMED).** **Auto-drive no longer dies on a ONE-FRAME player-position dropout.** `.305` BAT had 11 `Player position lost` stops in ~4 min; the log proved every one a SINGLE-FRAME dropout (position readable the tick before and after) — the engine re-resolving walkmesh placement racing our per-frame poll. A dropout now pauses the drive like an open dialog and gives up only after 30 consecutive bad ticks (~0.5 s); `s_playerEntityIdx < 0` says "Player entity unknown." so the two faults log distinctly; `ReadPlayerPlacementRaw()` owns its own SEH (C2712). `.306` BAT: **zero occurrences — CONFIRMED.** Full chapter in `CHANGELOG.md`.

**Prior (v0.18.3.305 — BAT'd, ALL THREE CONFIRMED; full chapter moved to `DEVNOTES_HISTORY.md` at `.307`).** #109 per-field axes cache works (one `gpbig1a` calibration, then `cached axes re-applied on field load` ×4, no relearn) — **but it dies with the process, so a session's FIRST load still runs quantized** (seen again in the `.306` BAT). #110: `.ca` extracted for ALL 894 fields → `offline/CAMERA_ANGLES.csv` + `offline/CAMERA_ANGLE_SURVEY.md`; engine threshold bracketed **(23.8°, 38.32°]**; the change would flip **93 fields** and stays UNSHIPPED until an independent verdict — predictions on record: `gpgmn3`(+38.80)/`gpbrdg1`(+129.10)→ALTERNATIVE, `gppark1`(−46.80)/`gpbig2a`(−24.76)→QUANTIZER; **two gpbig1a verdicts so far, both ALTERNATIVE, neither independent.** `[CAM-SNAP]`/`[CAM-SNAP-VERDICT]` shipped (log-only). SEPARATE BUG filed: 45 fields carry >1 camera, `LoadCameraAxes` reads camera 0 ONLY, 18 snap to different cardinals (`glwitch1`/`glstage1`/`ggroom1` — all in sewer/gate work). Wedge-repeat seen there, filed against #100 — **the `.307` ring is that work.**

**Prior (v0.18.3.304 — BAT'd, both fixes CONFIRMED).** `[NAV-CAL]` fired and corrected the axes; the bounded arrival test rejected 8 false "Arrived" claims at t=4.2–4.4. What remained was that the correction did not survive field loads → `.305`. Full chapter in `CHANGELOG.md` and `DEVNOTES_HISTORY.md`; shaft specifics in `claude/DDISTRICT_PRISON_SHAFT.md`.

**★ D-DISTRICT PRISON SHAFT — read `claude/DDISTRICT_PRISON_SHAFT.md` in the Claude project before touching `gpbig1a`/`gpbig2a`.** Consolidates the ring geometry, the floor-number sources, both staircases' coordinates and the Z rule, the camera-axes problem, and the recurring traps (catalog never opened; stale-gateway rule scoping; the infinite-line side test, now false-positived at **five** sites). The `.301`–`.303` chapters moved to `DEVNOTES_HISTORY.md` at `.305`.

**Prior (`.297` #85 state exclusion ENABLED):** moved to `DEVNOTES_HISTORY.md`. Short version: the refined three-part rule was correct on all six `glwater3` verdicts across both states; glwater2 is protected structurally by the ANCHOR GUARD rather than by a third BAT.

**Prior #85 chapters (`.286`–`.294`):** moved to `DEVNOTES_HISTORY.md` — read it before touching sewer/gate code. Still open there: `saku4` (Director, positionless by design), `saku5`/`6` (display-only), `hasigo` (promoted, never positioned), `hasigomodel` (spurious 4th "Exit" @(1178,104)).

**Prior (`.260` LOCAL — #82/#83 Caraway `glfurin4` diag).** Awaiting its own BAT. **Older** (`.209`–`.259`): `DEVNOTES_HISTORY.md`.

**★ .211 routenet (`src/world_map_routenet.inl`, 13 nodes/14 edges/1,099 pts):** Galbadia pass edge RETIRED (impassable live); Timber/Dollet route east via junctions `Yaulny Plains` (−31345,−25449), `Hasberry Plains` (−16452,−39492). Transit: junctions + at-mouth starts + target only; `RouteNetPlan` first, grid A* fallback. Camera heading = camYaw + key·512 + bias/2. Tears' Point excluded. MUST-READ before route work: `offline/ROUTENET.md`, `offline/BAT203_ANALYSIS.md`, `offline/TRIGGER_FIRING_AREAS.md`. Research docs = LEADS ONLY.

**★ wmx.obj FORMAT CRACKED** → spec in `Plan & Research Documents/WMX_OBJ_FORMAT.md` (READ before world-map work): 835 segs×0x9000, poly=16B (types @[13,14,15]), vert=8B (UP=neg-Y); locator 0x53DC70 + Z-mirror. Galbadia = one landmass. Walkability = the 200u height-STEP gate (§12).

**Reference fieldIds:** Fire Cavern A `0x0088` `bdview1` ≈(30260,−29221) (two-stage: world trigger → approach field). Balamb Town gate `0x006A` `bcgate_1` ≈(12894,−26776). B-Garden Front Gate 5 `0x00A3` `bggate_6` (NOT `fepic1`).

**Older:** `DEVNOTES_HISTORY.md`. OPEN: **#61** decoder spoken-"L", **#50** Angelo gauges, **#45** (blocked). Handedness: RIGHT=yaw+90 CW.

---

## Active backlog → GitHub Issues

Tracked as **GitHub issues**; `github:list_issues` for the live set. Breadcrumbs: **#34/#35** need a pre-SeeD save + `LINEDIAG_ENABLED=1`; **#37** size-refactor queue; **#43** GF per-level EXP; **#45** blocked; **#88** exits lost after battle; #28; #21. **File new bugs on GitHub, not here.**

**2026-07-31 review — #89-#106** (details on the issues; probes #89-#92 shipped in `.298`). By impact: **#99** victory "Unknown received 0 EXP", 8/16 battles — party array is NOT compacted, emit loop assumes it is · **#100** auto-drive wedges silently, "Stuck." never fired once · **#101** Scan = 34 s of total silence · **#102** party-switch announces "Squall, Squall, Squall" (0 == empty slot) · **#93** party filter never ran on Laguna fields (`kChars` stops at 7) · **#94** Events block still uses the infinite-line test — 4th instance of a bug fixed 3× elsewhere · **#95** phantom Save Point sitting on a gateway line · **#97** Event lines typed `ENT_OBJECT` inflate Object counts · **#98** Director-promoted controllers leak · **#103** item descriptions drop words · **#104** no save confirmation · **#105** false "all addresses found" · **#106** misleading drive refusals · **#96** log hygiene.

**Not a bug:** JAWS intercepts game keys until Insert+3 passthrough; NVDA unaffected.

---

## Architecture, safety & workflow rules

- Read **`DEVNOTES.md`** + **`NEXT_SESSION_PROMPT.md`** at session start; `DEVNOTES_HISTORY.md` only when tracing past decisions. Update both at every version bump AND after every BAT.
- **Version bump = ONE place:** `FF8OPC_VERSION` in `src/ff8_accessibility.h`, paired with a new top `CHANGELOG.md` entry; `push_to_github.ps1` refuses if the top `## vX.Y.Z` ≠ the macro.
- **Filesystem MCP for Windows project files.** `filesystem:edit_file` CORRUPTS on a literal `$` in the replacement — use hex `0x24` or `write_file`. OneDrive throws transient EPERM on rename — retry once. (Cowork Read/Edit/Write also work; its bash sandbox mounts the project, so grepping logs there is fine.)
- **NEVER re-enable the SET3 hook (0x1E)** — interception hangs the infirmary scene. CI-guarded.
- **Victory/transition TTS hooks the text renderer, NOT memory.**
- **F-key handlers gated on `!(GetAsyncKeyState(VK_MENU) & 0x8000)`** (no Alt combos). **F12 = per-session diagnostic ONLY** — grep `VK_F12` and remove old code before hooking a new one.
- **Source size (CI guard):** 60 KB soft / 80 KB hard. Split via textual `#include` of `.inl` from the parent `.cpp` (no header guards/namespaces inside `.inl`; `*_state.inl` statics first). **Split BEFORE the edit that needs the room, not by shaving comments to fit** (`.286`–`.293` deleted explanation until `.294` had to split anyway). **Prove a split inert**: reconstruct the pre-split file from the fragment, build both, diff real output. **NEXT UP: `field_navigation.cpp` at 81,792/81,920 — split it before anything else lands there.**
- **Diagnostics:** gate behind `#define X 0`, don't delete.
- **DO NOT "fix" the 126 placeholder field names.** `FIELD_DISPLAY_NAMES` has 126 entries marked `(???)` whose display name is the raw archive filename ("bgcrash1", "bgdin_1"), and the mod reads them out. **Aaron has ruled on this, 2026-08-16: leave them.** *"They are better than nothing and inform the blind player the scene / field / background / camera changed."* A field name is a change cue first and a place name second. **`NO_NAME_FIELDS` stays a two-entry exception list, not a policy:** field 95 `testbl6` is a Try-again MENU, and field 152 `bgbtl_1` is the Garden fight, which announces its own Game Controls briefing a second later — in both cases something better arrives immediately. Silence is only correct when something else is already speaking.
- **NEVER run git through the Cowork desktop bridge.** It cannot unlink `.git/index.lock` (`Operation not permitted`), so every command leaves one behind and the NEXT push dies on it -- three times now (2026-08-02, -08-12, -08-15). `push_to_github.bat` Step 0 clears a stale lock when no `git.exe` is running, but the rule is: read repo state from the logs or the GitHub API, never by running git on the mounted folder.
- **GitHub commit history is authoritative** — `github:list_commits` before quoting push state.
- **Position resolvers** (`ResolveLatePositions` → `MatchSet3LateCaptures` → `ResolveStructPositions` → centroid): the first three read live entity structs and silently return ANOTHER entity's data for out-of-window slots. Guard = reject a live read whose triangle ≠ the entity's own static SET3 triangle, in **both** struct-reading passes (`.290` guarded one, `.291` fixed it).
- **Entity-catalog identity:** SYM names are unreliable (kanban2 was Xu) — never expose them, and **never map a SYM to a friendly name because it reads like one** (`.288` mapped `ladline*`→"Ladder" on spelling; `ladline7` was the gate blocking Aaron, hidden 3 BAT cycles). Name only on behavioral evidence (sole entry reaching "In range." at the object; SETMODEL id shared with a known gate). `ResolveFriendlyName()` NEVER returns empty — on a miss it returns the capitalized SYM, so "just remove the row" leaks it; check `ENTITY_DISPLAY_NAMES` explicitly. `ENTITY_SKIP_NAMES` must be checked for JSM-injected objects too, not just Backgrounds. NPC/interaction labels are generic ("NPC N" / "Interaction N") only. Announce-time counters need TYPE-based matching for JSM-injected entries (entityIdx ≤ −300), not the legacy `entityIdx ≥ 0` test.
- **Navigation is screen-relative** (v0.17.0): cardinals map to arrow keys; project through `s_camRight/Down` before `atan2`. **F9 auto-drive uses `s_camRight/Down`** (quantized at load); **chase-drive uses `s_driveCam*`** (CALIB) — don't cross the streams. **F9 corridor steering is OFF** (v0.17.6.2).
- **EWM/battle-menu TTS are load-bearing.** EWM preserves "first-to-fill acts first, no skipped turns, natural ally/enemy ratio". Mechanical splits only unless Aaron approves.
- **Arrival detection / coord capture need the underlying decision VERIFIED, not just signal-presence.** Mid-drive replan must honor the same planner-eligibility gate as initial Start; when "fixing" a planner decline don't substitute a different region (the v0.14.95 mistake).
- **A catalog category can empty mid-visit.** Trigger lines are captured once on field entry; after a battle `[LINE-PAIR] captured=0` and every trigger-line exit disappears (#88). Before blaming a code change for "X vanished", check for a battle and compare against the FIRST field-entry block in the log.
- **Fields have STATE** (`.293`–`.297`, ENABLED). Entities can be alternate world-states of ONE object, gating `SET3` on a shared varblock vs different values; the catalog is built from STATIC data so it would inject every state at once → phantom duplicates. Three-part rule, all three parts load-bearing: **(1)** only a CONDITIONAL entity may be suppressed — a `JPF` (0x02) between the guard read and the `SET3`; without one the `SET3` always runs and the guard feeds a later branch. Not a SYM-name split (glwater2 `saku2` is conditional, `ladline6` is not). **(2)** the group must be ANCHORED — some member's value equals the live byte; otherwise the variable isn't encoding this group's state in a way we understand, so leave it alone (this is what keeps glwater2's three coexisting gates safe). **(3)** then suppress conditional members whose value ≠ live byte. Skips catalog INJECTION only; position data stays intact. A shared varblock does NOT imply mutual exclusion — a naive equality rule would have deleted Gate 3, which the `.295` dry run caught before it shipped.
- **One change per BAT cycle.** If a fix doesn't engage, READ THE LOG first; identical outcomes across "fixes" mean the diagnosis is wrong; when stuck, ask Aaron for one specific observation. His domain knowledge is ground truth but his recipes need verification. **When a fix targets one of several passes writing the same field, check ALL of them.** **Ship a diagnostic, not a guessed fix, when the mapping is inferred** (`.293`).
- **Logs:** `ff8_field.log` is large — bash-grep it. `ff8_nav_data.log` logs every player triangle change. **F11 screenshots are gold** — comparing two shots of the same room across saves is what revealed the fallen-ladder state (`.293`). `[MAPJUMP-HOOK]` = live engine oracle for exit destinations. **`[NAV-PROJ] start ... player=(x,y)` gives Aaron's exact position** — measure catalog entries against it before theorising about what's missing.
- **`deploy.bat` version regex** needs the `/B` anchor (v0.15.10.1). Inline-changelog accretion retired (v0.15.12.0) — `CHANGELOG.md` is canonical.
- Every Claude response starts with `## Claude Says`.
